---
import Layout from '../components/Layout.astro';
import LanguageBar from '../components/LanguageBar.astro';

const level1Outcomes = [
  'Terminal, VS Code y Claude Code operativos',
  'Primer repositorio en GitHub con historial limpio',
  'CLAUDE.md propio configurado y funcionando',
  'Prompts operativos que producen trabajo usable',
  'Primera automatización mapeada y documentada',
  'Proyecto publicado con URL real',
];

const level2Outcomes = [
  'Arquitectura de proyectos profesional',
  'Control de contexto y tokens optimizado',
  'Git workflow con commits y ramas',
  'Debugging sistemático sin depender ciegamente de IA',
  'Refactors seguros con validación por fases',
  'SOPs de deploy y documentación operativa',
];

const level3Outcomes = [
  'CLAUDE.md centralizado con contexto migrado',
  'Carpeta /prompts con prompts operativos propios',
  'Workflows documentados y listos para ejecutar',
  'Backlog de automatizaciones priorizado',
  'README operativo completo',
  'Sistema de trabajo con Claude Code desde cero',
];

const servicios = [
  { label: 'Escuela interna de IA para equipos',          color: 'text-emerald-600' },
  { label: 'Sistema de prompts y SOPs operativos',        color: 'text-sky-600'     },
  { label: 'Migración ChatGPT → Claude',                  color: 'text-violet-600'  },
  { label: 'Automatización de workflows de trabajo',      color: 'text-amber-600'   },
  { label: 'Documentación operativa de procesos',         color: 'text-rose-600'    },
  { label: 'Portal interno de formación personalizado',   color: 'text-teal-600'    },
];

// El ecosistema — tres capas: entidad, laboratorio, metodología
const ecosistema = [
  { layer: 'Entidad',      name: 'XNLAB Studio',  desc: 'Diseñamos sistemas, operadores y laboratorios de IA.', dot: 'bg-slate-900',  lbl: 'text-slate-400'  },
  { layer: 'Laboratorio',  name: 'MOONKEY LAB',   desc: 'El entorno de formación para operadores de IA.',       dot: 'bg-emerald-500', lbl: 'text-emerald-500' },
  { layer: 'Metodología',  name: 'Operator Path', desc: 'El método para pasar de usuario a constructor.',        dot: 'bg-slate-400',  lbl: 'text-slate-400'  },
];

// ¿Para quién es?
const audiencia = [
  { tag: '01', title: 'Operadores IA',              desc: 'Ya usas IA a diario y quieres convertir ese uso en sistemas que no dependan de recordar el prompt correcto.' },
  { tag: '02', title: 'Freelancers',                desc: 'Buscas entregar más rápido y con más criterio, automatizando lo repetitivo sin perder el control.' },
  { tag: '03', title: 'Fundadores',                 desc: 'Necesitas que tu equipo opere IA con método, no con improvisación dispersa entre chats.' },
  { tag: '04', title: 'Constructores de sistemas',  desc: 'Piensas en procesos, no en respuestas sueltas. Quieres infraestructura operativa propia.' },
];
---

<Layout title="Inicio" description="MOONKEY LAB by XNLAB Studio — Aprende a operar IA de forma sistemática. Sin código. Sin teoría infinita.">

  <!-- Hero — Atmospheric Portal -->
  <section class="relative overflow-hidden" style="background:linear-gradient(165deg,#030b05 0%,#060e08 38%,#080f09 62%,#0a1a0c 100%)">

    <!-- Atmosphere: depth layers -->
    <div class="absolute inset-0 pointer-events-none select-none">
      <!-- Layer 1: multi-point mist -->
      <div class="absolute inset-0" style="background:radial-gradient(ellipse at 68% 35%,rgba(16,185,129,0.13) 0%,transparent 52%),radial-gradient(ellipse at 12% 68%,rgba(6,78,59,0.18) 0%,transparent 48%),radial-gradient(ellipse at 88% 8%,rgba(4,120,87,0.08) 0%,transparent 38%),radial-gradient(ellipse at 42% 92%,rgba(2,44,34,0.22) 0%,transparent 42%)"></div>
      <!-- Layer 2: SVG fractal noise -->
      <svg class="absolute inset-0 w-full h-full opacity-[0.04]" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice">
        <filter id="atm-noise" x="0%" y="0%" width="100%" height="100%">
          <feTurbulence type="fractalNoise" baseFrequency="0.68" numOctaves="4" stitchTiles="stitch"/>
          <feColorMatrix type="saturate" values="0"/>
        </filter>
        <rect width="100%" height="100%" filter="url(#atm-noise)"/>
      </svg>
      <!-- Layer 3: edge vignette -->
      <div class="absolute inset-0" style="background:radial-gradient(ellipse at 50% 45%,transparent 30%,rgba(0,0,0,0.48) 100%)"></div>
    </div>

    <div class="relative pt-16 pb-32">
      <div class="flex flex-col lg:flex-row lg:items-center gap-10 lg:gap-14">

        <!-- Left: copy -->
        <div class="flex-1 min-w-0">
          <p class="font-mono text-[10px] tracking-[0.3em] uppercase mb-5" style="color:rgba(52,211,153,0.6)">THE MONKEY LAB</p>
          <h1 class="text-4xl sm:text-5xl font-bold leading-[1.08] tracking-tight mb-5" style="color:#f0fdf4">
            Bienvenido al Lab.<br/>
            <span style="color:#34d399">Monkeys en formación.</span>
          </h1>
          <p class="text-base leading-relaxed max-w-lg mb-3" style="color:#94a3b8">
            MOONKEY LAB forma operadores que aprenden a construir con IA de forma sistemática. Los llamamos Monkeys: aprendices ágiles, curiosos y constructores de sistemas reales.
          </p>
          <p class="text-lg font-bold leading-snug mb-8" style="color:#e2e8f0">
            No completas módulos.<br/>
            <span style="color:#34d399">Evolucionas dentro de la jungla.</span>
          </p>
          <div class="flex flex-wrap gap-3">
            <a href="/monkeys" class="inline-flex items-center gap-2 font-mono text-sm font-semibold px-6 py-3 rounded-xl transition-colors" style="background:#34d399;color:#022c22">
              <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
              Entrar al Lab
            </a>
            <a href="/modulos" class="inline-flex items-center gap-2 font-mono text-sm px-6 py-3 rounded-xl transition-colors" style="color:#94a3b8;border:1px solid rgba(255,255,255,0.11)">
              Empezar Nivel 1 →
            </a>
          </div>
        </div>

        <!-- Right: jungle portal -->
        <div class="lg:w-[440px] shrink-0 flex justify-center pt-8 lg:pt-0">
          <div class="relative w-[300px] h-[300px] sm:w-[360px] sm:h-[360px]">

            <!-- Outer atmospheric halo -->
            <div class="absolute pointer-events-none select-none" style="inset:-54px;background:radial-gradient(ellipse at 50% 50%,rgba(16,185,129,0.09) 0%,transparent 65%);border-radius:50%"></div>

            <!-- Leaf depth layer (blurred, distant) -->
            <div class="absolute -left-12 top-[24%] pointer-events-none select-none" style="transform:rotate(-22deg);opacity:0.22;filter:blur(1px)">
              <svg width="44" height="76" viewBox="0 0 38 66" xmlns="http://www.w3.org/2000/svg">
                <path d="M19 64 Q3 48 8 26 Q12 8 19 2 Q26 8 30 26 Q35 48 19 64Z" fill="#064e3b"/>
                <line x1="19" y1="64" x2="19" y2="8" stroke="#34d399" stroke-width="1.2" opacity="0.3"/>
              </svg>
            </div>
            <div class="absolute -right-9 top-[20%] pointer-events-none select-none" style="transform:rotate(18deg);opacity:0.18;filter:blur(0.5px)">
              <svg width="36" height="62" viewBox="0 0 32 56" xmlns="http://www.w3.org/2000/svg">
                <path d="M16 54 Q2 40 6 22 Q10 6 16 2 Q22 6 26 22 Q30 40 16 54Z" fill="#064e3b"/>
              </svg>
            </div>
            <div class="absolute -left-7 bottom-[18%] pointer-events-none select-none" style="transform:rotate(10deg);opacity:0.15;filter:blur(0.5px)">
              <svg width="28" height="50" viewBox="0 0 26 46" xmlns="http://www.w3.org/2000/svg">
                <path d="M13 44 Q2 33 5 19 Q8 6 13 2 Q18 6 21 19 Q24 33 13 44Z" fill="#065f46"/>
              </svg>
            </div>

            <!-- Main orb — the portal -->
            <div class="absolute inset-0 rounded-full overflow-hidden" style="background:radial-gradient(ellipse at 30% 20%,#0a3d2e 0%,#0d1b2a 48%,#020c05 100%);box-shadow:0 0 0 1px rgba(52,211,153,0.07),0 0 90px rgba(16,185,129,0.28),0 0 180px rgba(16,185,129,0.11),0 28px 56px rgba(0,0,0,0.72)">
              <!-- Inner sky glow -->
              <div class="absolute inset-0" style="background:radial-gradient(ellipse at 36% 17%,rgba(52,211,153,0.32) 0%,transparent 44%)"></div>
              <!-- Deep shadow ground -->
              <div class="absolute inset-0" style="background:radial-gradient(ellipse at 55% 85%,rgba(1,18,8,0.75) 0%,transparent 50%)"></div>
              <!-- Orbital rings -->
              <div class="absolute inset-3 rounded-full" style="border:1px solid rgba(52,211,153,0.11)"></div>
              <div class="absolute inset-[36px] rounded-full" style="border:1px solid rgba(52,211,153,0.07)"></div>
              <div class="absolute inset-[65px] rounded-full" style="border:1px solid rgba(52,211,153,0.04)"></div>
              <!-- Moon crescent — MAIN FOCUS -->
              <div class="absolute inset-0 flex items-center" style="justify-content:center;padding-bottom:24%">
                <svg width="154" height="154" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
                  <defs>
                    <mask id="moon-atm">
                      <rect width="100" height="100" fill="white"/>
                      <circle cx="65" cy="23" r="40" fill="black"/>
                    </mask>
                    <radialGradient id="moon-fill" cx="28%" cy="28%" r="70%">
                      <stop offset="0%" stop-color="#86efac"/>
                      <stop offset="55%" stop-color="#34d399"/>
                      <stop offset="100%" stop-color="#047857"/>
                    </radialGradient>
                  </defs>
                  <circle cx="35" cy="46" r="42" fill="rgba(52,211,153,0.05)"/>
                  <circle cx="35" cy="46" r="35" fill="url(#moon-fill)" mask="url(#moon-atm)" opacity="0.96"/>
                  <circle cx="35" cy="46" r="35" fill="none" stroke="rgba(167,243,208,0.55)" stroke-width="1.5" mask="url(#moon-atm)"/>
                </svg>
              </div>
              <!-- Monkey silhouette — SECONDARY, darker and more receded -->
              <div class="absolute bottom-0 left-0 right-0 flex justify-center">
                <svg width="155" height="105" viewBox="0 0 160 115" xmlns="http://www.w3.org/2000/svg" style="opacity:0.78">
                  <ellipse cx="80" cy="106" rx="48" ry="11" fill="rgba(0,0,0,0.75)"/>
                  <ellipse cx="80" cy="78" rx="19" ry="22" fill="rgba(0,0,0,0.86)"/>
                  <path d="M63 73 Q50 86 54 100" stroke="rgba(0,0,0,0.86)" stroke-width="10" fill="none" stroke-linecap="round"/>
                  <path d="M97 73 Q110 86 106 100" stroke="rgba(0,0,0,0.86)" stroke-width="10" fill="none" stroke-linecap="round"/>
                  <circle cx="80" cy="50" r="17" fill="rgba(0,0,0,0.88)"/>
                  <circle cx="62" cy="47" r="7" fill="rgba(0,0,0,0.88)"/>
                  <circle cx="98" cy="47" r="7" fill="rgba(0,0,0,0.88)"/>
                </svg>
              </div>
              <!-- Particles — scattered light -->
              <div class="absolute top-[11%] left-[21%] w-0.5 h-0.5 rounded-full" style="background:rgba(52,211,153,0.95)"></div>
              <div class="absolute top-[22%] right-[15%] w-1 h-1 rounded-full" style="background:rgba(52,211,153,0.65)"></div>
              <div class="absolute top-[37%] right-[9%] w-0.5 h-0.5 rounded-full" style="background:rgba(134,239,172,0.6)"></div>
              <div class="absolute top-[54%] left-[8%] w-1 h-1 rounded-full" style="background:rgba(52,211,153,0.42)"></div>
              <div class="absolute top-[29%] left-[13%] w-0.5 h-0.5 rounded-full" style="background:rgba(167,243,208,0.5)"></div>
              <div class="absolute top-[46%] left-[32%] w-0.5 h-0.5 rounded-full" style="background:rgba(52,211,153,0.35)"></div>
            </div>

            <!-- Status — no fake metrics -->
            <div class="absolute -bottom-10 left-1/2" style="transform:translateX(-50%);min-width:178px">
              <div class="rounded-2xl px-4 py-3.5" style="background:rgba(248,250,252,0.88);backdrop-filter:blur(20px) saturate(180%);border:1px solid rgba(255,255,255,0.65);box-shadow:0 4px 32px rgba(0,0,0,0.14)">
                <p class="font-mono text-[9px] tracking-widest uppercase mb-1.5" style="color:#059669">JUNGLA ACTIVA</p>
                <p class="text-sm font-semibold mb-1.5" style="color:#0f172a;line-height:1.35">Operadores aprendiendo.<br/>Sistemas evolucionando.</p>
                <div class="flex items-center gap-1.5">
                  <span class="w-1.5 h-1.5 rounded-full animate-pulse shrink-0" style="background:#34d399"></span>
                  <p class="font-mono text-[9px]" style="color:#64748b">Lab en expansión.</p>
                </div>
              </div>
            </div>

          </div>
        </div>

      </div>

      <!-- Language bar -->
      <div class="mt-24 pt-5 relative" style="border-top:1px solid rgba(255,255,255,0.07)">
        <LanguageBar />
      </div>
    </div>

    <!-- Gradient bleed: hero dark → page light -->
    <div class="absolute bottom-0 left-0 right-0 pointer-events-none" style="height:5rem;background:linear-gradient(to bottom,transparent,#f8fafc)"></div>
  </section>

  <!-- El ecosistema -->
  <section class="py-16 border-b border-slate-200">
    <span class="label block mb-3">EL ECOSISTEMA</span>
    <p class="text-slate-500 text-sm leading-relaxed max-w-xl mb-10">
      Tres capas. Una entidad que diseña, un laboratorio que forma, una metodología que estructura el camino.
    </p>
    <div class="relative">
      {ecosistema.map((e, i) => (
        <div class={`relative pl-8 ${i < ecosistema.length - 1 ? 'pb-9 border-l-2 border-slate-200' : 'border-l-2 border-transparent'}`}>
          <span class={`absolute -left-[7px] top-1 w-3 h-3 rounded-full ${e.dot} ring-4 ring-[#f8fafc]`}></span>
          <p class={`font-mono text-[10px] tracking-widest uppercase mb-1.5 ${e.lbl}`}>{e.layer}</p>
          <h3 class="text-xl font-bold text-slate-900 mb-1.5">{e.name}</h3>
          <p class="text-slate-500 text-sm leading-relaxed max-w-md">{e.desc}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- No somos una escuela -->
  <section class="py-20 border-b border-slate-200">
    <span class="label block mb-8">NO SOMOS UNA ESCUELA</span>
    <div class="space-y-4 max-w-2xl">
      <p class="text-2xl sm:text-3xl font-bold text-slate-300 leading-snug tracking-tight">
        La mayoría de cursos enseñan herramientas.
      </p>
      <p class="text-2xl sm:text-3xl font-bold text-slate-900 leading-snug tracking-tight">
        MOONKEY LAB enseña a construir sistemas.
      </p>
    </div>
    <div class="mt-12 pt-8 border-t border-slate-200 max-w-2xl">
      <p class="text-base text-slate-500 leading-relaxed mb-1.5">El objetivo no es consumir contenido.</p>
      <p class="text-base text-slate-900 font-semibold leading-relaxed">El objetivo es desarrollar criterio operativo.</p>
    </div>
  </section>

  <!-- Para quién es -->
  <section class="py-16 border-b border-slate-200">
    <span class="label block mb-3">¿PARA QUIÉN ES?</span>
    <p class="text-slate-500 text-sm leading-relaxed max-w-xl mb-8">
      Para quien quiere dejar de usar IA por impulso y empezar a operarla con criterio.
    </p>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      {audiencia.map(a => (
        <div class="bg-white border border-slate-200 rounded-2xl p-6 hover:border-slate-300 transition-colors">
          <p class="font-mono text-[10px] tracking-widest text-slate-400 uppercase mb-3">{a.tag}</p>
          <h3 class="text-base font-bold text-slate-900 mb-1.5">{a.title}</h3>
          <p class="text-slate-500 text-sm leading-relaxed">{a.desc}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- Progreso global -->
  <section class="py-10 border-b border-slate-200">
    <div class="flex items-center justify-between mb-4">
      <span class="label">TU PROGRESO</span>
      <span class="font-mono text-xs text-slate-500"><span id="progress-count">0</span>/3 niveles completados</span>
    </div>
    <div class="flex gap-3 mb-1.5">
      <div class="flex-1 h-2 rounded-full bg-slate-100 overflow-hidden">
        <div id="dot-l1" class="h-full w-1/3 bg-slate-200 rounded-full transition-colors duration-500"></div>
      </div>
      <div class="flex-1 h-2 rounded-full bg-slate-100 overflow-hidden">
        <div id="dot-l2" class="h-full w-full bg-slate-200 rounded-full transition-colors duration-500"></div>
      </div>
      <div class="flex-1 h-2 rounded-full bg-slate-100 overflow-hidden">
        <div id="dot-l3" class="h-full w-full bg-slate-200 rounded-full transition-colors duration-500"></div>
      </div>
    </div>
    <div class="flex gap-3 mb-4">
      <span class="flex-1 font-mono text-[10px] text-slate-400 text-center">Key 1</span>
      <span class="flex-1 font-mono text-[10px] text-slate-400 text-center">Key 2</span>
      <span class="flex-1 font-mono text-[10px] text-slate-400 text-center">Key 3</span>
    </div>
    <a href="/cuenta" class="font-mono text-xs text-slate-400 hover:text-emerald-600 transition-colors">Sincronizar progreso en la nube →</a>
  </section>

  <!-- Operator Path dark block -->
  <section class="py-10 border-b border-slate-200">
    <div class="bg-slate-900 rounded-2xl overflow-hidden">
      <div class="px-7 sm:px-9 py-8">
        <span class="font-mono text-[10px] tracking-widest text-emerald-400 uppercase block mb-3">OPERATOR PATH — METODOLOGÍA MOONKEY XN</span>
        <h2 class="text-xl font-bold text-white mb-2 leading-snug">De aprendiz a operador.</h2>
        <p class="text-slate-400 text-sm leading-relaxed mb-7 max-w-lg">Tres Keys. Cada una desbloquea la siguiente. Un sistema que no desaparece cuando cierras el chat.</p>
        <div class="grid sm:grid-cols-3 gap-px bg-white/[0.06] rounded-xl overflow-hidden mb-5">
          <div class="bg-slate-800/70 px-5 py-5">
            <span class="font-mono text-[10px] text-emerald-400 tracking-widest uppercase block mb-2">KEY 1</span>
            <p class="text-white text-sm font-semibold mb-1.5">Operator Foundations</p>
            <p class="text-slate-400 text-xs leading-relaxed">Terminal, GitHub, CLAUDE.md, prompts y primera automatización.</p>
          </div>
          <div class="bg-slate-800/70 px-5 py-5 border-t sm:border-t-0 sm:border-l border-white/[0.06]">
            <span class="font-mono text-[10px] text-violet-400 tracking-widest uppercase block mb-2">KEY 2</span>
            <p class="text-white text-sm font-semibold mb-1.5">Advanced Operator</p>
            <p class="text-slate-400 text-xs leading-relaxed">Arquitectura, debugging, refactors, MCPs y documentación operativa.</p>
          </div>
          <div class="bg-slate-800/70 px-5 py-5 border-t sm:border-t-0 sm:border-l border-white/[0.06]">
            <span class="font-mono text-[10px] text-amber-400 tracking-widest uppercase block mb-2">KEY 3</span>
            <p class="text-white text-sm font-semibold mb-1.5">Migration Master</p>
            <p class="text-slate-400 text-xs leading-relaxed">Contexto migrado, sistema de prompts propio y workflows documentados.</p>
          </div>
        </div>
        <p class="text-slate-500 text-xs font-mono">Moonkey — operador que ha completado las 3 Keys. Moon + Monkey + Key.</p>
      </div>
    </div>
  </section>

  <!-- De prompt a operador -->
  <section class="py-10 border-b border-slate-200">
    <span class="label block mb-5">DE PROMPT A OPERADOR</span>
    <div class="grid sm:grid-cols-3 gap-4">
      <div class="bg-white border-2 border-slate-200 rounded-xl p-5 shadow-sm">
        <p class="font-mono text-[10px] tracking-widest uppercase mb-2 text-slate-400">01</p>
        <p class="font-mono text-sm font-bold text-slate-700 mb-2">Monkey</p>
        <p class="text-slate-500 text-xs leading-relaxed">Entras en formación. Construyes los cimientos: entorno, CLAUDE.md, prompts, GitHub.</p>
      </div>
      <div class="bg-violet-50 border-2 border-violet-200 rounded-xl p-5 shadow-sm">
        <p class="font-mono text-[10px] tracking-widest uppercase mb-2 text-violet-400">02</p>
        <p class="font-mono text-sm font-bold text-violet-700 mb-2">Gorilla</p>
        <p class="text-slate-500 text-xs leading-relaxed">Ejecutas con fuerza. Arquitectura, debugging, refactors. Sistemas que funcionan sin supervisión.</p>
      </div>
      <div class="bg-emerald-50 border-2 border-emerald-200 rounded-xl p-5 shadow-sm">
        <p class="font-mono text-[10px] tracking-widest uppercase mb-2 text-emerald-500">03</p>
        <p class="font-mono text-sm font-bold text-emerald-700 mb-2">Eagle</p>
        <p class="text-slate-500 text-xs leading-relaxed">Vuelas solo. Contexto migrado, prompts propios, workflows documentados. Autonomía real.</p>
      </div>
    </div>
  </section>

  <!-- La jungla — visión / filosofía -->
  <section class="py-16 border-b border-slate-200">
    <div class="bg-slate-900 rounded-2xl px-8 sm:px-10 py-12">
      <span class="font-mono text-[10px] tracking-widest text-emerald-400 uppercase block mb-6">LA JUNGLA</span>
      <p class="text-2xl sm:text-3xl font-bold text-white leading-snug tracking-tight">
        No completas módulos.
      </p>
      <p class="text-2xl sm:text-3xl font-bold text-emerald-400 leading-snug tracking-tight mb-7">
        Evolucionas dentro de la jungla.
      </p>
      <p class="text-slate-400 text-sm leading-relaxed max-w-lg mb-9">
        MOONKEY LAB no es una colección de lecciones. Es un sistema vivo: cada operador que avanza deja el camino más claro para el siguiente.
      </p>
      <div class="flex flex-wrap items-center gap-x-3 gap-y-2 font-mono text-xs">
        <span class="text-white">Aprendes</span>
        <span class="text-emerald-400">→</span>
        <span class="text-white">construyes</span>
        <span class="text-emerald-400">→</span>
        <span class="text-white">compartes</span>
        <span class="text-emerald-400">→</span>
        <span class="text-white">otros aprenden</span>
        <span class="text-emerald-400">→</span>
        <span class="text-emerald-400 font-semibold">la jungla crece</span>
      </div>
      <p class="font-mono text-[10px] text-slate-500 leading-relaxed mt-5 max-w-lg">
        La jungla necesita Monkeys que aprenden, Gorillas que ejecutan, Eagles que vuelan, Lions que lideran y Elephants que recuerdan la arquitectura.
      </p>
    </div>
  </section>

  <!-- Escuela completada (hidden by default) -->
  <div id="school-complete-banner" class="hidden mt-8 bg-emerald-50 border-2 border-emerald-300 rounded-2xl px-8 py-7 text-center">
    <span class="label block mb-2">ESCUELA COMPLETADA</span>
    <h2 class="text-xl font-bold text-slate-900 mb-2">MOONKEY LAB completado</h2>
    <p class="text-slate-600 text-sm max-w-md mx-auto">Has completado los tres niveles. Ya operas sistemas IA con criterio y autonomía. Lo que viene ahora lo decides tú.</p>
  </div>

  <!-- EL CAMINO -->
  <section class="py-4">
    <div class="rounded-3xl overflow-hidden" style="background:linear-gradient(140deg,#071a0e 0%,#0f172a 45%,#0a1a0f 100%)">
      <div class="px-7 sm:px-10 py-10">
        <div class="flex flex-col lg:flex-row lg:items-start gap-8 lg:gap-12">

          <!-- Left: heading -->
          <div class="lg:w-64 shrink-0">
            <span class="font-mono text-[10px] tracking-widest text-emerald-400 uppercase block mb-3">EL CAMINO</span>
            <h2 class="text-2xl sm:text-3xl font-bold text-white leading-snug mb-4">Tres niveles para convertirte en un operador completo.</h2>
            <p class="text-slate-400 text-sm leading-relaxed mb-6">Cada nivel deja una huella. Cada huella te hace libre.</p>
            <a href="/info" class="inline-flex items-center gap-2 font-mono text-xs text-emerald-400 border border-emerald-800/60 px-4 py-2 rounded-xl hover:bg-emerald-900/30 transition-colors">
              <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
              Cómo funciona
            </a>
          </div>

          <!-- Right: 3 level cards -->
          <div class="flex-1 space-y-3">

            <!-- Nivel 1 -->
            <div class="relative rounded-2xl overflow-hidden p-5" style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.08)">
              <div class="flex items-center justify-between mb-3">
                <div class="flex items-center gap-3">
                  <div class="w-9 h-9 rounded-full flex items-center justify-center shrink-0" style="background:rgba(16,185,129,0.15);border:1px solid rgba(16,185,129,0.3)">
                    <svg width="15" height="15" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                      <defs><mask id="mk-c1"><rect width="20" height="20" fill="white"/><circle cx="13" cy="5.5" r="5.5" fill="black"/></mask></defs>
                      <circle cx="8.5" cy="7.5" r="6.5" fill="#34d399" mask="url(#mk-c1)"/>
                    </svg>
                  </div>
                  <div>
                    <span class="font-mono text-[9px] tracking-widest text-emerald-400 uppercase block leading-none mb-0.5">NIVEL 1</span>
                    <span class="font-mono text-xs text-white font-semibold">Fundamentos</span>
                  </div>
                </div>
                <div class="flex items-center gap-2">
                  <span id="l1-progress" class="font-mono text-[10px] text-slate-500">0/8</span>
                  <span id="l1-completed-badge" class="hidden font-mono text-[10px] px-2 py-0.5 rounded-lg" style="background:rgba(16,185,129,0.18);color:#6ee7b7;border:1px solid rgba(52,211,153,0.25)">✓ Completado</span>
                  <span id="l1-status" class="font-mono text-[10px] text-emerald-400 font-semibold">Disponible</span>
                </div>
              </div>
              <p class="text-slate-400 text-xs leading-relaxed mb-3">La base para operar Claude Code, GitHub, automatizaciones y sistemas IA. 8 módulos prácticos con ejercicios reales.</p>
              <ul class="space-y-1.5 mb-4">
                {level1Outcomes.map(o => (
                  <li class="flex items-center gap-2 text-xs text-slate-500">
                    <span class="w-1 h-1 rounded-full bg-emerald-500/70 shrink-0"></span>
                    {o}
                  </li>
                ))}
              </ul>
              <a href="/modulos" class="block w-full text-center font-mono text-xs font-semibold px-4 py-2.5 rounded-xl transition-colors" style="background:#34d399;color:#022c22">
                Empezar Nivel 1 →
              </a>
            </div>

            <!-- Nivel 2 -->
            <div class="relative rounded-2xl overflow-hidden p-5" style="background:rgba(255,255,255,0.03);border:1px solid rgba(255,255,255,0.06)">
              <div id="l2-lock" class="absolute inset-0 rounded-2xl flex flex-col items-center justify-center z-10 gap-2.5" style="background:rgba(7,12,22,0.84);backdrop-filter:blur(4px)">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#64748b" stroke-width="2" stroke-linecap="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                <span class="font-mono text-[10px] text-slate-500 tracking-widest uppercase">Bloqueado</span>
                <p class="text-slate-500 text-[10px] text-center leading-relaxed" style="max-width:160px">Completa Nivel 1 para desbloquear</p>
                <a href="/modulos" class="font-mono text-[10px] text-violet-400 border border-violet-800/60 px-3 py-1 rounded-lg hover:bg-violet-900/30 transition-colors">Ir a Nivel 1 →</a>
              </div>
              <div class="flex items-center justify-between mb-3">
                <div class="flex items-center gap-3">
                  <div class="w-9 h-9 rounded-full flex items-center justify-center shrink-0" style="background:rgba(139,92,246,0.15);border:1px solid rgba(139,92,246,0.3)">
                    <svg width="15" height="15" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                      <defs><mask id="mk-c2"><rect width="20" height="20" fill="white"/><circle cx="13" cy="5.5" r="5.5" fill="black"/></mask></defs>
                      <circle cx="8.5" cy="7.5" r="6.5" fill="#a78bfa" mask="url(#mk-c2)"/>
                    </svg>
                  </div>
                  <div>
                    <span class="font-mono text-[9px] tracking-widest text-violet-400 uppercase block leading-none mb-0.5">NIVEL 2</span>
                    <span class="font-mono text-xs text-white font-semibold">Construcción</span>
                  </div>
                </div>
                <div class="flex items-center gap-2">
                  <span id="l2-progress" class="font-mono text-[10px] text-slate-500">0/10</span>
                  <span id="l2-completed-badge" class="hidden font-mono text-[10px] px-2 py-0.5 rounded-lg" style="background:rgba(139,92,246,0.18);color:#c4b5fd;border:1px solid rgba(139,92,246,0.25)">✓ Completado</span>
                  <span id="l2-status" class="font-mono text-[10px] text-slate-500">Bloqueado</span>
                </div>
              </div>
              <p class="text-slate-500 text-xs leading-relaxed mb-3">Claude Code como sistema profesional. Arquitectura, debugging, refactors, MCPs y documentación operativa avanzada.</p>
              <ul class="space-y-1.5 mb-4">
                {level2Outcomes.map(o => (
                  <li class="flex items-center gap-2 text-xs text-slate-600">
                    <span class="w-1 h-1 rounded-full bg-violet-500/60 shrink-0"></span>
                    {o}
                  </li>
                ))}
              </ul>
              <div id="l2-cta-locked" class="block w-full text-center font-mono text-xs font-medium text-slate-600 px-4 py-2.5 rounded-xl cursor-not-allowed" style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.07)">
                Bloqueado — completa Nivel 1
              </div>
              <a id="l2-cta-unlocked" href="/advanced" class="hidden block w-full text-center font-mono text-xs font-semibold px-4 py-2.5 rounded-xl transition-colors" style="background:#a78bfa;color:#1e1b4b">
                Empezar Nivel 2 →
              </a>
            </div>

            <!-- Nivel 3 -->
            <div class="relative rounded-2xl overflow-hidden p-5" style="background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.05)">
              <div id="l3-lock" class="absolute inset-0 rounded-2xl flex flex-col items-center justify-center z-10 gap-2.5" style="background:rgba(7,12,22,0.84);backdrop-filter:blur(4px)">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#64748b" stroke-width="2" stroke-linecap="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                <span class="font-mono text-[10px] text-slate-500 tracking-widest uppercase">Bloqueado</span>
                <p class="text-slate-500 text-[10px] text-center leading-relaxed" style="max-width:160px">Completa Nivel 2 para desbloquear</p>
                <a href="/advanced" class="font-mono text-[10px] text-amber-400 border border-amber-800/60 px-3 py-1 rounded-lg hover:bg-amber-900/30 transition-colors">Ir a Nivel 2 →</a>
              </div>
              <div class="flex items-center justify-between mb-3">
                <div class="flex items-center gap-3">
                  <div class="w-9 h-9 rounded-full flex items-center justify-center shrink-0" style="background:rgba(245,158,11,0.15);border:1px solid rgba(245,158,11,0.3)">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fbbf24" stroke-width="2" stroke-linecap="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                  </div>
                  <div>
                    <span class="font-mono text-[9px] tracking-widest text-amber-400 uppercase block leading-none mb-0.5">NIVEL 3</span>
                    <span class="font-mono text-xs text-white font-semibold">Maestría</span>
                  </div>
                </div>
                <div class="flex items-center gap-2">
                  <span id="l3-progress" class="font-mono text-[10px] text-slate-500">0/8</span>
                  <span id="l3-completed-badge" class="hidden font-mono text-[10px] px-2 py-0.5 rounded-lg" style="background:rgba(245,158,11,0.18);color:#fcd34d;border:1px solid rgba(245,158,11,0.25)">✓ Completado</span>
                  <span id="l3-status" class="font-mono text-[10px] text-slate-500">Bloqueado</span>
                </div>
              </div>
              <p class="text-slate-500 text-xs leading-relaxed mb-3">Convierte conversaciones, prompts y workflows dispersos en un sistema operativo propio para Claude Code.</p>
              <ul class="space-y-1.5 mb-4">
                {level3Outcomes.map(o => (
                  <li class="flex items-center gap-2 text-xs text-slate-600">
                    <span class="w-1 h-1 rounded-full bg-amber-500/60 shrink-0"></span>
                    {o}
                  </li>
                ))}
              </ul>
              <div id="l3-cta-locked" class="block w-full text-center font-mono text-xs font-medium text-slate-600 px-4 py-2.5 rounded-xl cursor-not-allowed" style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.07)">
                Bloqueado — completa Nivel 2
              </div>
              <a id="l3-cta-unlocked" href="/migrador" class="hidden block w-full text-center font-mono text-xs font-semibold px-4 py-2.5 rounded-xl transition-colors" style="background:#fbbf24;color:#451a03">
                Empezar Nivel 3 →
              </a>
            </div>

          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Principios -->
  <section class="py-8">
    <div class="rounded-2xl px-7 sm:px-10 py-7" style="background:#0f172a">
      <div class="grid grid-cols-2 sm:grid-cols-4 gap-5">
        <div class="flex items-start gap-3">
          <div class="w-8 h-8 rounded-lg flex items-center justify-center shrink-0 mt-0.5" style="background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.08)">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#34d399" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          </div>
          <div>
            <p class="font-mono text-xs text-white font-semibold leading-snug mb-0.5">Sin academias vacías</p>
            <p class="font-mono text-[10px] text-slate-500 leading-snug">Aprendizaje real.</p>
          </div>
        </div>
        <div class="flex items-start gap-3">
          <div class="w-8 h-8 rounded-lg flex items-center justify-center shrink-0 mt-0.5" style="background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.08)">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#34d399" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 11 12 14 22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
          </div>
          <div>
            <p class="font-mono text-xs text-white font-semibold leading-snug mb-0.5">Sin humo</p>
            <p class="font-mono text-[10px] text-slate-500 leading-snug">Resultados verificables.</p>
          </div>
        </div>
        <div class="flex items-start gap-3">
          <div class="w-8 h-8 rounded-lg flex items-center justify-center shrink-0 mt-0.5" style="background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.08)">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#34d399" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/><polyline points="17 6 23 6 23 12"/></svg>
          </div>
          <div>
            <p class="font-mono text-xs text-white font-semibold leading-snug mb-0.5">Sin límites</p>
            <p class="font-mono text-[10px] text-slate-500 leading-snug">Aprende a tu ritmo.</p>
          </div>
        </div>
        <div class="flex items-start gap-3">
          <div class="w-8 h-8 rounded-lg flex items-center justify-center shrink-0 mt-0.5" style="background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.08)">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#34d399" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
          </div>
          <div>
            <p class="font-mono text-xs text-white font-semibold leading-snug mb-0.5">Con jungla</p>
            <p class="font-mono text-[10px] text-slate-500 leading-snug">Nunca construyes solo.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Elige tu camino -->
  <section class="py-16 border-t border-slate-200">
    <span class="label block mb-2">ELIGE TU CAMINO</span>
    <p class="text-slate-500 text-sm leading-relaxed max-w-xl mb-8">
      MOONKEY LAB es el punto de entrada al Operator Path. Tres rutas según dónde estés.
    </p>
    <div class="grid sm:grid-cols-3 gap-5">
      {[
        {
          tag: 'Aprender',
          who: 'Quiero aprender como Monkey',
          desc: 'Empieza el Operator Path. Sin registro, sin código previo. Nivel 1 disponible ahora.',
          cta: 'Empezar Nivel 1 →',
          href: '/modulos',
          bg: 'bg-emerald-50',
          border: 'border-emerald-200',
          text: 'text-emerald-700',
          lbl: 'text-emerald-500',
          btn: 'bg-emerald-500 text-white hover:bg-emerald-600',
        },
        {
          tag: 'Evaluar',
          who: 'Probar si estoy listo',
          desc: '60 minutos. Una tarea real. Un entregable concreto. Para detectar si tienes criterio de operador.',
          cta: 'Hacer Test de Operador →',
          href: '/prueba-operador',
          bg: 'bg-sky-50',
          border: 'border-sky-200',
          text: 'text-sky-700',
          lbl: 'text-sky-500',
          btn: 'bg-sky-500 text-white hover:bg-sky-600',
        },
        {
          tag: 'Implementar',
          who: 'Implementar en mi equipo',
          desc: 'Escuela interna, sistema de prompts y SOPs. Implementación del Operator Path a medida.',
          cta: 'Ver planes de equipo →',
          href: '/planes',
          bg: 'bg-amber-50',
          border: 'border-amber-200',
          text: 'text-amber-700',
          lbl: 'text-amber-500',
          btn: 'bg-amber-500 text-white hover:bg-amber-600',
        },
      ].map(c => (
        <div class={`${c.bg} border ${c.border} rounded-2xl p-6 flex flex-col gap-3`}>
          <span class={`font-mono text-[10px] tracking-widest font-bold uppercase ${c.lbl}`}>{c.tag}</span>
          <h3 class={`font-semibold text-sm ${c.text}`}>{c.who}</h3>
          <p class="text-slate-500 text-xs leading-relaxed flex-1">{c.desc}</p>
          <a href={c.href} class={`inline-block font-mono text-xs font-medium px-4 py-2.5 rounded-xl transition-colors ${c.btn}`}>{c.cta}</a>
        </div>
      ))}
    </div>
  </section>

  <!-- Sección comercial -->
  <section id="servicios" class="py-16 border-t border-slate-200">
    <div class="bg-slate-900 rounded-2xl px-8 sm:px-10 py-10">
      <div class="flex flex-wrap items-start justify-between gap-6 mb-8">
        <div>
          <span class="font-mono text-[10px] tracking-widest uppercase text-emerald-400 block mb-3">SERVICIOS — XN TEAM SYSTEM</span>
          <h2 class="text-2xl sm:text-3xl font-bold text-white mb-3 leading-snug">
            También podemos construir<br/>esto para tu equipo.
          </h2>
          <p class="text-slate-400 text-sm leading-relaxed max-w-lg">
            Lo que enseña esta escuela también lo construimos como sistema interno. Desde un portal de formación hasta un sistema completo de prompts, SOPs y automatizaciones operativas.
          </p>
        </div>
      </div>
      <div class="grid sm:grid-cols-2 gap-2.5 mb-8">
        {servicios.map(s => (
          <div class="flex items-center gap-3 bg-white/[0.04] border border-white/[0.07] rounded-xl px-4 py-3">
            <span class={`font-mono text-xs font-bold shrink-0 ${s.color}`}>—</span>
            <span class="text-slate-300 text-sm">{s.label}</span>
          </div>
        ))}
      </div>
      <div class="flex flex-wrap gap-3 items-center">
        <a href="/onboarding" class="inline-block font-mono text-sm font-medium text-slate-900 bg-emerald-400 px-6 py-3 rounded-xl hover:bg-emerald-300 transition-colors">
          Ver proceso de colaboración →
        </a>
        <a href="/planes" class="inline-block font-mono text-sm text-slate-400 border border-slate-600 px-5 py-3 rounded-xl hover:border-slate-400 transition-colors">
          Ver planes de acceso
        </a>
      </div>
    </div>
  </section>

  <!-- Reiniciar progreso -->
  <section class="py-8 border-t border-slate-200 flex items-center justify-between">
    <span class="text-slate-300 text-xs font-mono">MOONKEY LAB · MMXXVI</span>
    <button
      id="reset-progress-btn"
      class="font-mono text-xs text-slate-300 hover:text-rose-400 transition-colors cursor-pointer"
    >
      Reiniciar progreso
    </button>
  </section>

</Layout>

<script is:inline>
  (function () {
    var l1 = localStorage.getItem('moonkey_level_1_completed') === 'true';
    var l2 = localStorage.getItem('moonkey_level_2_completed') === 'true';
    var l3 = localStorage.getItem('moonkey_level_3_completed') === 'true';
    var count = (l1 ? 1 : 0) + (l2 ? 1 : 0) + (l3 ? 1 : 0);

    var l1c = 0; for (var j = 0; j <= 7; j++) { if (localStorage.getItem('moonkey_level_1_module_' + j) === 'true') l1c++; }
    var l2c = 0; for (var j = 1; j <= 10; j++) { if (localStorage.getItem('moonkey_level_2_module_' + j) === 'true') l2c++; }
    var l3c = 0; for (var j = 1; j <= 8; j++) { if (localStorage.getItem('moonkey_level_3_section_' + j) === 'true') l3c++; }
    var el1p = document.getElementById('l1-progress'); if (el1p) el1p.textContent = l1c + '/8';
    var el2p = document.getElementById('l2-progress'); if (el2p) el2p.textContent = l2c + '/10';
    var el3p = document.getElementById('l3-progress'); if (el3p) el3p.textContent = l3c + '/8';

    var progEl = document.getElementById('progress-count');
    if (progEl) progEl.textContent = count;

    if (l1) {
      var d1 = document.getElementById('dot-l1');
      if (d1) { d1.classList.remove('bg-slate-200'); d1.classList.add('bg-emerald-500'); }
    }
    if (l2) {
      var d2 = document.getElementById('dot-l2');
      if (d2) { d2.classList.remove('bg-slate-200'); d2.classList.add('bg-violet-500'); }
    }
    if (l3) {
      var d3 = document.getElementById('dot-l3');
      if (d3) { d3.classList.remove('bg-slate-200'); d3.classList.add('bg-amber-500'); }
    }

    function hideLock(id) {
      var el = document.getElementById(id);
      if (el) el.style.display = 'none';
    }
    function showBadge(id, statusId, text, cls) {
      var badge = document.getElementById(id);
      if (badge) badge.classList.remove('hidden');
      var status = document.getElementById(statusId);
      if (status) { status.textContent = text; if (cls) status.className = cls; }
    }

    // L1
    if (l1) {
      showBadge('l1-completed-badge', 'l1-status', 'Completado', 'font-mono text-xs text-emerald-600 font-semibold');
    }

    // L2
    if (l1 && !l2) {
      hideLock('l2-lock');
      var s2 = document.getElementById('l2-status');
      if (s2) { s2.textContent = 'Disponible'; s2.className = 'font-mono text-xs text-violet-600 font-semibold'; }
      var l2locked = document.getElementById('l2-cta-locked');
      var l2unlocked = document.getElementById('l2-cta-unlocked');
      if (l2locked) l2locked.classList.add('hidden');
      if (l2unlocked) l2unlocked.classList.remove('hidden');
    }
    if (l2) {
      hideLock('l2-lock');
      showBadge('l2-completed-badge', 'l2-status', 'Completado', 'font-mono text-xs text-violet-600 font-semibold');
      var l2locked2 = document.getElementById('l2-cta-locked');
      var l2unlocked2 = document.getElementById('l2-cta-unlocked');
      if (l2locked2) l2locked2.classList.add('hidden');
      if (l2unlocked2) l2unlocked2.classList.remove('hidden');
    }

    // L3
    if (l2 && !l3) {
      hideLock('l3-lock');
      var s3 = document.getElementById('l3-status');
      if (s3) { s3.textContent = 'Disponible'; s3.className = 'font-mono text-xs text-amber-600 font-semibold'; }
      var l3locked = document.getElementById('l3-cta-locked');
      var l3unlocked = document.getElementById('l3-cta-unlocked');
      if (l3locked) l3locked.classList.add('hidden');
      if (l3unlocked) l3unlocked.classList.remove('hidden');
    }
    if (l3) {
      hideLock('l3-lock');
      showBadge('l3-completed-badge', 'l3-status', 'Completado', 'font-mono text-xs text-amber-600 font-semibold');
      var l3locked2 = document.getElementById('l3-cta-locked');
      var l3unlocked2 = document.getElementById('l3-cta-unlocked');
      if (l3locked2) l3locked2.classList.add('hidden');
      if (l3unlocked2) l3unlocked2.classList.remove('hidden');
    }

    // School complete
    if (l1 && l2 && l3) {
      var banner = document.getElementById('school-complete-banner');
      if (banner) banner.classList.remove('hidden');
    }

    // Reset
    var resetBtn = document.getElementById('reset-progress-btn');
    if (resetBtn) {
      resetBtn.addEventListener('click', function () {
        if (confirm('¿Reiniciar todo el progreso de MOONKEY LAB?')) {
          var keys = [
            'moonkey_level_1_completed', 'moonkey_level_2_completed', 'moonkey_level_3_completed'
          ];
          for (var i = 0; i <= 7; i++) keys.push('moonkey_level_1_module_' + i);
          for (var i = 1; i <= 10; i++) keys.push('moonkey_level_2_module_' + i);
          for (var i = 1; i <= 8; i++) keys.push('moonkey_level_3_section_' + i);
          keys.forEach(function(k) { localStorage.removeItem(k); });
          window.location.reload();
        }
      });
    }
  })();
</script>
