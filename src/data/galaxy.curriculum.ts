// ════════════════════════════════════════════════════════════════════════
// Master curriculum — deep lessons authored by the "cabinet of sages" (one
// domain expert per constellation). Keyed by constellation id → module code.
// `galaxy.ts` holds the structure (title/blurb); this holds the actual teaching.
// A module with no lesson here simply renders its blurb (graceful fallback).
//
// GENERATED by scripts/gen-curriculum.mjs from the cabinet-of-sages workflow.
// ════════════════════════════════════════════════════════════════════════

export interface Lesson {
  objetivo: string;
  porque: string;
  leccion: string[];
  ejercicio: string;
  entregable: string;
  insight: string;
  errores: string[];
}

export type Curriculum = Record<string, Record<string, Lesson>>;

export const curriculum: Curriculum = {
  "operator-core": {
    "OC-01": {
      "objetivo": "Dejar tu terminal, VS Code y Claude Code instalados, autenticados y verificados para empezar a construir un proyecto real en menos de una hora.",
      "porque": "Sin un entorno operativo sólido pierdes horas peleando con errores de PATH, permisos y autenticación en vez de construir. El 80% del abandono de principiantes ocurre aquí, antes de escribir una sola línea útil.",
      "leccion": [
        "Un operador no 'usa' la IA desde una web: la opera desde su máquina, con acceso a sus archivos, su git y su terminal. Eso es Claude Code. Antes de instalarlo necesitas tres cimientos: una terminal donde corres comandos, un editor (VS Code) donde ves y editas el código, y un gestor de paquetes para instalar herramientas. En macOS la terminal es Terminal.app o iTerm2; en Windows usa WSL2 (Ubuntu) porque Claude Code y la mayoría de herramientas asumen un entorno tipo Unix. No intentes operar desde PowerShell puro: te va a costar el doble.",
        "Primero comprueba qué tienes. Abre Terminal y ejecuta `node --version` y `git --version`. Si `node` no existe o es menor que v18, instala Node LTS. En macOS lo limpio es Homebrew: `/bin/bash -c \"$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\"` y luego `brew install node git`. Tras instalar Homebrew, el instalador te imprime DOS líneas `eval \"$(/opt/homebrew/bin/brew shellenv)\"` que debes añadir a tu `~/.zprofile` — si las saltas, `brew` 'desaparece' al abrir una terminal nueva. Este es el primer fallo de PATH que vas a encontrar y conviene entenderlo: el PATH es la lista de carpetas donde el shell busca comandos; si la herramienta no está en una de ellas, el shell dice `command not found` aunque el archivo exista en el disco.",
        "Instala Claude Code con `npm install -g @anthropic-ai/claude-code` y verifica con `claude --version`. Si npm se queja de permisos (`EACCES`) NO uses `sudo npm` — eso te deja archivos root que luego no puedes borrar. La solución correcta es apuntar npm a un prefijo en tu home: `npm config set prefix ~/.npm-global` y añadir `export PATH=~/.npm-global/bin:$PATH` a tu `~/.zshrc`. Recarga con `source ~/.zshrc`. Ahora `claude` arranca y te pide autenticarte con tu cuenta Anthropic vía navegador; ese token queda guardado y no lo vuelves a meter.",
        "VS Code es tu ventana al código mientras Claude Code es tus manos. Instálalo desde code.visualstudio.com y, dentro, abre la paleta de comandos (Cmd+Shift+P) → 'Shell Command: Install code command in PATH'. Eso te da el comando `code .` para abrir la carpeta actual en VS Code desde la terminal — el gesto que harás cien veces al día. Instala al menos estas extensiones: la oficial de tu lenguaje, GitLens, y un formateador como Prettier. No infles el editor con 40 extensiones; cada una es superficie de ataque y ruido.",
        "El flujo de trabajo real es siempre el mismo y lo vas a interiorizar este módulo. Abres una terminal, navegas a tu proyecto con `cd ~/mi-proyecto`, escribes `claude` para lanzar la sesión, y en otra pestaña (o panel dividido de VS Code) tienes el editor abierto con `code .`. Claude Code propone cambios, tú los revisas en el editor o con `git diff`, y los aceptas. La terminal y el editor no compiten: la terminal es donde ocurre la acción, el editor es donde verificas. En MOONKEY LAB, por ejemplo, una sesión típica es `cd ~/xop && claude` y pedirle que toque un componente Astro mientras tú miras el archivo en VS Code.",
        "Verifica el entorno completo con una prueba de fuego antes de dar el módulo por cerrado. Crea una carpeta `mkdir ~/lab-01 && cd ~/lab-01`, inicializa git con `git init`, lanza `claude`, y pídele que cree un `hola.txt` con tu nombre. Sal de Claude, ejecuta `cat hola.txt` para confirmar que el archivo existe en disco, y `code .` para verlo en el editor. Si los cuatro pasos funcionan (terminal navega, claude arranca y escribe, el archivo aparece, VS Code lo abre) tu entorno está operativo. Documenta en una nota personal las versiones exactas que instalaste: cuando algo se rompa dentro de un mes, esa nota te ahorra una tarde."
      ],
      "ejercicio": "Desde cero, deja tu máquina operativa y demuéstralo: instala Node LTS, git, VS Code y Claude Code. Crea `~/lab-01`, hazlo repo git con `git init`, lanza `claude` dentro, y pídele que genere un archivo `entorno.md` que liste las salidas de `node --version`, `git --version` y `claude --version`. Abre el resultado con `code entorno.md` y confirma que las tres versiones son las que instalaste.",
      "entregable": "Una carpeta `~/lab-01` con un repo git inicializado y un archivo `entorno.md` que contiene las tres versiones verificadas (Node ≥18, git, Claude Code), generado por Claude Code y abierto en VS Code.",
      "insight": "La terminal no compite con VS Code: la terminal es donde ocurre la acción y el editor es donde la verificas. Un operador siempre tiene ambos abiertos y nunca confía en lo que la IA dice que hizo sin mirar el archivo en disco.",
      "errores": [
        "Usar `sudo npm install -g` para saltarte un error de permisos: te deja archivos propiedad de root que luego no puedes actualizar ni borrar sin más sudo. Arregla el prefijo de npm en tu home en su lugar.",
        "Operar desde PowerShell puro en Windows en vez de WSL2: la mitad de los comandos de los tutoriales asumen Unix y vas a traducir cada uno a mano.",
        "Olvidar añadir la línea `brew shellenv` (o el `export PATH` de npm) a tu archivo de perfil: la herramienta 'funciona' en esa terminal pero 'desaparece' al abrir una nueva.",
        "Creer que el archivo se creó porque Claude dijo que lo creó: siempre confirma con `cat` o abriendo el archivo en VS Code antes de seguir.",
        "Inflar VS Code con decenas de extensiones el primer día: añaden ruido, lentitud y superficie de ataque. Empieza con el formateador y la extensión de tu lenguaje, nada más."
      ]
    },
    "OC-02": {
      "objetivo": "Escribir un archivo CLAUDE.md que dé a Claude Code memoria persistente de tu proyecto, de modo que cada sesión arranque sabiendo tu stack, tus convenciones y tus prohibiciones sin que se lo repitas.",
      "porque": "Sin CLAUDE.md, cada sesión empieza de cero y la IA inventa convenciones, añade dependencias que no quieres y toca cosas que no debe. Con él, trabaja preciso en vez de genérico — es la diferencia entre un becario que olvida todo cada mañana y uno que ya conoce la casa.",
      "leccion": [
        "CLAUDE.md es un archivo de texto en la raíz de tu proyecto que Claude Code lee automáticamente al arrancar cada sesión. No es documentación para humanos (aunque también sirve): es el contexto que la IA carga antes de hacer nada. Si tu `package.json` dice React pero tú usas Astro, o si tienes una regla de 'nunca añadas dependencias sin justificarlas', ese archivo es donde vive. La IA no adivina tu criterio; tú se lo escribes una vez y lo reutiliza para siempre.",
        "El error del principiante es escribir un CLAUDE.md genérico ('este es un proyecto web, usa buenas prácticas'). Eso no aporta nada porque la IA ya asume buenas prácticas. El valor está en lo ESPECÍFICO y lo NO-OBVIO: qué stack REAL usas (no el que parece), qué tablas de tu base de datos son de otro proyecto y no debe tocar, qué patrón de i18n sigues, qué se traduce y qué no. Mira el CLAUDE.md real de MOONKEY LAB: tiene una sección '⚠️ La DB Supabase es COMPARTIDA con XHUB IRON' que lista las tablas prohibidas (`iron_*`, `world_*`) y las permitidas (`profiles, progress, feedback, leads, proofs`). Esa única línea evita que la IA borre datos de otro producto. Ese es el tipo de conocimiento que solo tú tienes y que el archivo captura.",
        "Estructura un CLAUDE.md eficaz en secciones claras con encabezados Markdown. Las que importan: (1) Visión — una frase de qué es el proyecto. (2) Stack REAL — versiones y herramientas exactas, marcando dónde la realidad difiere de lo aparente. (3) Modelo de seguridad o de datos — quién puede tocar qué. (4) Convenciones — nombres, estructura de carpetas, tipografía, lo que sea que tenga reglas. (5) Tono — cómo debe sonar el texto que genere. (6) Reglas de coste — qué NO hacer. En MOONKEY esa última sección dice literalmente 'No añadir dependencias sin justificación. No JS donde basta HTML/CSS.' — prohibiciones concretas, no consejos vagos.",
        "La regla de oro es: cada línea de CLAUDE.md debe cambiar una decisión. Si una frase no altera lo que la IA haría por defecto, sóbrala. 'Escribe código limpio' no cambia nada. 'Hrefs deben ser locale-aware: usa localizedPath(/ruta, locale), nunca /ruta crudo (rompe bajo prefijo de idioma)' cambia cada link que genere. Lo segundo es oro; lo primero es relleno. Un CLAUDE.md de 80 líneas densas vale más que uno de 300 líneas de obviedades, y consume menos contexto en cada sesión.",
        "CLAUDE.md es un documento vivo, no un acto fundacional. Cada vez que la IA hace algo mal de forma sistemática — añade una dependencia que no querías, usa el patrón de i18n equivocado, traduce un término de marca que no se traduce — ese es un fallo del archivo, no de la IA. Vuelve y añade la regla. Con el tiempo el archivo se convierte en el destilado de todas las correcciones que has hecho. En la práctica, una buena sesión termina con 'añade esto a CLAUDE.md para que no vuelva a pasar'. Mantén también las cosas de tu máquina y preferencias personales fuera del CLAUDE.md del repo (eso va en memoria de usuario o en un CLAUDE.local.md no versionado): el del repo lo comparte el equipo.",
        "Para escribirlo, no partas de una plantilla en blanco. Ejecuta `claude` en tu proyecto y pídele: 'Recorre la estructura del repo, lee package.json y los archivos de config, y propón un borrador de CLAUDE.md que capture el stack real y las convenciones que detectes.' La IA es buenísima leyendo tu propio código y resumiéndolo. Luego TÚ corriges: añades las prohibiciones que ella no puede saber (las tablas de otro proyecto, los términos que no se traducen, las decisiones de negocio) y borras lo genérico. Ese ciclo — la IA propone desde el código, tú inyectas el conocimiento que solo está en tu cabeza — produce un archivo honesto en veinte minutos."
      ],
      "ejercicio": "En un proyecto tuyo (o clona uno de ejemplo), lanza `claude` y pídele que genere un borrador de CLAUDE.md leyendo la estructura real del repo. Luego edítalo a mano hasta que cada sección sea específica: stack con versiones, una prohibición concreta (algo que NO debe tocar o añadir), y una regla de convención que cambie cómo genera código. Borra toda frase que no altere una decisión. En una sesión nueva, comprueba que la IA respeta una de tus reglas sin que se la repitas.",
      "entregable": "Un archivo `CLAUDE.md` en la raíz de tu proyecto, de entre 40 y 120 líneas, con secciones de Stack, Convenciones, una prohibición concreta y Tono — donde cada línea cambia una decisión de la IA — verificado en una sesión nueva.",
      "insight": "Cada línea de CLAUDE.md debe cambiar una decisión que la IA tomaría por defecto. Si una frase no altera su comportamiento, es relleno que gasta contexto. El archivo no es documentación: es el destilado de todas las correcciones que ya hiciste, para no repetirlas.",
      "errores": [
        "Escribir CLAUDE.md genérico ('usa buenas prácticas', 'código limpio'): no cambia nada porque la IA ya lo asume por defecto. Solo lo específico y no-obvio aporta.",
        "Documentar el stack aparente en vez del real: si package.json sugiere una cosa pero usas otra, dilo explícitamente o la IA seguirá la pista falsa.",
        "Tratarlo como un acto fundacional inmutable: cuando la IA falla sistemáticamente, es un agujero del archivo. Vuelve y añade la regla.",
        "Meter secretos, claves o rutas absolutas de tu máquina: el archivo se versiona y se comparte. Eso va en memoria de usuario o CLAUDE.local.md.",
        "Olvidar las prohibiciones — lo que NO debe hacer (tablas ajenas, dependencias, términos que no se traducen) — que es justo el conocimiento que la IA no puede deducir sola."
      ]
    },
    "OC-03": {
      "objetivo": "Construir un arsenal personal de prompts reutilizables —cada uno con rol, contexto, estructura e intención explícitos— que producen trabajo de calidad consistente en vez de respuestas de lotería.",
      "porque": "Un prompt vago da resultados aleatorios y te obliga a iterar cinco veces. Un prompt estructurado da el resultado correcto a la primera, y al guardarlo lo reutilizas para siempre. La diferencia entre un aficionado y un operador es que el operador no improvisa: tiene plantillas.",
      "leccion": [
        "Un prompt no es una pregunta: es una instrucción de trabajo. Cuando le pides a un compañero competente que haga algo, no dices 'arregla el login'; dices 'el magic-link de Supabase no redirige tras autenticar, revisa el callback en src/lib/supabase.ts, el problema es probablemente la URL de redirect en la config'. Le das rol implícito, contexto, foco y una hipótesis. Un buen prompt hace lo mismo de forma explícita. Los cuatro pilares son: ROL (quién debe ser la IA: 'eres un ingeniero de Postgres revisando RLS'), CONTEXTO (qué necesita saber: el stack, el archivo, la restricción), ESTRUCTURA (cómo quieres la salida: 'devuelve solo el SQL, sin explicación' o 'lista 3 opciones con trade-offs'), e INTENCIÓN (el objetivo real detrás de la petición, no solo el paso).",
        "El ROL no es teatro: cambia qué conocimiento activa la IA y qué estándar aplica. 'Revisa este código' produce una revisión blanda. 'Eres un auditor de seguridad adversarial: tu trabajo es refutar la afirmación de que esta RLS es segura, busca el camino de escalación' produce un análisis completamente distinto, porque le has dado una postura. En XCAP, los prompts de research llevan el rol de 'analista que no se miente a sí mismo' precisamente porque el sesgo por defecto de un modelo es complacer; el rol lo contrarresta.",
        "La ESTRUCTURA de salida es lo que más tiempo te ahorra y lo que más se olvida. Si no especificas el formato, la IA elige uno y suele ser prosa larga que tienes que recortar. Di exactamente qué quieres: 'Devuelve un único bloque de SQL listo para pegar, sin comentarios ni explicación.' O 'Responde con una tabla de tres columnas: opción, ventaja, riesgo.' O 'Dame el diff, no el archivo completo.' En MOONKEY, donde el tono es 'claro, directo, sin humo ni emojis', un prompt de generación de copy incluye esa restricción de formato textualmente, porque si no la IA mete adornos que luego borras a mano.",
        "La INTENCIÓN es el porqué, y desbloquea soluciones que no pediste. Si dices 'añade un índice a esta columna', la IA lo añade. Si dices 'esta query tarda 2 segundos en la página de cuenta y quiero bajarla de 200ms; el índice es mi primera idea pero abre a alternativas', puede que te diga que el problema real es un N+1 y el índice no ayuda. Cuando das la intención, la IA puede cuestionar tu solución propuesta y ofrecer una mejor. Cuando solo das la orden, ejecuta a ciegas.",
        "Un arsenal es una colección versionada de estos prompts, organizada por tarea, que reutilizas. No empieces de cero cada vez: ten un prompt de 'auditoría de seguridad de RLS', uno de 'genera copy en la voz de la marca', uno de 'refactoriza este componente preservando comportamiento', uno de 'escribe el mensaje de commit a partir del diff'. Guárdalos en un archivo `prompts.md` en tu proyecto o en una nota. MOONKEY LAB tiene una página entera, `/prompts`, dedicada a esto, porque un arsenal es un activo: cada prompt afinado es trabajo que no repites. Parametriza los prompts con huecos `[ARCHIVO]`, `[RESTRICCIÓN]` que rellenas al usarlos.",
        "Itera el prompt, no la salida. Cuando un resultado sale mal, el reflejo del principiante es corregir la respuesta a mano. El reflejo del operador es preguntarse 'qué le faltó saber al prompt' y arreglar el prompt. Si la IA usó tabs y tú quieres espacios, no edites el archivo: añade 'indentación con 2 espacios' al prompt y guárdalo así para siempre. Cada corrección que metes en la plantilla es una corrección que no vuelves a hacer. Con el tiempo tus prompts del arsenal se vuelven quirúrgicos y los resultados salen bien a la primera — que es el objetivo: dejar de iterar."
      ],
      "ejercicio": "Elige una tarea que repites (generar mensajes de commit, revisar seguridad, escribir copy). Escribe un prompt con los cuatro pilares explícitos —rol, contexto, estructura de salida e intención— y pruébalo. Luego degrádalo a una versión vaga ('hazme un commit message') y compara las dos salidas. Afina el bueno hasta que dé el resultado correcto a la primera y guárdalo, parametrizado con huecos, en un `prompts.md`.",
      "entregable": "Un archivo `prompts.md` con al menos tres prompts reutilizables, cada uno con rol, contexto, formato de salida e intención marcados, y parametrizado con huecos `[...]` — más una nota corta comparando la salida del prompt estructurado contra la versión vaga de uno de ellos.",
      "insight": "Itera el prompt, no la salida. Cuando un resultado sale mal, no corrijas la respuesta a mano: arregla el prompt y guárdalo así. Cada corrección que metes en la plantilla es trabajo que no vuelves a hacer nunca.",
      "errores": [
        "Omitir el formato de salida: sin él la IA elige prosa larga que recortas a mano. Di 'solo el SQL', 'dame el diff', 'tabla de 3 columnas'.",
        "Confundir rol con teatro: el rol ('auditor adversarial que debe refutar') cambia qué estándar aplica la IA, no es decoración.",
        "Dar la orden sin la intención: 'añade un índice' se ejecuta a ciegas; 'quiero bajar esta query de 2s a 200ms, el índice es mi idea' deja que la IA proponga algo mejor.",
        "Corregir la salida en vez del prompt: pierdes la mejora. La próxima vez vuelves a cometer el mismo error.",
        "No guardar los prompts que funcionan: un prompt afinado y luego olvidado es trabajo tirado. El arsenal es un activo que se acumula."
      ]
    },
    "OC-04": {
      "objetivo": "Versionar tu trabajo con Git y GitHub de forma que puedas experimentar, romper cosas y volver atrás sin miedo — usando ramas, commits atómicos y diffs para revisar cada cambio que la IA propone.",
      "porque": "Sin control de versiones, un cambio malo de la IA puede destruir horas de trabajo y no hay vuelta atrás. Con Git, cada estado bueno queda guardado y puedes deshacer cualquier cosa. Es la red de seguridad que te permite trabajar rápido y sin miedo.",
      "leccion": [
        "Git resuelve un solo problema, pero es el más importante: poder volver a cualquier estado anterior de tu proyecto. Cada vez que haces un `commit`, congelas una foto del proyecto a la que siempre puedes regresar. Esto cambia tu psicología de trabajo: cuando sabes que el último estado bueno está guardado, dejas a la IA hacer cambios agresivos sin miedo, porque lo peor que pasa es `git restore` y vuelves atrás. Un operador sin Git trabaja con miedo; con Git, trabaja rápido. El flujo mínimo es: `git status` para ver qué cambió, `git add -p` para revisar y elegir cambios trozo a trozo, `git commit -m 'mensaje'` para congelar, y `git log --oneline` para ver tu historia.",
        "El ciclo diario es ver-revisar-congelar. Tras una sesión de Claude Code, NUNCA confíes en que el cambio es correcto: ejecuta `git diff` y lee exactamente qué tocó. Aquí descubres si la IA cambió algo que no debía, borró una línea sin querer o añadió una dependencia. `git diff` es tu herramienta de revisión número uno y la usas en cada sesión. Solo cuando el diff te convence, haces `git add` y `git commit`. Este hábito —leer el diff antes de aceptar— es lo que separa a un operador de alguien que copia y pega a ciegas.",
        "Los commits deben ser atómicos: un commit, un cambio lógico, con un mensaje que explique el PORQUÉ, no el qué. 'Arregla el login' es malo (¿qué tenía?). 'Corrige redirect de magic-link: la URL de callback no incluía el locale y rompía bajo /en/' es bueno: dentro de seis meses, ese mensaje te dice por qué tocaste ese archivo. Claude Code es excelente escribiendo mensajes de commit a partir del diff — pídele 'escribe un mensaje de commit para estos cambios explicando el porqué' y suele clavarlo. Pero revisa: a veces describe el qué y tienes que pedirle el porqué.",
        "Las ramas (`branches`) son universos paralelos para experimentar sin tocar lo que funciona. La rama principal (`main`) es tu versión estable. Cuando vas a probar algo arriesgado —una refactorización grande, una feature nueva— creas una rama con `git checkout -b mi-experimento`, trabajas ahí, y si sale bien la fusionas; si sale mal, la borras y `main` ni se entera. Regla práctica de operador: nunca trabajes directamente en `main` para cambios no triviales. Crea una rama. En MOONKEY LAB cada cambio de seguridad o de RLS va en su rama y se fusiona solo cuando `get_advisors` da luz verde.",
        "GitHub es Git en la nube: tu copia de seguridad remota y el sitio desde donde se despliega tu proyecto. Conectas tu repo local con `git remote add origin git@github.com:usuario/repo.git` y subes con `git push`. A partir de ahí, `git push` tras cada sesión buena guarda tu trabajo fuera de tu máquina — si tu disco muere, tu proyecto vive. Además, plataformas como Cloudflare Pages (que despliega MOONKEY LAB desde `garciafradepablo-pixel/xop`) observan tu repo de GitHub y publican automáticamente cada push a `main`. Así Git no es solo versionado: es el motor de tu deploy, que verás en el siguiente módulo. Para hablar con GitHub desde la terminal, instala el CLI `gh` y autentícate con `gh auth login`.",
        "Un `.gitignore` es tan importante como los commits: lista los archivos que Git debe IGNORAR. Aquí van `node_modules/` (se reinstala desde package.json, no se versiona), archivos `.env` (secretos — NUNCA subas claves a GitHub), y artefactos de build. Subir un `.env` con una clave a un repo público es uno de los incidentes de seguridad más comunes del mundo, y una vez subida la clave, aunque borres el commit, queda en la historia: hay que rotarla. Por eso el `.gitignore` se configura ANTES del primer commit. Pídele a Claude Code 'genera un .gitignore para un proyecto Astro con Supabase' y revisa que incluye `.env` y `node_modules`."
      ],
      "ejercicio": "Toma un proyecto, inicialízalo con `git init` y crea un `.gitignore` que excluya `node_modules` y `.env`. Haz un commit inicial. Crea una rama `git checkout -b experimento`, pide a Claude Code un cambio agresivo, revisa el resultado con `git diff`, y haz commit con un mensaje que explique el porqué (pídeselo a la IA y corrígelo). Vuelve a `main` con `git checkout main` y confirma que el cambio no está ahí. Sube todo a un repo nuevo de GitHub con `gh repo create` y `git push`.",
      "entregable": "Un repo en GitHub con un `.gitignore` correcto (excluyendo `.env` y `node_modules`), un historial de al menos dos commits con mensajes que explican el porqué, y una rama `experimento` separada de `main` demostrando trabajo aislado.",
      "insight": "Lee el `git diff` antes de aceptar cualquier cambio de la IA. Es tu herramienta de revisión número uno: ahí descubres lo que la IA tocó sin decírtelo. Aceptar a ciegas es la forma más rápida de meter un bug que no sabrás de dónde salió.",
      "errores": [
        "Trabajar directamente en `main` para cambios arriesgados: si salen mal, contaminas tu versión estable. Crea una rama y aísla el experimento.",
        "Subir un archivo `.env` con secretos a GitHub: queda en la historia para siempre aunque borres el commit; hay que rotar la clave. Configura `.gitignore` ANTES del primer commit.",
        "Aceptar los cambios de la IA sin leer `git diff`: te tragas líneas borradas o dependencias añadidas que no notarás hasta que algo se rompa.",
        "Mensajes de commit que describen el qué ('cambios en login') en vez del porqué: dentro de seis meses no te dicen nada. Explica la causa.",
        "Commits gigantes que mezclan cinco cambios distintos: imposibles de revisar y de revertir selectivamente. Un commit, un cambio lógico."
      ]
    },
    "OC-05": {
      "objetivo": "Convertir un script o tarea manual en un sistema que corre y publica solo — desde automatizar un comando repetitivo hasta desplegar tu sitio automáticamente en cada push y programar tareas que se ejecutan sin ti.",
      "porque": "Todo lo que haces a mano y repites es tiempo que pierdes y un punto donde te olvidas un paso. Automatizar el deploy y las tareas recurrentes convierte tu trabajo de 'lo hago yo cada vez' a 'el sistema lo hace solo y bien', que es la diferencia entre un hobby y una operación.",
      "leccion": [
        "La automatización empieza en lo pequeño: un comando que repites se convierte en un script. Si cada vez que terminas escribes `npm run build && git add -A && git commit -m wip && git push`, eso es un script `deploy.sh` de cuatro líneas que ejecutas con `./deploy.sh`. El principio es: lo que haces más de tres veces, lo guardas. Pide a Claude Code 'crea un script bash que haga build, commit con timestamp y push, y que aborte si el build falla' — el `&&` entre comandos ya garantiza que si uno falla, los siguientes no corren, que es exactamente lo que quieres en un deploy.",
        "El deploy moderno de un sitio estático es casi gratis de montar y se llama deploy continuo. Plataformas como Cloudflare Pages observan tu repo de GitHub: cada vez que haces `git push` a `main`, ellos clonan tu repo, ejecutan tu comando de build (`npm run build` para Astro), y publican el resultado en una URL real. MOONKEY LAB funciona exactamente así: `git push` al repo `garciafradepablo-pixel/xop` y unos segundos después los cambios están en `moonkeylab.pages.dev`. No subes archivos por FTP, no tocas un servidor: tu único gesto de deploy es `git push`. Esto une el módulo de Git con este: Git no solo versiona, es el gatillo del deploy.",
        "Configurar el deploy continuo es un proceso de una sola vez. Conectas tu cuenta de Cloudflare (o Vercel, o Netlify) a GitHub, eliges el repo, y declaras dos cosas: el comando de build (`npm run build`) y la carpeta de salida (`dist` en Astro). Cloudflare guarda esa config y a partir de ahí cada push dispara un build. Si el build falla, NO publica — tu sitio anterior sigue vivo. Esa es una propiedad de seguridad enorme: un commit roto no tira tu sitio, simplemente no se despliega y te avisa. Verás el log del build en el panel de Cloudflare; cuando algo falla en deploy pero funciona en local, ese log es lo primero que lees.",
        "Las variables de entorno son el puente entre tu deploy y tus secretos, y aquí se cruza con seguridad. Tu código necesita la URL de Supabase y la anon key, pero esas NO van hardcodeadas en el repo (lo viste en el módulo de Git). En local viven en un `.env`; en el deploy las metes en el panel de Cloudflare como variables de entorno, y la plataforma las inyecta en el build. Distinción crítica que aprenderás a fondo en Ops & Security: la anon key de Supabase SÍ puede vivir en el cliente (está protegida por RLS), pero una service-role key JAMÁS — esa daría acceso total saltándose toda la seguridad. Saber qué clave va dónde es competencia de operador.",
        "El siguiente nivel es automatizar tareas recurrentes que no dependen de un push: cosas que deben correr según un horario. Esto son tareas programadas (cron). En XCAP, por ejemplo, hay 'autopilot ticks' — bucles que corren periódicamente para ingerir datos de mercado y actualizar el ledger de Market Memory sin que nadie pulse un botón. La idea general: defines un comando y un horario ('cada día a las 6:00', 'cada 15 minutos'), y la plataforma lo ejecuta sola. Para un sitio estático esto suele vivir en funciones serverless (Cloudflare Workers) o en cron jobs. La clave de operador es que el bucle sea honesto e idempotente: que correr dos veces no duplique datos, y que acumule aprendizaje en vez de ruido — exactamente la disciplina del 'capital invariant' de XCAP, donde el estado solo cambia en eventos reales, no en cada tick.",
        "Automatizar sin observabilidad es peligroso: un sistema que corre solo y falla en silencio es peor que uno manual. Por cada automatización, ten una forma de saber si funcionó. El build de Cloudflare te manda un email si falla. Una tarea cron debe loguear su resultado en algún sitio que tú revises. La regla: nunca automatices algo que no puedas verificar después. Empieza siempre por una versión que corre y te avisa, y solo cuando confías en ella la dejas correr de verdad sola. Un deploy automático sin revisar el log la primera vez es cómo se publica un sitio roto a producción sin enterarte."
      ],
      "ejercicio": "Toma el repo de GitHub del módulo anterior y conéctalo a Cloudflare Pages (o Vercel): configura el comando de build y la carpeta de salida, y mete las variables de entorno en el panel en vez de en el código. Haz un cambio pequeño, `git push`, y confirma en el log de build que se desplegó solo a una URL real. Como bonus, escribe un `deploy.sh` que haga build+commit+push y aborte si el build falla.",
      "entregable": "Un sitio desplegado en una URL pública real (Cloudflare Pages o similar) que se reconstruye automáticamente en cada `git push`, con los secretos en variables de entorno del panel (no en el repo) y un `deploy.sh` que aborta ante un build fallido.",
      "insight": "Nunca automatices algo que no puedas verificar después. Un sistema que corre solo y falla en silencio es peor que uno manual. Cada automatización necesita un canal que te avise — el log de build, un email, un registro que revisas — antes de confiar en que corre sola.",
      "errores": [
        "Hardcodear la URL y las claves de Supabase en el código en vez de usar variables de entorno: las subes a GitHub y se exponen. Van en el `.env` local y en el panel del deploy.",
        "Confundir la anon key con la service-role key: la anon SÍ puede ir al cliente (RLS la protege), la service-role JAMÁS — daría acceso total saltándose la seguridad.",
        "Dejar un deploy automático sin mirar el log la primera vez: si el build falla en la nube pero funciona en local, publicas un sitio roto o no publicas nada sin enterarte.",
        "Escribir tareas cron no idempotentes: correr dos veces duplica datos. El bucle debe acumular aprendizaje, no ruido (la disciplina de los ticks de XCAP).",
        "Automatizar sin observabilidad: un script que falla en silencio te da una falsa sensación de que todo va bien hasta que el daño es grande."
      ]
    },
    "OC-06": {
      "objetivo": "Trasladar tu trabajo de ChatGPT a Claude Code montando un sistema documentado y versionado — donde el contexto vive en CLAUDE.md y en archivos del repo en vez de perderse en hilos de chat, y donde la IA actúa sobre tus archivos reales en vez de solo conversar.",
      "porque": "En ChatGPT tu contexto vive atrapado en hilos que se pierden y la IA no toca tus archivos reales: copias y pegas a mano. Migrar a Claude Code convierte ese caos en un sistema documentado, versionado y operativo. Es el salto de 'conversar con una IA' a 'operar una IA sobre tu proyecto real'.",
      "leccion": [
        "La diferencia fundamental no es de modelo, es de paradigma. ChatGPT es una conversación: tú escribes, la IA responde texto, y tú copias ese texto a tu proyecto a mano. El contexto vive en el hilo, y cuando el hilo crece demasiado o lo cierras, ese contexto se evapora. Claude Code es un operador: vive en tu terminal, lee y escribe tus archivos directamente, ejecuta comandos, hace commits. El contexto no vive en una conversación efímera — vive en CLAUDE.md y en tu repo, que son permanentes y versionados. Migrar es mover tu conocimiento de un sitio que se borra a uno que persiste.",
        "Empieza por auditar qué contexto valioso tienes atrapado en ChatGPT. Probablemente hay hilos donde definiste el stack de un proyecto, decisiones de arquitectura, convenciones de estilo, prompts que te funcionaron bien. Todo eso es activo que ahora mismo solo existe en hilos. El primer paso de la migración es extraerlo: recorre tus conversaciones importantes y saca las decisiones duraderas (no el chat completo, las conclusiones). 'Usamos Astro, no React, por X', 'el tono es directo sin emojis', 'la DB la comparto con otro proyecto y estas tablas son intocables'. Eso es exactamente el material de un CLAUDE.md.",
        "El corazón de la migración es construir el CLAUDE.md a partir de ese contexto extraído. Lo que en ChatGPT tenías que repetir al inicio de cada hilo ('recuerda que uso Astro y Supabase y el tono es...'), en Claude Code lo escribes una vez en CLAUDE.md y se carga solo en cada sesión, para siempre. Esto conecta directamente con el módulo OC-02: la migración no es más que volcar el contexto disperso de tus hilos en el archivo de memoria persistente. Un buen ejercicio es pedirle a ChatGPT mismo: 'resume todas las decisiones técnicas y convenciones de este hilo en formato de bullets para un archivo de contexto' — y usar esa salida como borrador.",
        "Cambia también tu forma de pedir las cosas. En ChatGPT pides 'escríbeme el código para X' y recibes un bloque que pegas. En Claude Code pides 'crea el archivo src/lib/x.ts que haga X, siguiendo las convenciones de CLAUDE.md, y enséñame el diff' — la IA crea el archivo en su sitio, tú revisas el diff con git, y haces commit. El gesto de copiar-y-pegar desaparece. Esto requiere desaprender un hábito: dejar de tratar a la IA como un generador de texto y empezar a tratarla como un colaborador que actúa sobre tu proyecto. Al principio cuesta confiar en que tocará los archivos correctos; por eso `git diff` y los commits del módulo OC-04 son tu red de seguridad.",
        "Migrar bien significa montar el sistema completo, no solo cambiar de herramienta. El sistema de un operador es: un repo con git, un CLAUDE.md con el contexto, un .gitignore protegiendo secretos, un arsenal de prompts en un prompts.md, y el deploy continuo conectado. Cuando tienes esos cinco elementos, has dejado de 'usar una IA' y has montado una operación. Cada uno de los módulos anteriores de Operator Core era una pieza; este módulo las ensambla migrando un proyecto real desde el caos de hilos de ChatGPT hasta ese sistema documentado. El resultado es que cualquier sesión nueva — tuya o de un colaborador — arranca con todo el contexto cargado.",
        "El valor que sentirás de inmediato es la continuidad. En ChatGPT, retomar un proyecto tras una semana significa releer hilos y recordar dónde estabas. En el sistema migrado, abres la terminal, `cd` al proyecto, `claude`, y la IA ya sabe el stack, las convenciones y las prohibiciones porque están en CLAUDE.md; tú ves dónde estabas en `git log`. El proyecto se documenta a sí mismo. Esa continuidad es lo que permite operar muchos proyectos a la vez sin que se mezclen — como hace falta cuando llevas XNLAB, XHUB, XCAP, Espejo y esta escuela en paralelo: cada uno con su CLAUDE.md, su repo y su contexto aislado, todos operados con el mismo flujo."
      ],
      "ejercicio": "Elige un proyecto que hayas estado llevando en ChatGPT. Extrae de tus hilos las decisiones y convenciones duraderas (pídele a ChatGPT que las resuma en bullets). Crea el repo con git, vuelca ese contexto en un CLAUDE.md, añade un .gitignore que proteja secretos, y guarda en un prompts.md los 2-3 prompts que más usabas. Luego lanza `claude` y pídele una tarea real del proyecto pidiendo el diff en vez de un bloque de texto — revísalo con git y haz commit. Compara la experiencia con cómo lo hacías antes.",
      "entregable": "Un proyecto migrado y operativo: repo git con CLAUDE.md (contexto extraído de tus hilos), .gitignore protegiendo secretos, prompts.md con tus prompts clave, y al menos un commit de un cambio real hecho por Claude Code sobre los archivos (no copiado-pegado).",
      "insight": "El cambio real no es de modelo, es de paradigma: pasas de conversar (contexto atrapado en hilos que se borran) a operar (contexto vivo en CLAUDE.md y el repo, permanente y versionado). Migrar es mover tu conocimiento de un sitio que se evapora a uno que persiste y se carga solo.",
      "errores": [
        "Copiar el contenido completo de los hilos de ChatGPT en vez de extraer solo las decisiones duraderas: llenas el CLAUDE.md de ruido conversacional en vez de reglas que cambian decisiones.",
        "Seguir usando Claude Code como ChatGPT — pidiendo bloques de texto para copiar-pegar — en vez de dejar que actúe sobre los archivos y revisar el diff. Pierdes todo el valor del paradigma.",
        "Migrar la herramienta pero no montar el sistema: sin CLAUDE.md, sin git, sin .gitignore, sigues en el caos, solo que en otra terminal.",
        "Confiar a ciegas en que la IA tocó los archivos correctos sin revisar `git diff`: justo al migrar, cuando aún no tienes intuición de su comportamiento, esa red de seguridad es imprescindible.",
        "Mezclar el contexto de varios proyectos en un solo sitio: cada proyecto necesita su propio repo y su propio CLAUDE.md aislado, o las convenciones de uno se filtran a otro."
      ]
    },
    "OC-07": {
      "objetivo": "Construir un checklist de verificación reutilizable y aplicarlo a un cambio real de Claude Code antes de publicarlo, demostrando que el cambio hace lo que dice.",
      "porque": "El fallo número uno del operador novato no es escribir mal el prompt: es creerse el \"listo, ya funciona\" de la IA. Claude Code te entrega un diff que compila, suena seguro y a veces hasta corre, pero verificar que compila NO es verificar que funciona. La verificación es la frontera entre alguien que mueve archivos y un operador en quien se puede confiar: significa que cuando dices \"está hecho\", está hecho. No es una fase opcional al final del trabajo; es el trabajo. Un cambio sin verificar es una hipótesis, no un entregable, y enviar hipótesis a producción es como la academia pierde clientes y como tú pierdes la mañana siguiente arreglando lo que rompiste ayer.",
      "leccion": [
        "Verificación NO es testing automatizado, y NO es solo de seguridad. Es la disciplina general de probar que un cambio se comporta como esperas ANTES de publicarlo. Tiene cuatro niveles que se aplican en orden y casi nunca necesitas los cuatro: (1) ¿compila / arranca sin error? (2) ¿el diff dice lo que creías pedir? (3) ¿el comportamiento observable cambió como esperabas? (4) ¿rompiste algo que antes funcionaba (regresión)?",
        "El nivel barato y obligatorio: LEER EL DIFF. Antes de aceptar nada, `git diff` (o el panel de diff del editor). Claude Code es excelente describiendo lo que hizo y mediocre delatando lo que hizo de más: un archivo que no pediste tocar, un `console.log` olvidado, una dependencia añadida, un bloque borrado \"de paso\". Si el diff toca más de lo que tu frase pedía, eso es una señal, no una casualidad. Regla: nunca aceptes un cambio cuyo diff no entiendes línea por línea.",
        "\"Parece que funciona\" es una mentira cómoda con tres caras. Cara 1: arrancó sin error → confundes 'no crashea' con 'hace lo correcto'. Cara 2: lo probé una vez con el caso feliz → no probaste el input vacío, el duplicado, el usuario sin permiso. Cara 3: la IA me dijo que lo verificó → la IA no ejecutó nada, predijo texto. El antídoto es siempre el mismo: observar el comportamiento real con tus propios ojos, no aceptar el reporte de quien hizo el cambio (ni humano ni IA).",
        "Smoke test = el camino más corto que demuestra que la pieza central respira. No es cobertura total; es el 20% de pruebas que atrapan el 80% de los desastres. Para una web: levanta `npm run dev`, abre la ruta que tocaste, haz la acción principal, mira que pasa lo esperado. Para un script: córrelo con un input conocido y compara la salida con lo que SABES que debe dar. Defines el smoke test ANTES de pedir el cambio, no después, para no engañarte ajustando la prueba al resultado.",
        "Aserción sobre observación: no mires la pantalla buscando confirmación, define de antemano la afirmación falsable. Mal: \"a ver si carga bien\". Bien: \"al enviar el formulario sin email, debe aparecer el error 'email requerido' y NO debe insertarse fila en `leads`\". Una buena verificación se puede escribir como una frase que sería claramente falsa si el cambio falló. Si no puedes escribir esa frase, no sabes qué estás verificando.",
        "Regresión: el cambio nuevo a menudo rompe lo viejo. La pregunta que separa al senior del junior no es \"¿mi feature funciona?\" sino \"¿qué funcionaba antes que mi cambio pudo haber roto?\". En esta escuela, tocar `galaxy.ts` puede romper el mapa SVG y todas las páginas de constelación, porque se generan del mismo archivo. Antes de publicar, lista mentalmente qué OTRAS cosas dependen de lo que tocaste y comprueba al menos una.",
        "Cómo usar a Claude Code COMO verificador, sin delegarle el juicio: pídele que ejecute el comando real (`npm run build`, el test, el script) y te pegue la SALIDA cruda, no su resumen. \"Corre el build y pégame la salida completa\" es verificable; \"¿funciona el build?\" invita a la alucinación complaciente. La IA verifica ejecutando y mostrando evidencia; tú verificas leyendo esa evidencia. Nunca al revés.",
        "Verificación proporcional al riesgo (no toda verificación cuesta lo mismo). Cambiar un texto de copy: leer el diff basta. Tocar RLS, un trigger de Postgres o el flujo de pago: build + smoke + regresión + revisar advisors. El operador maduro calibra el esfuerzo de verificación al coste de equivocarse, no verifica todo igual ni confía en todo igual."
      ],
      "ejercicio": "Toma un cambio pequeño y real en este repo (ej. ajustar el `blurb` de un módulo en `src/data/galaxy.ts`, o añadir un término al `glosario.ts`). ANTES de tocar nada, escribe en un archivo `VERIFY.md` tres aserciones falsables sobre el resultado esperado (ej. \"la página /constelacion/operator-core muestra el nuevo blurb\", \"`npm run build` termina sin error\", \"el resto de módulos siguen apareciendo iguales\"). Pide el cambio a Claude Code. Luego: (1) lee el `git diff` completo y anota cualquier línea que no esperabas; (2) ejecuta `npm run build` y pega la salida; (3) levanta `npm run dev` y comprueba con tus ojos tus tres aserciones, marcando cada una PASA/FALLA. Si alguna falla, NO publicas: arreglas y repites el ciclo.",
      "entregable": "Un `VERIFY.md` con las 3 aserciones falsables escritas ANTES del cambio, cada una marcada PASA/FALLA con la evidencia observada (salida del build pegada + qué viste en pantalla), y la confirmación de que el `git diff` no contiene líneas inesperadas. Es la prueba de que verificaste comportamiento, no de que la IA dijo que sí.",
      "insight": "La pregunta del operador no es \"¿la IA dijo que funciona?\" sino \"¿qué evidencia tengo, con mis ojos, de que funciona — y qué pudo romper que antes funcionaba?\". Quien envía sin esa evidencia no está más rápido: está pidiendo prestado tiempo a su yo de mañana, con intereses.",
      "errores": [
        "Aceptar el diff sin leerlo porque \"compila\" — compilar prueba sintaxis, no intención; lee siempre línea por línea lo que tocó.",
        "Probar solo el caso feliz y declarar victoria — el bug vive en el input vacío, el duplicado y el usuario sin permiso, no en el camino que ya sabías que iba a funcionar.",
        "Pedir a Claude Code \"¿esto funciona?\" en vez de \"corre el comando y pégame la salida\" — lo primero invita a una alucinación complaciente; lo segundo produce evidencia.",
        "Olvidar la regresión: verificar solo la feature nueva y no comprobar qué dependía de lo que tocaste (en este repo, `galaxy.ts` alimenta el mapa Y todas las páginas de constelación).",
        "Escribir la prueba DESPUÉS de ver el resultado y ajustarla para que pase — la aserción se define antes del cambio o no vale como verificación."
      ]
    },
    "OC-08": {
      "objetivo": "Diseñar un protocolo de sesión larga: detectar la degradación de contexto, montar un handoff que reinicie limpio sin perder estado, y delegar una rama de trabajo a un subagente. Entregar el protocolo aplicado a una sesión real.",
      "porque": "Este es el modo de fallo DIARIO del operador IA, y casi nadie lo nombra. La ventana de contexto no es infinita: a medida que la sesión crece, Claude Code empieza a olvidar decisiones de hace una hora, a repetir trabajo, a contradecir lo que acordasteis, a editar el archivo equivocado. No es que el modelo se haya vuelto tonto; es que tú seguiste empujando una sesión que ya estaba saturada. El operador novato lo aguanta, pelea contra una IA cada vez más perdida, y pierde la tarde. El profesional reconoce la señal temprano y hace lo contraintuitivo: para, resume, reinicia o delega. Saber CUÁNDO empezar de cero y cómo hacerlo sin perder el hilo vale más que cualquier prompt perfecto, porque ningún prompt sobrevive a un contexto podrido.",
      "leccion": [
        "Las señales de degradación de contexto, en orden de aparición: (1) la IA te vuelve a preguntar algo que ya respondiste; (2) re-implementa código que ya existía o deshace un cambio acordado; (3) sus respuestas se vuelven genéricas, pierden los nombres concretos de tu proyecto; (4) edita el archivo equivocado o inventa rutas. En cuanto veas la señal 1-2, estás en la zona roja. No esperes a la señal 4: ahí ya estás limpiando un destrozo, no avanzando.",
        "La intuición correcta es contraintuitiva: cuando la sesión se descarría, parar y reiniciar es MÁS RÁPIDO que insistir. Pelear contra un contexto saturado es interés compuesto negativo: cada turno añade ruido al ruido. La pregunta no es \"¿cómo le explico mejor?\" sino \"¿este contexto todavía me ayuda o ya me estorba?\". Si estorba, el arreglo no es un prompt mejor, es un contexto nuevo.",
        "El handoff: cómo reiniciar SIN perder estado. Antes de cerrar una sesión derailada, pide el resumen de traspaso: \"Resume en un bloque qué intentábamos, qué decisiones tomamos, qué archivos tocamos, qué quedó pendiente y cuál es el siguiente paso concreto\". Ese bloque es tu estado portátil. Sesión nueva → pegas el resumen → Claude Code arranca con contexto fresco y memoria de lo importante, sin arrastrar el ruido. El handoff convierte un reinicio de pérdida total en un reinicio de costo casi cero.",
        "Tu memoria persistente vive en disco, no en la ventana. Lo que de verdad importa no debe vivir solo en el chat: vive en `CLAUDE.md` (decisiones de proyecto, convenciones, qué NO tocar) y en archivos de notas del repo. Una sesión nueva que lee tu `CLAUDE.md` ya sabe lo esencial. Regla: si una decisión la vas a necesitar mañana, no la dejes en el chat — escríbela en `CLAUDE.md`. La ventana de contexto es RAM volátil; el repo es tu disco.",
        "Prevención: sesiones cortas y de foco único superan a la maratón. Una sesión = un objetivo acotado. \"Arregla la verificación de RLS\" es buena sesión; \"refactoriza todo el backend\" es una maratón que se va a degradar a mitad. Cuando una tarea es grande, la partes en sesiones independientes con handoff entre ellas, no la metes entera en una ventana que no aguanta.",
        "Delegación a subagentes: paralelismo y aislamiento de contexto. Un subagente es una sesión hija con su PROPIA ventana de contexto, a la que le das una tarea acotada y un entregable definido. Dos ganancias: (1) trabajo paralelo — un subagente audita seguridad mientras otro escribe tests; (2) aislamiento — el subagente quema su contexto investigando un archivo enorme y te devuelve solo el resumen, sin contaminar tu sesión principal. Delegas lo que es voluminoso de explorar pero compacto de reportar.",
        "Cómo escribir un buen encargo de subagente: como si el subagente no tuviera memoria de tu conversación — porque no la tiene. Rutas absolutas, contexto autocontenido, y un entregable explícito (\"devuélveme X\"). Un encargo vago a un subagente es peor que no delegar: gastas una ventana entera para recibir basura genérica. El subagente brilla en tareas con frontera clara, no en \"ayúdame con esto\".",
        "Recuperar el hilo cuando Claude Code se pierde a media tarea: no sigas empujando. Para, pídele que te diga con SUS palabras qué cree que está haciendo y por qué. Si su versión no coincide con la tuya, ahí está la deriva — corrígela explícitamente o haz handoff a sesión limpia. La peor opción es la del novato: dar más instrucciones encima de un modelo que ya entendió mal, apilando correcciones sobre un malentendido de base."
      ],
      "ejercicio": "En tu próxima sesión real de trabajo en este repo, lleva un registro vivo. (1) Anota el primer momento en que detectes una señal de degradación (la IA repite, olvida un nombre, toca el archivo equivocado) y cuál fue. (2) En ese punto, en vez de insistir, pide el bloque de handoff (qué intentábamos, decisiones, archivos, pendiente, siguiente paso). (3) Abre una sesión nueva, pega el handoff, y verifica que retoma sin re-preguntar lo ya resuelto. (4) Aparte, delega a un subagente UNA tarea acotada con entregable explícito — ej. \"Lee `src/data/galaxy.ts` y devuélveme la lista de todos los `code` de módulo con su `status`, nada más\" — y comprueba que el reporte es usable sin que el subagente conociera tu conversación.",
      "entregable": "Un `SESSION-LOG.md` con: la señal de degradación que detectaste (cuál y en qué turno), el bloque de handoff que generaste, la confirmación de que la sesión nueva retomó sin re-preguntar, y el encargo + reporte del subagente. Es la prueba de que sabes operar la sesión como un recurso finito, no aguantarla hasta que se rompe.",
      "insight": "La habilidad invisible del operador senior no es escribir el prompt perfecto: es saber cuándo el contexto actual ya es un lastre y tener el reflejo de reiniciar limpio con handoff en vez de pelear. La ventana de contexto es RAM volátil; tu disciplina de handoff y tu `CLAUDE.md` son el disco. Quien confunde los dos pierde la tarde defendiendo una sesión que ya estaba muerta.",
      "errores": [
        "Insistir en una sesión saturada con más y más prompts — es interés compuesto negativo; cada turno añade ruido, el arreglo es contexto nuevo, no prompt mejor.",
        "Reiniciar sin handoff y perder todo el estado — siempre pide el bloque de resumen (intención, decisiones, archivos, pendiente, siguiente paso) antes de cerrar.",
        "Dejar decisiones importantes solo en el chat en vez de escribirlas en `CLAUDE.md` — el chat es RAM volátil; lo que necesitarás mañana va a disco.",
        "Meter una tarea enorme (\"refactoriza todo\") en una sola maratón que se degrada a mitad — parte en sesiones de foco único con handoff entre ellas.",
        "Delegar a un subagente con un encargo vago y sin entregable — sin rutas absolutas, contexto autocontenido y un \"devuélveme X\" explícito, gastas una ventana entera para recibir basura genérica."
      ]
    }
  },
  "data-systems": {
    "DS-01": {
      "objetivo": "Modelar un esquema en Postgres con tablas, claves, tipos y relaciones que reflejen invariantes reales del dominio y no te obliguen a migraciones dolorosas tres semanas después.",
      "porque": "El modelo de datos es la decisión más cara de revertir de todo el stack: el código se reescribe en una tarde, pero una tabla mal tipada con 50.000 filas en producción te persigue durante meses. En la DB compartida de MOONKEY LAB (profiles, progress, proofs) un tipo flojo o una FK olvidada es un agujero de integridad que ninguna RLS arregla.",
      "leccion": [
        "Postgres no es \"una hoja de cálculo con SQL\". Es un motor relacional con un sistema de tipos serio, y tu primer trabajo como operador de datos es usar ese sistema de tipos para que estados imposibles sean literalmente irrepresentables. Antes de escribir un solo CREATE TABLE, enumera las entidades del dominio y las relaciones entre ellas. En MOONKEY LAB las entidades reales son: un usuario (profiles), su avance por módulo (progress), las pruebas que sube (proofs), el feedback que deja (feedback) y los leads de captación. Cada una es una tabla. Cada relación 1-a-N entre ellas es una foreign key. No empieces por las columnas; empieza por las flechas entre cajas.",
        "El identificador. En Supabase la tabla profiles cuelga de auth.users, así que su clave primaria NO es un id autogenerado nuevo: es `id uuid primary key references auth.users(id) on delete cascade`. Esto es el patrón canónico de Supabase y es deliberado — el uuid del usuario autenticado ES la identidad en toda tu app, y `auth.uid()` (que usarás en cada policy RLS) devuelve exactamente ese uuid. Para tablas hijas como progress o proofs sí usas una clave propia: `id uuid primary key default gen_random_uuid()`, más una columna `user_id uuid not null references auth.users(id) on delete cascade`. Fíjate en `not null` y en `on delete cascade`: si borras un usuario, sus pruebas se van con él; si permitieras user_id nulo, tendrías filas huérfanas que ninguna policy `user_id = auth.uid()` podría ni mostrar ni proteger.",
        "Los tipos importan y son baratos de acertar al principio. Para timestamps usa SIEMPRE `timestamptz` (with time zone), nunca `timestamp` a secas — un proyecto con usuarios en Chiang Mai, Mallorca y Madrid no puede permitirse ambigüedad de zona horaria, y `timestamptz` guarda en UTC y convierte en la frontera. Para dinero, `numeric`, jamás `float` (los flotantes binarios no representan 0.10 exacto). Para un campo con un conjunto cerrado de valores —el rango del operador: monkey, gorilla, eagle— tienes dos opciones honestas: un `enum` de Postgres, o `text` con un `check (role in ('monkey','gorilla','eagle'))`. Prefiero el CHECK sobre el enum en proyectos jóvenes: añadir un valor a un enum requiere `ALTER TYPE` y tiene fricción transaccional, mientras que cambiar un CHECK es un ALTER de tabla ordinario. Para datos semiestructurados —el payload de una proof, metadatos flexibles— usa `jsonb`, no `json`: jsonb es binario, indexable con GIN y deduplica claves.",
        "Defaults y NOT NULL son tu primera línea de validación, antes que cualquier RLS o cualquier check de la app. `created_at timestamptz not null default now()` significa que es imposible insertar una fila sin fecha. `status text not null default 'forming'` significa que el estado nunca es nulo y por tanto tu código nunca necesita ramas `if status is null`. Cada columna que dejas nullable es una rama condicional que pagas para siempre en el código que la lee. La regla: una columna es NOT NULL por defecto, y solo la haces nullable cuando la ausencia de valor es un estado de dominio legítimo y distinto de un valor vacío.",
        "Unicidad e índices. La relación \"un usuario tiene exactamente un registro de progreso por módulo\" no es un comentario: es `unique (user_id, module_code)`. Esa restricción única además te da el índice que necesitas para el upsert (`insert ... on conflict (user_id, module_code) do update`), que es justo como MOONKEY persiste el avance. Aparte de las claves, crea índices sobre las columnas por las que filtras de verdad: si consultas proofs por `user_id` constantemente, `create index on proofs (user_id)`. Pero no índices por reflejo — cada índice ralentiza los INSERT y ocupa espacio; índexa lo que mides que se consulta, no lo que imaginas que se consultará.",
        "Aplica el esquema como una migración versionada, nunca a mano en el editor SQL. En Supabase eso es `apply_migration` (un archivo .sql con nombre y timestamp), no `execute_sql` suelto. La diferencia es auditoría: dentro de seis meses querrás saber por qué proofs tiene esa columna, y la respuesta está en el historial de migraciones, no en tu memoria. Tras aplicar, corre `generate_typescript_types` para que el tipo de TypeScript del cliente derive del esquema real — así la DB es la fuente única de verdad y el front no puede mentir sobre la forma del dato.",
        "Un ejemplo concreto y completo para proofs, la tabla donde un operador sube la prueba de que completó un entregable: una clave propia uuid, un user_id NOT NULL con FK y cascade, un module_code con CHECK contra la lista de códigos válidos, un payload jsonb, un created_at timestamptz NOT NULL default now(), y un índice por user_id. Ese diseño hace que (a) toda fila pertenezca a un usuario existente, (b) ninguna fila quede huérfana, (c) ningún module_code inventado entre, y (d) la consulta \"mis pruebas\" sea rápida. Sobre esa base —y solo sobre ella— tiene sentido poner RLS en el módulo siguiente."
      ],
      "ejercicio": "En el proyecto Supabase moonkey-lab (o una branch de desarrollo), escribe la migración SQL que crea la tabla `proofs` desde cero: id uuid PK con gen_random_uuid(), user_id uuid NOT NULL references auth.users(id) on delete cascade, module_code text NOT NULL con CHECK contra al menos tres códigos reales ('DS-01','DS-02','DS-03'), payload jsonb NOT NULL default '{}', created_at timestamptz NOT NULL default now(), y un índice sobre user_id. Aplícala con apply_migration. Luego intenta a propósito un INSERT con module_code='XX-99' y otro con user_id de un uuid que no existe en auth.users, y comprueba que Postgres rechaza ambos. Finalmente corre generate_typescript_types y verifica que el tipo Proof generado refleja exactamente tus columnas y nulabilidades.",
      "entregable": "Un archivo de migración `supabase/migrations/<timestamp>_create_proofs.sql` aplicado en una branch, más una captura del error de Postgres ante el INSERT inválido (violación de CHECK y de FK) y el tipo TypeScript `Proof` generado a partir del esquema.",
      "insight": "El modelo de datos correcto es el que convierte un bug de la aplicación en un error de la base de datos. Cada CHECK, cada NOT NULL y cada FK es una clase entera de bugs que tu código de aplicación ya no tiene que defender — porque Postgres los rechaza antes de que existan.",
      "errores": [
        "Usar `timestamp` en vez de `timestamptz` y descubrir el desfase horario solo cuando un usuario en Tailandia ve fechas equivocadas; en producción ya es tarde.",
        "Dejar columnas nullable \"por si acaso\": cada nullable innecesario es una rama `if x is null` que arrastras en todo el código que lee la tabla.",
        "Crear la tabla profiles con un id propio en vez de `references auth.users(id)`, rompiendo la cadena entre auth.uid() y la identidad — y dejando la RLS posterior sin un campo contra el que comparar.",
        "Aplicar el esquema con execute_sql a mano en vez de apply_migration: pierdes el historial versionado y nadie podrá reconstruir por qué el esquema es como es.",
        "Sembrar índices por reflejo sobre columnas que nunca filtras: penalizas cada INSERT y ocupas espacio sin acelerar ninguna consulta real."
      ]
    },
    "DS-02": {
      "objetivo": "Escribir y verificar políticas Row Level Security que hagan que un usuario solo pueda leer y escribir sus propias filas, entendiendo que el gate del cliente es UX y la RLS es la única seguridad real en un sitio estático.",
      "porque": "MOONKEY LAB y Espejo son sitios estáticos sin handler server-side: no hay un backend donde meter un `if (user.id === row.user_id)`. El cliente lleva la anon key, que es pública por diseño. Si la RLS está mal, cualquiera con las DevTools abiertas lee la tabla profiles entera. La RLS no es una capa más de defensa: es la ÚNICA capa.",
      "leccion": [
        "Interioriza el modelo de amenaza antes de tocar una policy. En una SSG con Supabase, el atacante NO usa tu interfaz. Abre la consola del navegador, coge la anon key (que está en el bundle, porque tiene que estar), instancia su propio cliente Supabase y llama `supabase.from('profiles').select('*')`. Tu archivo admin.astro que \"oculta\" el panel no existe para él. El botón deshabilitado no existe para él. Lo único que se interpone entre su SELECT y todas las filas de todos los usuarios es la Row Level Security de Postgres. Por eso la frase del módulo: el gate del cliente es UX (mejora la experiencia del usuario legítimo), la RLS es la seguridad (detiene al ilegítimo).",
        "RLS se activa por tabla y por defecto es deny-all. `alter table profiles enable row level security` — y en ese instante, sin ninguna policy, NADIE (salvo el rol de servicio y el dueño de la tabla) puede leer ni escribir nada. Esto es lo correcto: empiezas desde cero permisos y abres exactamente lo que necesitas. El error catastrófico es habilitar RLS y olvidar una operación: si pones policies de SELECT pero ninguna de INSERT, los INSERT fallan silenciosamente; peor, si NO habilitas RLS en una tabla nueva, está completamente abierta a la anon key. Por eso DS-06 introduce los advisors: detectan exactamente la tabla sin RLS que se te olvidó.",
        "Una policy tiene dos cláusulas que confunde todo el mundo: USING y WITH CHECK. USING filtra qué filas existentes ve o toca la operación (SELECT, UPDATE, DELETE). WITH CHECK valida qué filas nuevas o modificadas se permiten escribir (INSERT, UPDATE). Para SELECT solo hay USING. Para INSERT solo hay WITH CHECK. Para UPDATE hay AMBAS: USING dice qué filas puedes actualizar, WITH CHECK dice en qué pueden convertirse. Un error clásico es poner USING en un INSERT y que no haga nada, o poner solo USING en un UPDATE y permitir que el usuario mueva su fila a otro user_id. El patrón self-or-admin de MOONKEY: `using (user_id = auth.uid() or is_admin())`.",
        "El patrón canónico self. Para proofs, la policy de inserción real de MOONKEY es `proofs_insert_self`: `create policy proofs_insert_self on proofs for insert to authenticated with check (user_id = auth.uid())`. Léela en voz alta: un usuario autenticado puede insertar una proof solo si el user_id de la fila que inserta es igual a su propia identidad. No puede crear una proof a nombre de otro, porque WITH CHECK rechazaría cualquier fila con user_id ≠ auth.uid(). Fíjate en `to authenticated`: la policy ni siquiera aplica al rol anon, así que un visitante sin sesión no puede insertar nada. Para SELECT, el patrón self-or-admin: `using (id = auth.uid() or is_admin())` en profiles — ves tu fila, o todas si eres admin.",
        "auth.uid() es el corazón del sistema y conviene saber qué es: una función SECURITY DEFINER de Supabase que extrae el `sub` del JWT que el cliente envía en cada petición. El cliente no puede falsificarlo porque el JWT está firmado por Supabase con un secreto que el cliente no tiene; manipularlo invalida la firma y Postgres rechaza la sesión. Por eso `user_id = auth.uid()` es confiable de un modo que `user_id = <valor que mandó el cliente>` jamás lo sería. La autoridad vive en la firma del token, no en la buena fe del front.",
        "Verificación por impersonación — esto separa a un operador serio de uno que \"cree\" que su RLS funciona. No basta con leer la policy y asentir. En el editor SQL de Supabase puedes simular ser un usuario concreto fijando el rol y el claim: `set local role authenticated; set local request.jwt.claims to '{\"sub\":\"<uuid-del-usuario-A>\"}';` y entonces `select * from profiles`. Si ves filas de otros usuarios, tu RLS está rota. El modelo de seguridad de MOONKEY dice explícitamente \"verificado por impersonación\": un no-admin no puede leer filas ajenas, y eso se PROBÓ, no se asumió. Haz lo mismo: impersona al usuario A, intenta leer la fila del usuario B, y exige que el resultado sea cero filas.",
        "Dos trampas finales. Primera: el rol `service_role` (la clave de servicio, NUNCA en el cliente) bypassa toda RLS — por eso jamás vive en una SSG y solo se usa en edge functions de confianza. Segunda: las policies son permisivas por defecto y se combinan con OR, no con AND. Si tienes dos policies de SELECT sobre la misma tabla, una fila visible para CUALQUIERA de las dos es visible. Esto sorprende: añadir una policy nunca restringe, solo amplía. Para restringir necesitas policies RESTRICTIVE explícitas, o —más simple y lo habitual— una sola policy bien escrita por operación."
      ],
      "ejercicio": "En una branch de moonkey-lab, sobre la tabla proofs ya creada: habilita RLS, y escribe tres policies — `proofs_select_self` (SELECT, using user_id=auth.uid() or is_admin()), `proofs_insert_self` (INSERT, with check user_id=auth.uid()), y NINGUNA policy de UPDATE/DELETE (deny por defecto). Crea dos usuarios de prueba A y B con sendas proofs. En el editor SQL, impersona a A (set local role authenticated + jwt claims sub=uuid_A) y verifica: (1) un SELECT devuelve solo la proof de A; (2) un INSERT con user_id=uuid_B es rechazado por WITH CHECK; (3) un DELETE de la proof de A es rechazado porque no hay policy. Documenta cada uno de los tres resultados.",
      "entregable": "Migración con las tres policies sobre proofs, más un log de la sesión de impersonación que demuestra los tres comportamientos: A no ve a B (SELECT), A no puede escribir como B (INSERT con WITH CHECK), y nadie puede borrar (sin policy DELETE).",
      "insight": "Una RLS que no has roto a propósito impersonando a otro usuario es una RLS que no sabes si funciona. La confianza en seguridad no viene de leer la policy; viene de haber intentado el abuso y haber visto a Postgres negarlo.",
      "errores": [
        "Confundir el gate de admin.astro con seguridad: ocultar el panel en el front no impide que el atacante llame a la tabla directamente con la anon key desde la consola.",
        "Poner USING donde va WITH CHECK (o al revés): un INSERT con solo USING no valida nada y deja entrar filas con user_id ajeno.",
        "Habilitar RLS y olvidar la policy de alguna operación: los INSERT empiezan a fallar en silencio, o peor, una tabla sin RLS queda abierta de par en par a la anon key.",
        "Asumir que la RLS funciona por leerla, en vez de verificarla por impersonación con set role + jwt claims y exigir cero filas ajenas.",
        "Creer que añadir una segunda policy permisiva restringe el acceso: las policies se combinan con OR, así que cada policy nueva solo amplía lo visible, nunca lo reduce."
      ]
    },
    "DS-03": {
      "objetivo": "Implementar autenticación por magic-link con Supabase y manejar la sesión del lado del cliente de forma honesta: saber qué garantiza la sesión, qué no, y por qué eso no debilita tu RLS.",
      "porque": "MOONKEY LAB y Espejo usan login sin contraseña: el usuario pone su email, recibe un enlace, hace clic y queda autenticado. Es la mejor UX y elimina toda una clase de vulnerabilidades (no almacenas contraseñas, no hay fugas de hashes). Pero la sesión vive en el navegador, y un operador que no entiende dónde y cómo se guarda el token confundirá conveniencia con seguridad.",
      "leccion": [
        "Cómo funciona el magic-link, paso a paso. El cliente llama `supabase.auth.signInWithOtp({ email })`. Supabase genera un token de un solo uso, lo asocia al email y manda un correo con un enlace que apunta a tu app con ese token en el fragmento de URL. El usuario hace clic; tu app, al cargar, detecta el token, lo canjea con Supabase por un par access_token / refresh_token, y a partir de ahí el cliente está autenticado. El access_token es un JWT firmado con caducidad corta (por defecto una hora); el refresh_token es de larga duración y sirve para obtener nuevos access tokens sin que el usuario vuelva a hacer login. Todo esto lo orquesta el SDK; tu trabajo es entender el flujo, no reimplementarlo.",
        "Dónde vive la sesión. Por defecto, el SDK de Supabase en el navegador persiste la sesión en localStorage. Esto tiene una consecuencia de seguridad que debes decir en voz alta: un token en localStorage es legible por cualquier JavaScript que corra en tu página. Eso significa que tu superficie de ataque número uno es el XSS — si un atacante consigue inyectar JS en tu sitio (un script de terceros comprometido, un innerHTML con input sin sanear), puede leer el token y robar la sesión. La defensa no es esconder el token; es no tener XSS: no metas HTML de usuario sin sanear, audita cada dependencia de front, y trata cada `<script>` de terceros como código que verá tus tokens.",
        "La distinción honesta que da nombre al módulo: la sesión es identidad, no autorización. Que el cliente tenga un access_token válido prueba QUIÉN es (auth.uid() devolverá su uuid), pero NO le da permiso para nada por sí mismo. El permiso lo decide la RLS en cada query. Esto es liberador: no tienes que defender tus datos en el front. Aunque un atacante robe una sesión, solo puede hacer lo que la RLS permite a ESE usuario — ver sus propias filas, no las de otros. La sesión robada de un usuario normal no da acceso de admin, porque is_admin() revalida contra la fila de profiles, no contra una claim que el cliente pueda manipular.",
        "Manejo de estado de sesión en la app. El SDK expone `supabase.auth.getSession()` (lee la sesión actual, posiblemente del localStorage) y `supabase.auth.onAuthStateChange((event, session) => ...)` (te notifica de SIGNED_IN, SIGNED_OUT, TOKEN_REFRESHED). En una SSG como MOONKEY no tienes server-side rendering del estado de auth, así que la página carga primero en estado \"desconocido\" y luego, en cliente, resuelves si hay sesión. Diseña para eso: muestra un estado neutro de carga, no parpadees entre \"invitado\" y \"logueado\". El gate visual (mostrar /cuenta solo si hay sesión) es UX legítima — recuerda DS-02: es UX, la seguridad sigue en la RLS.",
        "El trigger que cierra el círculo. Cuando un usuario se registra por primera vez vía magic-link, Supabase crea una fila en auth.users. Pero tu app necesita una fila correspondiente en profiles. Eso NO lo hace el cliente (no debe poder elegir su propio role ni founder_badge). Lo hace un trigger SECURITY DEFINER en la base de datos: `handle_new_user`, que se dispara `after insert on auth.users` y crea la fila de profiles con valores por defecto seguros (role='monkey', nunca admin). Así la creación del perfil es autoritativa del servidor: el usuario no puede nacer admin porque el trigger, no el cliente, decide los valores iniciales. Este patrón lo desarrollas a fondo en DS-04.",
        "Logout y expiración, hechos honestamente. `supabase.auth.signOut()` borra los tokens del localStorage y revoca el refresh_token en el servidor. Importante: si solo borras el localStorage a mano sin llamar signOut, el refresh_token sigue válido en el servidor — hazlo siempre por el SDK. Sobre expiración: no prometas \"sesión para siempre\". El access_token caduca en una hora y el SDK lo refresca con el refresh_token de forma transparente; si el refresh_token se revoca o caduca, el usuario vuelve a login. Comunica esto al usuario con honestidad en lugar de fingir persistencia eterna.",
        "Configura las URLs de redirección en el panel de Supabase (Auth > URL Configuration). El magic-link redirige a una URL que DEBE estar en la allowlist, o Supabase rechaza el canje — esto previene que un atacante haga que el link redirija a un dominio que controle. En MOONKEY, las redirect URLs incluyen el dominio de producción (moonkeylab.pages.dev) y localhost para desarrollo, y nada más. Una allowlist de redirección laxa es un vector de phishing real."
      ],
      "ejercicio": "En MOONKEY (o un clon local apuntando a una branch de Supabase) implementa el flujo completo: una página de login que llama signInWithOtp con el email del usuario y muestra \"revisa tu correo\"; el manejo del canje al volver del enlace; y una página /cuenta que usa getSession + onAuthStateChange para mostrar el email del usuario logueado o redirigir a login si no hay sesión. Verifica tres cosas: (1) tras el clic en el magic-link existe una fila en profiles creada por el trigger handle_new_user con role='monkey'; (2) signOut borra la sesión y revoca el refresh token; (3) configura las redirect URLs en el panel y confirma que un redirect a un dominio NO listado es rechazado.",
      "entregable": "Un flujo de login por magic-link funcionando contra una branch de Supabase, con captura de: la fila profiles autocreada por el trigger (role='monkey'), el estado de sesión leído en /cuenta, y la pantalla de Auth > URL Configuration mostrando la allowlist de redirect URLs.",
      "insight": "La sesión prueba quién eres, no qué puedes hacer. Si tu seguridad se rompe cuando alguien roba una sesión, es que estabas confiando en el cliente para autorizar — y la autorización siempre tiene que vivir en la RLS, donde una sesión robada solo abre lo que ESE usuario ya podía ver.",
      "errores": [
        "Tratar el token en localStorage como secreto seguro: es legible por cualquier JS de la página, así que tu defensa real es no tener XSS, no esconder el token.",
        "Confundir tener sesión con tener permiso: la sesión da identidad (auth.uid()), pero cada acceso lo sigue decidiendo la RLS — nunca autorices en el front.",
        "Crear la fila de profiles desde el cliente en vez de con el trigger handle_new_user: dejarías que el usuario eligiera su propio role y abriría auto-escalación a admin.",
        "Borrar el localStorage a mano en vez de llamar signOut: el refresh_token sigue vivo en el servidor y la sesión puede reanudarse.",
        "Dejar la allowlist de redirect URLs abierta o con comodines: convierte el magic-link en un vector de phishing que redirige a un dominio del atacante."
      ]
    },
    "DS-04": {
      "objetivo": "Escribir funciones SECURITY DEFINER, RPCs y triggers que ejecuten lógica privilegiada de forma controlada, sin abrir agujeros de escalación de privilegios.",
      "porque": "Hay operaciones que la RLS por sí sola no puede expresar: comprobar si alguien es admin (la propia comprobación necesita leer profiles, lo que crearía recursión), ascender el rango de un operador revalidando reglas, o impedir que un usuario se ponga a sí mismo el founder_badge. SECURITY DEFINER es la herramienta — y es exactamente donde, mal usada, abres la puerta trasera que toda tu RLS intentaba cerrar.",
      "leccion": [
        "Qué significa SECURITY DEFINER. Una función normal en Postgres corre con los permisos de quien la LLAMA (SECURITY INVOKER, el default). Una función SECURITY DEFINER corre con los permisos de quien la CREÓ (típicamente un rol con privilegios, dueño de las tablas). Esto le permite hacer cosas que el llamante no podría hacer directamente — por ejemplo, leer profiles para comprobar un rol, aunque la RLS le negaría al usuario ese SELECT. Es potente y es peligroso: una SECURITY DEFINER es un pequeño trozo de código que corre por encima de la RLS. Cada una es una excepción a tu modelo de seguridad, así que cada una debe ser auditada como tal.",
        "El caso is_admin(). Necesitas saber si el usuario actual es admin para usarlo en policies (`using (... or is_admin())`). Pero si la policy de SELECT de profiles depende de leer profiles para saber el rol, tienes recursión infinita: para leer tu fila necesitas saber si eres admin, para saberlo lees profiles, lo que dispara la policy otra vez. La solución es is_admin() como SECURITY DEFINER: corre con privilegios del dueño, lee profiles SIN pasar por RLS, devuelve un booleano. Crucialmente, NO devuelve datos sensibles — solo true/false sobre el llamante (`select role = 'admin' from profiles where id = auth.uid()`). Una SECURITY DEFINER segura expone la mínima información: una decisión, no un dataset.",
        "Blindar el search_path — esta es la vulnerabilidad clásica y la que los advisors marcan sin piedad. Una SECURITY DEFINER que no fija su search_path es explotable: un atacante crea una tabla o función con el mismo nombre que una que tu función usa, en un esquema que esté antes en el search_path, y tu función privilegiada ejecuta el código del atacante con permisos de dueño. La defensa es obligatoria: `create function is_admin() ... security definer set search_path = '' as $$ ... $$;` (o `set search_path = pg_catalog, public` cualificando explícitamente). Con search_path vacío, referencias toda tabla con su esquema completo: `public.profiles`, no `profiles`. Sin esto, tu función de seguridad ES el agujero.",
        "RPCs: lógica de negocio invocable desde el cliente. Un RPC en Supabase es una función Postgres expuesta vía `supabase.rpc('nombre', args)`. En MOONKEY los RPCs reales son update_operator_rank (asciende el rango revalidando), my_referral_stats (devuelve estadísticas de referidos del usuario) e is_admin. El patrón de oro: el RPC NO confía en los argumentos del cliente para la identidad. update_operator_rank no recibe \"qué usuario ascender\" como parámetro libre — usa auth.uid() internamente. Si recibiera un user_id como argumento, un atacante llamaría rpc('update_operator_rank', { user_id: 'el de otro' }). La identidad SIEMPRE sale de auth.uid() dentro de la función, nunca de un parámetro que el cliente controle.",
        "El RPC revalida, no obedece. update_operator_rank no es \"pon mi rango a X porque lo pido\". Revalida las reglas: ¿completó el usuario las pruebas requeridas para ese rango? El comentario del modelo de seguridad de MOONKEY es explícito — \"Rangos solo vía update_operator_rank (revalida rol)\". El cliente no puede saltar de monkey a admin pidiéndolo; el RPC comprueba las condiciones reales en la DB y solo entonces escribe. Esta es la diferencia entre un RPC que es una API de negocio (verifica invariantes) y uno que es un agujero (escribe lo que le digan). Otorga EXECUTE solo a `authenticated`, nunca a anon: `grant execute on function update_operator_rank to authenticated`.",
        "Triggers: invariantes que se aplican pase lo que pase. Algunos invariantes no pueden depender de que la app los respete. \"Un no-admin nunca puede cambiar su propio role ni su founder_badge\" es uno: si dependiera de la app, cualquier UPDATE directo vía anon key lo saltaría. La solución de MOONKEY es el trigger guard_privileged_profile_columns, que se dispara `before update on profiles` y, si el llamante no es admin y está intentando cambiar role o founder_badge, lanza una excepción que aborta la transacción. Combinado con handle_new_user (after insert on auth.users, crea profiles con role='monkey'), el resultado es que un usuario NACE como monkey y NO PUEDE auto-promoverse — ni por la app, ni por un UPDATE crudo con la anon key. El trigger es la red bajo la RLS.",
        "La disciplina de auditoría. Cada SECURITY DEFINER que escribas: ¿tiene search_path fijado? ¿devuelve la mínima información posible? ¿deriva la identidad de auth.uid() y no de un parámetro? ¿está su EXECUTE restringido al rol correcto? Tras crear o cambiar cualquiera de estas funciones, corre get_advisors (DS-06): el advisor de seguridad marca SECURITY DEFINER sin search_path y funciones con permisos laxos. Una SECURITY DEFINER es código privilegiado; trátala con la paranoia que merece código que corre por encima de tu propia seguridad."
      ],
      "ejercicio": "En una branch de moonkey-lab: (1) Escribe is_admin() como SECURITY DEFINER con `set search_path = ''`, que lea public.profiles y devuelva un booleano sobre auth.uid(). (2) Escribe un trigger before-update en profiles que aborte si un no-admin intenta modificar role o founder_badge, e impleméntalo derivando admin de is_admin(). (3) Prueba el abuso: con una sesión de usuario normal (impersonado), intenta `update profiles set role='admin' where id=auth.uid()` y verifica que el trigger lanza excepción. (4) Corre get_advisors(type='security') y confirma que no aparece ningún warning de search_path mutable sobre tus funciones.",
      "entregable": "Migración con is_admin() (SECURITY DEFINER, search_path fijado) y el trigger guard sobre profiles, más evidencia de: el UPDATE de auto-escalación rechazado por el trigger, y un get_advisors de seguridad limpio (sin warnings de search_path).",
      "insight": "SECURITY DEFINER es la única parte de tu sistema que corre por encima de la RLS, así que es el único sitio donde un descuido escala a brecha total. La regla mínima no negociable: search_path fijado, identidad desde auth.uid() nunca desde parámetros, y permisos de EXECUTE restringidos — porque aquí no hay segunda red.",
      "errores": [
        "Crear una SECURITY DEFINER sin `set search_path`: deja que un atacante secuestre nombres de tabla/función y ejecute su código con permisos de dueño — los advisors lo marcan por algo.",
        "Pasar el user_id como argumento del RPC en vez de usar auth.uid() dentro: el cliente llamaría el RPC con el id de otro usuario y operaría en su nombre.",
        "Hacer que update_operator_rank obedezca el rango pedido en vez de revalidar las condiciones: convierte el ascenso en auto-escalación a un clic.",
        "Confiar en la app para impedir que un usuario cambie su role: un UPDATE directo con la anon key lo saltaría; el invariante tiene que vivir en un trigger.",
        "Otorgar EXECUTE de los RPCs a anon o a public en vez de solo a authenticated: expones lógica de negocio privilegiada a peticiones sin sesión."
      ]
    },
    "DS-05": {
      "objetivo": "Diseñar una sincronización local↔nube donde el estado que vive en localStorage sube a Postgres sin perder datos ni crear duplicados, resolviendo conflictos de forma determinista.",
      "porque": "Espejo arranca con su estado en localStorage (su seam Store está pensado para pasar de localStorage a Supabase sin reescribir la app) y MOONKEY persiste el progreso del operador que primero existe en el navegador y luego debe subir a la nube cuando el usuario hace login. Si la sync es ingenua, un usuario que avanzó offline y luego se loguea pierde su progreso, o lo duplica, o sobreescribe lo que tenía en otro dispositivo.",
      "leccion": [
        "El problema real no es \"copiar datos\". Es reconciliar dos fuentes de verdad que evolucionaron por separado: el localStorage de este navegador y la fila en Postgres (que pudo cambiar desde otro dispositivo). Antes de escribir código, decide la política de conflicto explícitamente, porque \"lo que pase\" es la receta de la pérdida de datos. Las opciones honestas: last-write-wins (gana el timestamp más reciente, simple pero puede perder ediciones concurrentes), merge por campo (combinas campo a campo según reglas), o append-only (nunca sobreescribes, solo añades, y derivas el estado). Para progreso de aprendizaje —que es monótono, solo avanza— la mejor política suele ser \"el máximo gana\": si local dice módulo 3 completado y nube dice módulo 5, el resultado es 5; nunca retrocedes.",
        "El localStorage como capa, no como verdad. El patrón del seam Store de Espejo es la abstracción correcta: tu app no llama a localStorage ni a Supabase directamente, llama a un Store con una interfaz (get, set, list). Hay una implementación LocalStore (localStorage) y una SupabaseStore (Postgres). La app no sabe cuál usa. Esto convierte \"pasar a la nube\" de una reescritura a un cambio de implementación detrás de la misma interfaz. La sync, entonces, es una operación entre dos Stores: leer todo de LocalStore, reconciliar con SupabaseStore, escribir el resultado en ambos. Construye el seam ANTES de necesitar la nube; es barato al principio y carísimo de retrofitear.",
        "Idempotencia es la propiedad que te salva. La sync se va a interrumpir: el usuario cierra la pestaña a mitad, la red cae, el SDK reintenta. Si tu sync no es idempotente, una segunda ejecución crea duplicados. La herramienta es el upsert con clave natural: `insert into progress (user_id, module_code, completed_at) values (...) on conflict (user_id, module_code) do update set completed_at = greatest(progress.completed_at, excluded.completed_at)`. Esa unique (user_id, module_code) de DS-01 es justo lo que hace el upsert posible. Ejecuta la sync dos veces seguidas: si el estado final es idéntico, es idempotente. Si la segunda vez duplica filas o cambia algo, tienes un bug que en producción se manifiesta como datos corruptos a medianoche.",
        "El momento crítico: el primer login tras trabajar como invitado. El usuario avanzó en localStorage sin sesión, luego hace magic-link (DS-03) y obtiene un auth.uid(). Ahora hay que adoptar el estado anónimo bajo su identidad. El flujo seguro: al disparar onAuthStateChange con SIGNED_IN, lees el progreso de LocalStore, lo subes con upsert ligando user_id = auth.uid(), y solo entonces marcas el local como sincronizado. NO borres el local hasta confirmar que la nube lo recibió (la confirmación es la respuesta sin error del upsert). Si borras antes y la subida falla, perdiste el dato. Orden: subir, confirmar, marcar sincronizado, opcionalmente limpiar.",
        "La RLS sigue mandando durante la sync. Cuando subes el progreso con la sesión del usuario, el upsert va con su JWT, así que la policy `with check (user_id = auth.uid())` aplica: no puedes subir progreso a nombre de otro, aunque el localStorage dijera otra cosa. Esto es bueno — la sync no es una puerta trasera a la seguridad. Significa que debes ligar user_id a auth.uid() en el momento de subir, no usar un user_id que arrastrabas del estado anónimo (que no tenía identidad real). La sync respeta el modelo: el servidor sigue siendo la autoridad sobre de quién es cada fila.",
        "Conflictos entre dispositivos, el caso que la gente olvida. Usuario avanza en el móvil (sube a nube), luego abre el portátil que tenía estado local viejo. Sin cuidado, el portátil sobreescribe la nube con datos atrasados. La defensa: la reconciliación NO es \"local pisa nube\", es \"reconciliar ambos según la política\". Para progreso monótono, traes la nube, haces el merge \"máximo gana\" con el local, y escribes el resultado a ambos. Así el portátil aprende lo que hizo el móvil en vez de borrarlo. Para datos no monótonos necesitas timestamps por campo (updated_at) y last-write-wins por campo, lo que exige guardar esos timestamps desde el principio — otra razón para los timestamptz de DS-01.",
        "Estados de la sync, visibles y honestos. Modela explícitamente: synced (local == nube), pending (hay cambios locales sin subir), syncing (en curso), error (falló, reintentar). No mientas al usuario con un check verde si la subida falló. Un indicador honesto de \"cambios sin guardar\" evita que el usuario cierre la pestaña creyendo que estaba a salvo. La sync silenciosa que falla en silencio es peor que no tener sync: el usuario confía y pierde datos sin saberlo."
      ],
      "ejercicio": "Sobre la tabla progress (user_id, module_code, completed_at, con unique(user_id, module_code)): implementa un seam Store con dos backends, LocalStore (localStorage) y SupabaseStore. Escribe una función sync() que: lea el progreso local, lo reconcilie con el de la nube usando 'máximo completed_at gana' vía upsert con `on conflict do update set completed_at = greatest(...)`, ligando user_id = auth.uid(). Prueba tres escenarios: (1) idempotencia — corre sync() dos veces y verifica estado final idéntico, cero duplicados; (2) primer login — avanza como invitado, loguéate, y confirma que el progreso anónimo aparece bajo tu user_id en Postgres; (3) dos dispositivos — simula nube con módulo 5 y local con módulo 3, corre sync, y verifica que el resultado es 5 en ambos lados (no retrocede).",
      "entregable": "Un módulo Store con interfaz común y dos implementaciones (LocalStore/SupabaseStore) más una función sync() idempotente basada en upsert, con un log de los tres escenarios: doble ejecución sin duplicados, adopción del estado anónimo al login, y reconciliación máximo-gana entre dos dispositivos sin pérdida.",
      "insight": "La sync no es copiar datos, es reconciliar dos fuentes de verdad que divergieron — y la única forma de no perder nada es elegir la política de conflicto explícitamente y hacer la operación idempotente con un upsert sobre clave natural. Si no puedes correr tu sync dos veces seguidas con el mismo resultado, no tienes sync, tienes una bomba de tiempo.",
      "errores": [
        "Borrar el localStorage antes de confirmar que la subida a la nube tuvo éxito: si el upsert falla, el dato se pierde para siempre.",
        "Sync no idempotente sin upsert sobre clave natural: una ejecución interrumpida y reintentada duplica filas que aparecen como corrupción a deshoras.",
        "Dejar que el dispositivo con estado viejo pise la nube ('local gana') en vez de reconciliar: el portátil borra lo que el móvil avanzó.",
        "Arrastrar el user_id del estado anónimo en vez de ligarlo a auth.uid() al subir: la policy WITH CHECK lo rechazará, o peor, intentarás escribir bajo una identidad que no es la real.",
        "Mostrar un check verde de 'sincronizado' cuando la subida falló: el usuario confía, cierra la pestaña y pierde el trabajo sin saberlo."
      ]
    },
    "DS-06": {
      "objetivo": "Operar la base de datos con cambios versionados mediante migraciones y usar los advisors de Supabase como un linter de seguridad continuo que te avisa de tablas sin RLS, funciones sin search_path y otros agujeros antes de que lleguen a producción.",
      "porque": "MOONKEY comparte la base de datos con XHUB IRON: un cambio descuidado puede pisar tablas de otro proyecto o dejar una nueva tabla sin RLS abierta a la anon key. Sin migraciones versionadas no hay forma de saber qué cambió ni de revertirlo; sin los advisors, descubres el agujero de seguridad cuando alguien ya lo explotó. Esta es la disciplina que mantiene honesto todo lo anterior.",
      "leccion": [
        "Migraciones: la base de datos como código versionado. Cada cambio de esquema —una tabla, una columna, una policy, una función— es un archivo .sql con timestamp en supabase/migrations/, aplicado con apply_migration, nunca con execute_sql a mano. La diferencia es la misma que entre commitear y editar archivos en producción por SSH: una te da historial, revisión y rollback; la otra te da amnesia. El nombre del archivo (`<timestamp>_create_proofs.sql`, `<timestamp>_add_proofs_rls.sql`) cuenta la historia del esquema. Cuando dentro de seis meses te preguntes por qué una columna existe, la respuesta está en la migración que la introdujo, con su nombre y su fecha — no en tu memoria ni en la de nadie.",
        "execute_sql es para LEER, apply_migration es para CAMBIAR. Esta regla operativa evita el error más común. Usa execute_sql para inspeccionar (select, explain, comprobar el estado), para impersonar y verificar RLS (DS-02), para exploración. En el momento en que el SQL altera el esquema o las policies de forma que quieres que persista, va en una migración. Un cambio de seguridad aplicado con execute_sql que funciona pero no está versionado es deuda: nadie sabe que existe, nadie puede revisarlo, y al recrear el proyecto desaparece.",
        "Branches de Supabase para no romper producción. Antes de aplicar una migración con impacto, créala en una branch (create_branch), pruébala allí —incluida la verificación por impersonación de DS-02 y el get_advisors de DS-04— y solo entonces hazle merge a producción (merge_branch). La branch tiene su propia base de datos efímera; rompes lo que quieras sin tocar a usuarios reales. Esto es especialmente crítico en MOONKEY porque la DB es compartida: una migración que toca por error una tabla iron_* o world_* de XHUB se prueba y se descarta en la branch, no en la base viva que da servicio a dos proyectos.",
        "Los advisors son tu linter de seguridad. get_advisors(type='security') corre un conjunto de comprobaciones que detectan exactamente los agujeros que estos módulos enseñan a evitar: tablas con RLS deshabilitada (DS-02), funciones SECURITY DEFINER con search_path mutable (DS-04), policies que exponen datos, columnas sin protección. get_advisors(type='performance') marca lo otro: foreign keys sin índice, índices duplicados, queries sin cubrir. La disciplina no negociable del CLAUDE.md de MOONKEY: 'Cambios de RLS/seguridad: aplicar como migración versionada + get_advisors después'. Cada vez que tocas seguridad, el advisor es el cierre del bucle — no asumes que está bien, lo verificas con la herramienta.",
        "Cómo leer un advisor y actuar. Un warning del advisor no es ruido para silenciar; es una vulnerabilidad concreta con remedio concreto. 'RLS disabled on public.proofs' significa que cualquiera con la anon key lee la tabla — el remedio es enable row level security + policies, en una migración. 'Function public.is_admin has a role mutable search_path' significa que la función es secuestrable — el remedio es `alter function ... set search_path = ''`, en una migración. La rutina madura: tocas algo → migración → get_advisors → si hay warning, otra migración que lo cierra → get_advisors limpio. No hay 'lo arreglo luego' en advisors de seguridad; luego es después de la brecha.",
        "Aislamiento en la DB compartida, el riesgo específico de MOONKEY. La base aloja tablas de MOONKEY (profiles, progress, feedback, leads, proofs) y de XHUB IRON (iron_*, world_*, focus_*, daily_focus_history). Tu disciplina de migraciones debe respetar esa frontera: una migración de MOONKEY NUNCA debe alterar, ni por descuido de un DROP o un ALTER demasiado amplio, una tabla del otro proyecto. Antes de aplicar, lee el diff de la migración como un adversario: ¿toca solo las tablas que dije? El advisor y la revisión del SQL son las dos redes. En una DB compartida, un cambio mal acotado no es un bug tuyo, es un incidente de otro proyecto.",
        "Auditoría como hábito, no como evento. La auditoría no es una cosa que haces antes de un launch; es el estado por defecto de operar datos en serio. list_migrations te da el historial completo de cómo llegó el esquema a donde está. get_logs te muestra qué está fallando en tiempo real. get_advisors es el chequeo de salud que corres tras cada cambio y periódicamente aunque no cambies nada (porque Supabase añade nuevas comprobaciones y porque el contexto cambia). El operador que trata la base de datos como un sistema vivo que se audita continuamente es el que no tiene la llamada de las 3am — porque vio el warning en la branch, una semana antes, con get_advisors."
      ],
      "ejercicio": "Toma la migración de RLS de proofs que escribiste en DS-02 pero esta vez con disciplina completa: (1) crea una branch de moonkey-lab; (2) aplica en ella, como migraciones versionadas separadas y con nombres descriptivos, la creación de la tabla y sus policies; (3) corre get_advisors(type='security') ANTES de las policies y confirma que aparece el warning 'RLS disabled' sobre proofs; (4) aplica las policies y vuelve a correr get_advisors, confirmando que el warning desaparece; (5) introduce a propósito una función SECURITY DEFINER sin search_path, comprueba que el advisor la marca, arréglala con set search_path='' en otra migración, y confirma advisor limpio; (6) haz merge_branch a producción solo con el advisor en verde. Documenta la lista de migraciones final con list_migrations.",
      "entregable": "Una branch con migraciones versionadas y nombradas para tabla+policies+función, más una secuencia de salidas de get_advisors que demuestra el ciclo cerrar-warning: 'RLS disabled' presente → ausente tras policies, 'mutable search_path' presente → ausente tras el fix, y un get_advisors final limpio antes del merge a producción.",
      "insight": "Los advisors convierten tu modelo de seguridad de algo que crees haber hecho bien en algo que la herramienta confirma que está bien. La regla del operador serio: ningún cambio de seguridad se da por terminado hasta que get_advisors está limpio — porque el coste de un warning ignorado no es un warning, es una brecha que descubres por las malas.",
      "errores": [
        "Aplicar cambios de esquema o policies con execute_sql en vez de apply_migration: pierdes historial, revisión y rollback, y el cambio desaparece al recrear el proyecto.",
        "Tocar la base de producción compartida directamente en vez de probar en una branch: un ALTER o DROP demasiado amplio se convierte en un incidente para XHUB IRON.",
        "Saltarse get_advisors tras un cambio de seguridad: dejas viva justo la tabla sin RLS o la función sin search_path que el advisor habría marcado en segundos.",
        "Tratar un warning del advisor como ruido a silenciar en vez de una vulnerabilidad con remedio concreto: 'lo arreglo luego' en seguridad es 'lo arreglo después de la brecha'.",
        "No leer el diff de la migración como adversario antes de aplicar en una DB compartida: un cambio mal acotado pisa tablas iron_*/world_* de otro proyecto sin que te enteres."
      ]
    }
  },
  "builders": {
    "BD-01": {
      "objetivo": "Montar y comprender un sitio Astro que genera HTML estático por ruta y locale, y un proyecto Vite SPA, sabiendo exactamente qué JS llega al navegador y por qué carga rápido.",
      "porque": "El 90% de la lentitud de la web moderna es JavaScript que no hacía falta enviar. Astro y Vite te dan velocidad por defecto, pero solo si entiendes dónde se ejecuta cada cosa: build-time vs runtime. Confundirlos es la causa raíz de sitios estáticos que de repente pesan 400KB de JS.",
      "leccion": [
        "Empieza por la distinción que lo gobierna todo: en Astro el código de un componente .astro se ejecuta en BUILD (en tu máquina o en CI), no en el navegador. El resultado es HTML plano. MOONKEY es exactamente esto: `astro.config.mjs` declara `i18n` con `fallbackType: 'rewrite'` y NO usa SSR. Cuando corres `npm run build`, Astro recorre `src/pages/`, ejecuta cada .astro una vez por locale y escribe HTML a `dist/`. No hay servidor en producción: Cloudflare Pages sirve archivos. Eso es lo que hace que `moonkeylab.pages.dev` cargue en milisegundos.",
        "Crea el esqueleto desde cero para verlo sin magia: `npm create astro@latest mi-sitio -- --template minimal --no-install`, luego `cd mi-sitio && npm install && npm run dev`. Abre `http://localhost:4321`. Edita `src/pages/index.astro` y mira que un `const hoy = new Date()` impreso con `{hoy}` se congela en el momento del build cuando haces `npm run build`. Ese es el aha-moment: el JS del frontmatter (entre los `---`) corre una vez, no en cada visita.",
        "Ahora el control fino: las islas. Por defecto un componente de framework (React/Svelte) en Astro se renderiza a HTML estático SIN su JS. Solo si le pones una directiva `client:*` se hidrata. `client:load` hidrata al cargar, `client:visible` cuando entra en viewport, `client:idle` cuando el hilo está libre. Regla de oficio: empieza SIN directiva. Solo añade `client:visible` cuando el componente NECESITA interactividad real. En MOONKEY el `IntersectionObserver` de reveal vive en `Layout.astro` como un `<script>` global, no como una isla por página — eso es la decisión correcta: un script de 15 líneas en vez de hidratar un framework entero.",
        "Vite es el motor que está debajo de Astro y también el de Espejo (que es una SPA React pura: `vite.config.ts`, `@vitejs/plugin-react`). La diferencia: Espejo SÍ envía React al navegador porque necesita estado interactivo en vivo (lecturas de cámara con MediaPipe, navegación con react-router). Esa es la línea divisoria real del oficio: ¿el contenido es mayormente lectura (web de XNLAB, MOONKEY) o es una app con estado denso (Espejo)? Lo primero pide Astro/SSG; lo segundo, Vite+SPA. No mezcles religiones: no metas una SPA entera dentro de Astro cuando el 95% es contenido.",
        "Mide, no opines. Corre `npm run build` y mira el tamaño de `dist/`. En MOONKEY un build de página de contenido debería tener ~0KB de JS de página (solo el script de reveal compartido). Abre DevTools → Network, recarga con caché vacía y filtra por JS: si ves un bundle de framework en una página que es solo texto, has metido una directiva `client:*` de más o un import que arrastra una librería pesada al cliente. El comando `npx astro build --verbose` te lista qué se prerenderiza.",
        "Failure mode clásico: importar una librería de fecha/markdown/iconos en el frontmatter creyendo que es build-only, pero usarla dentro de un `<script>` cliente o pasarla a una isla — y de pronto va al bundle. Otra: olvidar que `import.meta.env` en Astro distingue `PUBLIC_*` (va al cliente) del resto (se queda en build). Si una variable que creías privada aparece en `dist/`, la prefijaste mal. Y la trampa de Vite: cualquier cosa bajo `public/` se copia verbatim sin procesar — no pongas ahí secretos ni esperes que Vite optimice esos assets."
      ],
      "ejercicio": "Crea un sitio Astro mínimo con dos páginas: `/` (solo HTML, cero JS) y `/contador` con un componente React `<Counter client:visible />`. Corre `npm run build`. Abre `dist/` y demuestra con DevTools Network que `/` descarga 0KB de JS y `/contador` solo hidrata cuando haces scroll hasta el botón. Documenta el tamaño exacto de ambos bundles.",
      "entregable": "Carpeta `astro-islas/` con el repo + un `MEDICIONES.md` que liste: tamaño de `dist/`, KB de JS por página (captura de Network), y una frase explicando por qué `/` no envía nada y `/contador` sí.",
      "insight": "La pregunta correcta nunca es '¿qué framework uso?' sino '¿qué JS DEBE existir en el navegador?'. Astro invierte el default de la web: parte de cero JS y te obliga a justificar cada byte de interactividad con una directiva explícita. El día que internalizas que el frontmatter corre en build y no en runtime, dejas de enviar basura.",
      "errores": [
        "Poner `client:load` en todo 'por si acaso' — hidratas el sitio entero y pierdes la ventaja de Astro; empieza sin directiva y añade `client:visible` solo donde haya interacción real.",
        "Creer que el JS del frontmatter (entre `---`) corre en el navegador; corre en build, una sola vez, y su salida queda congelada en el HTML.",
        "Meter una variable secreta en `import.meta.env.PUBLIC_*` — el prefijo PUBLIC la expone en `dist/`; lo no-público se queda en build.",
        "Confundir cuándo usar Astro vs Vite-SPA: contenido de lectura → Astro/SSG; app con estado denso y vivo como Espejo → Vite+React.",
        "Asumir que `public/` se optimiza: se copia verbatim, sin hashing ni minificación, así que no es el sitio para assets que quieres que Vite procese."
      ]
    },
    "BD-02": {
      "objetivo": "Construir un sistema visual coherente con design tokens (CSS custom properties) y componentes reutilizables, en vez de CSS suelto repetido, de forma que un cambio de marca sea una línea y no una cacería.",
      "porque": "El CSS suelto no escala: el día que el cliente pide 'el violeta un poco más oscuro' acabas buscando `#7c3aed` en 40 archivos y te dejas tres. Los tokens convierten ese cambio en una edición de una variable. Es la diferencia entre un producto mantenible y deuda visual.",
      "leccion": [
        "Un design token es una variable con nombre semántico, no un valor crudo repetido. MOONKEY define los suyos en `src/styles/global.css`: `--surface-base: #f8fafc`, `--text-primary: #0f172a`, más la paleta emerald/violet/amber. La regla de oro: los componentes NUNCA usan el hex directo, usan el token. Así `--surface-base` aparece una vez; si cambia la paleta, cambia un sitio. El antipatrón es escribir `color: #0f172a` en cada card — has acoplado 30 archivos a un valor que debería tener nombre.",
        "El caso más instructivo del oficio está en Espejo, porque ahí los tokens son DINÁMICOS por tenant. Mira `src/brand/BrandProvider.tsx`: la función `applyTheme(brand)` hace `root.style.setProperty('--brand', brand.accent)` y deriva `--brand-soft`, `--brand-line`, `--brand-glow` con una función `withAlpha(hex, a)` que convierte el hex de marca en rgba con transparencia. Toda la UI pública de Espejo pinta con `var(--brand)`. Resultado: el mismo código sirve a Luna Rosa (rosa `#e58fb0`) y a cualquier influencer, cambiando UN campo `accent`. Eso es un design system que es también el motor del white-label.",
        "Componente vs clase suelta: un componente encapsula estructura + tokens + comportamiento bajo un nombre. MOONKEY tiene `PromptBlock`, `AnimalIcon`, `MoonkeyLogo`, `FlowDiagram` en `src/components/`. La señal de que necesitas un componente: copias el mismo bloque de markup+clases una tercera vez. Hasta dos veces, duplica; a la tercera, extrae. No abstraigas antes — la abstracción prematura de un componente con 8 props 'por si acaso' es peor que la duplicación honesta.",
        "Con Tailwind hay una trampa específica que MOONKEY documenta explícitamente: NUNCA interpoles clases como `bg-${theme}`. Tailwind hace tree-shaking escaneando el código en busca de strings de clase COMPLETOS en build; una clase construida en runtime no existe en el CSS generado y se renderiza sin estilo. Por eso `galaxy.ts` tiene un objeto `THEME` que mapea cada constelación a sus clases literales (`violet`, `amber`, `emerald`…) ya escritas. La lección: las clases dinámicas se resuelven con un MAPA de strings literales, no con interpolación.",
        "Tipografía como token de sistema: MOONKEY fija `font-display` = Space Grotesk para h1–h3, Inter para body, JetBrains Mono para labels (`.label` = mono uppercase tracking-widest emerald-600). Eso no es decoración suelta: es una decisión codificada una vez que toda la app hereda. Si cada heading eligiera su propia fuente, no tendrías marca, tendrías ruido. El sistema vive en la capa de tokens/utilidades, no en cada `<h1>`.",
        "Cómo verificar coherencia: haz un grep de valores crudos que NO deberían existir fuera del archivo de tokens. `grep -rn '#[0-9a-fA-F]\\{6\\}' src/components/` en MOONKEY debería devolver casi nada — si un hex aparece en un componente, es un token fugado que hay que reemplazar por `var(--token)`. Ese grep es tu linter de coherencia visual hecho a mano, y deberías correrlo antes de cada merge."
      ],
      "ejercicio": "Toma una landing con 3 secciones que use hex repetidos. Extrae TODOS los colores y radios a tokens en un `:root` (`--surface`, `--text`, `--accent`, `--radius`). Luego replica el patrón de Espejo: añade una función `applyTheme(accent)` que cambie `--accent` en runtime y demuestra que con UNA llamada toda la página cambia de marca. Verifica con `grep` que ningún hex sobrevive fuera del bloque de tokens.",
      "entregable": "Carpeta `tokens-tenant/` con `styles/tokens.css` (todas las variables), los componentes usando `var(--token)`, y un botón que llama `applyTheme()` con 3 colores distintos. Más un `grep` log probando 0 hex sueltos en componentes.",
      "insight": "Un design system no es una librería de componentes bonitos: es la indirección que separa 'qué valor' de 'dónde se usa'. El test definitivo es el de Espejo — si puedes cambiar la marca entera de un producto white-label con un solo `setProperty`, tienes un sistema; si tienes que tocar varios archivos, tienes CSS disfrazado.",
      "errores": [
        "Interpolar clases de Tailwind (`bg-${theme}`) — el JIT no las ve en build y se renderizan sin estilo; usa un mapa de strings literales como el `THEME` de galaxy.ts.",
        "Escribir hex crudos en componentes en vez de `var(--token)` — acoplas N archivos a un valor que debería tener un solo punto de cambio.",
        "Abstraer un componente con 8 props 'por si acaso' antes de tener 3 usos reales; la duplicación honesta es mejor que la abstracción prematura.",
        "Dejar que cada heading elija su fuente en vez de fijar la jerarquía tipográfica una vez en la capa de tokens.",
        "Olvidar derivar los estados del color de marca (soft/line/glow): un solo accent sin sus variantes con alpha produce sombras y bordes incoherentes — copia el patrón `withAlpha` de Espejo."
      ]
    },
    "BD-03": {
      "objetivo": "Diseñar un producto multi-tenant white-label real: un solo código base que sirve a muchos clientes con su propia marca, datos y configuración, usando el patrón de costura (seam) de Store que aísla la UI del almacenamiento.",
      "porque": "El white-label es el modelo de negocio que multiplica un build por N clientes sin reescribir nada. Pero solo funciona si el aislamiento entre tenants es real: una fuga de datos de un cliente a otro mata el producto. El seam Store de Espejo es la arquitectura que lo hace posible y a la vez prepara la migración a backend.",
      "leccion": [
        "Multi-tenant significa: una instancia del software, muchos clientes (tenants) que la perciben como suya. White-label añade: cada tenant la ve con SU marca, sin rastro de la tuya. Espejo es el caso canónico — `src/brand/types.ts` lo dice en el comentario: 'cada influencer es un tenant con su marca'. El tenant se identifica por un `slug` en la URL (`/r/<slug>`) y todo —nombre, accent, glyph, voz, qué lecturas se ofrecen, el CTA de upsell— sale de un objeto `Brand`. La app pública entera se pinta a partir de ese objeto.",
        "El corazón arquitectónico es el SEAM de datos. Mira `src/brand/store.ts`: define una `interface Store` con firmas async (`getBrand`, `listBrands`, `saveBrand`, `addSubscriber`, `listSubscribers`) y una implementación `LocalStore` sobre localStorage. El comentario lo deja explícito: 'Hoy: localStorage. Mañana: Supabase (mismas firmas). La UI nunca toca el almacenamiento directamente, solo este interfaz.' Esa interfaz es la costura: la UI depende de la ABSTRACCIÓN, no de la implementación. Cambiar de backend = escribir una nueva clase que implemente `Store`, sin tocar un solo componente.",
        "Cómo el tenant llega a la UI: `BrandProvider.tsx` recibe el `slug`, llama `store.getBrand(slug)`, y si existe hace `applyTheme(brand)` y mete el brand en un Context React (`useBrand()`). Si no existe, marca `notFound`. Patrón limpio: un punto de entrada resuelve el tenant, aplica su tema, y lo provee a todo el árbol. Cualquier componente hace `const brand = useBrand()` y pinta lo suyo. Nadie más toca el storage ni decide el tema.",
        "El aislamiento entre tenants es la línea roja del white-label, y aquí está el riesgo real: `listSubscribers(slug)` filtra por `brandSlug`. En localStorage eso es trivial pero FRÁGIL — todo vive en el mismo navegador, no hay frontera de seguridad real. El momento crítico es la migración a Supabase: ahí el aislamiento DEBE moverse a Postgres RLS (una fila de subscriber solo es visible para su tenant), exactamente la disciplina que MOONKEY aplica con `is_admin()` y políticas self-or-admin. En multi-tenant real, el filtro `WHERE slug = ...` en el cliente NO es seguridad; la seguridad es RLS en la base.",
        "Configuración por tenant sin ramas de código: fíjate que Espejo no tiene `if (tenant === 'lunarosa')` en ningún sitio. La variación es DATOS (`Brand.readings`, `Brand.voice`, `Brand.offer`), no lógica condicional. Esa es la regla del white-label sano: las diferencias entre clientes viven en configuración (datos), nunca en bifurcaciones de código. En cuanto escribes el primer `if (cliente X)` has empezado a forkear el producto y a perder la economía del modelo.",
        "Hay un guardarraíl ético específico en Espejo que no es opcional: la memoria del proyecto marca que NO se construye sifón encubierto de leads ni venta de datos personales/biométricos — solo datos consentidos de primera parte y agregados anónimos. En un producto multi-tenant que maneja datos de los usuarios finales de tus clientes, el modelo de consentimiento (`types.ts` tiene versión del texto de consentimiento aceptado y `Brand.legal.responsible`) es parte de la arquitectura, no un añadido legal posterior. El responsable del tratamiento es el influencer-tenant, y eso debe estar modelado en los datos."
      ],
      "ejercicio": "Construye un mini producto white-label: una interfaz `Store` con `getTenant(slug)`/`listItems(slug)`, una `LocalStore` sobre localStorage con 2 tenants seed (distinto accent, distinto nombre). Un `TenantProvider` que resuelva el slug de la URL, aplique el tema y lo provea por Context. Demuestra que cambiando solo el slug en la URL la app entera cambia de marca, sin un solo `if` por cliente.",
      "entregable": "Carpeta `white-label-seam/` con `store.ts` (interface + LocalStore), `TenantProvider`, y dos rutas `/t/<slug>` que renderizan dos marcas distintas desde el MISMO código. Un `README` que señale dónde está la costura y qué se cambiaría para migrar a backend.",
      "insight": "El seam Store de Espejo es la pieza de ingeniería más valiosa del producto: separa 'qué hace la app' de 'dónde viven los datos' detrás de una interfaz de 5 firmas. Esa indirección es lo que convierte una demo en localStorage en un SaaS multi-tenant — y lo que hace que el filtro por slug del cliente nunca pueda confundirse con seguridad real (esa vive en RLS).",
      "errores": [
        "Filtrar por tenant solo en el cliente (`WHERE slug=...` en JS) y creer que eso aísla datos — en backend el aislamiento DEBE ser RLS en Postgres, no un filtro de aplicación.",
        "Meter `if (cliente === 'X')` en el código: en cuanto bifurcas por cliente has forkeado el producto; la variación va en datos/config, nunca en lógica.",
        "Acoplar la UI directamente a localStorage/Supabase en vez de a la interfaz `Store` — pierdes la costura y la migración se vuelve una reescritura.",
        "Olvidar el modelo de consentimiento y el responsable del tratamiento por tenant: en multi-tenant con datos de usuarios finales, eso es arquitectura, no trámite (límite ético de Espejo: nada de sifón encubierto de datos).",
        "No definir el caso `tenant no encontrado`: sin un `notFound` explícito, un slug inválido rompe la app en vez de mostrar un estado limpio."
      ]
    },
    "BD-04": {
      "objetivo": "Arquitectar internacionalización para 6 idiomas sin duplicar páginas, entendiendo los patrones de traducción, el fallback, y por qué los hrefs deben ser locale-aware.",
      "porque": "i18n hecho a la fuerza (una carpeta de páginas por idioma) multiplica el mantenimiento por 6 y garantiza que las traducciones se desincronicen. La arquitectura correcta tiene UNA plantilla y los idiomas son datos, no copias. Es la diferencia entre escalar a 6 locales o ahogarse en ellos.",
      "leccion": [
        "MOONKEY corre 6 locales — `['es','en','th','fr','it','ch']` (config en `src/i18n/config.ts`). ES vive en la raíz; el resto bajo prefijo (`/en/`, `/th/`, `/fr/`, `/it/`, `/ch/`). La pieza que evita duplicar páginas es Astro i18n con `fallbackType: 'rewrite'` en `astro.config.mjs`: durante el build, Astro REESCRIBE cada página con el contexto de cada locale, así que un único `index.astro` genera 6 HTML traducidos. No hay `pages/en/index.astro`, `pages/fr/index.astro`… hay UNO. Esa es la regla central: una plantilla, N salidas.",
        "El fallback es lo que hace el sistema robusto sin traducirlo todo de golpe: `astro.config.mjs` declara `fallback: { en:'es', th:'es', fr:'es', it:'es', ch:'es' }`, y la función `useTranslations` en `src/i18n/utils.ts` implementa la misma cadena: `dict[key] ?? base[key] ?? key`. Es decir: si falta una clave en francés, cae a español; si tampoco está, devuelve la clave misma (visible, fácil de cazar). Esto te deja LANZAR con traducciones parciales sin romper nada — el francés muestra español donde aún no tradujiste, no una página rota.",
        "MOONKEY usa TRES patrones de i18n y CLAUDE.md avisa de la inconsistencia, lo cual es una lección de oficio en sí: (1) diccionario `src/i18n/ui.ts` con `t('key')` para chrome/nav corto (~240 keys); (2) objetos de contenido inline con `cLocale = (es|th) ? locale : 'en'` que colapsa FR/IT/CH a EN, para contenido pesado como el currículum; (3) páginas `src/pages/en/*.astro` que sobreescriben SOLO en inglés. El patrón 3 es deuda: hay 5 páginas raíz hardcodeadas en español, así que `/th/fr/it/ch` muestran ES en ellas. Lección: elige UN patrón dominante (el diccionario con fallback) y trata los otros como deuda a pagar, no como variedad sana.",
        "El bug más caro de i18n con prefijos es el href crudo. Si escribes `<a href='/cuenta'>` desde una página bajo `/fr/`, mandas al usuario fuera de su idioma. Por eso MOONKEY tiene `localizedPath(pathname, target)` en `utils.ts`: quita el prefijo de locale actual, reconstruye la ruta y le antepone el target (`/fr/cuenta`), dejando ES en raíz. CLAUDE.md lo marca como obligatorio: 'usa `localizedPath('/ruta', locale)`, nunca `/ruta` crudo'. Internaliza esto: en un sitio con prefijos de idioma, CADA enlace pasa por la función de localización, sin excepción.",
        "Detectar el locale activo tiene una sutileza que `utils.ts` resuelve: `resolveLocale(currentLocale, pathname)` prefiere `Astro.currentLocale` sobre la detección por URL, porque durante un rewrite de fallback la URL puede haber perdido el prefijo y solo el contexto de Astro sabe el locale real. Si detectas locale solo por `pathname.split('/')` te equivocas justo en las páginas que usan fallback. Regla: confía en el contexto del framework primero, en la URL como respaldo.",
        "El selector de idioma necesita dos diccionarios separados: `LOCALE_LABELS` (ES, EN, TH…) para el switcher compacto y `LOCALE_NAMES` (Español, ไทย, Italiano…) para el dropdown completo. Y ojo a los casos raros: 'ch' aquí es Deutsch (CH), suizo-alemán, no chino — un comentario en `config.ts` lo aclara. Esa clase de ambigüedad (un código que parece otro idioma) es exactamente lo que documentas en el código para que nadie traduzca al idioma equivocado seis meses después."
      ],
      "ejercicio": "Monta un sitio Astro con 3 locales (es raíz, en/fr con prefijo). Crea UN `index.astro` que use `t('hero.title')` desde un diccionario `ui.ts`. Implementa `useTranslations` con fallback `dict[key] ?? base[key] ?? key` y `localizedPath`. Deja una clave SIN traducir en FR y demuestra que cae a ES (no rompe). Pon un selector de idioma que use `localizedPath` para no sacar al usuario de su locale.",
      "entregable": "Carpeta `i18n-3locales/` con `i18n/config.ts`, `ui.ts`, `utils.ts` (useTranslations + localizedPath), un `index.astro` único que genera 3 HTML, y una captura mostrando FR cayendo a ES en la clave sin traducir.",
      "insight": "i18n bien hecho convierte el idioma en un parámetro de datos sobre UNA plantilla, no en copias de páginas. El fallback en cadena (`dict ?? base ?? key`) es lo que te permite lanzar con traducciones a medias sin páginas rotas — y `localizedPath` en cada href es la diferencia invisible entre un usuario que se queda en su idioma y uno que se cae a la raíz al primer clic.",
      "errores": [
        "Duplicar páginas por idioma (`pages/fr/index.astro`, `pages/en/index.astro`…) en vez de una plantilla con rewrite — multiplicas el mantenimiento por 6 y las traducciones se desincronizan.",
        "Escribir hrefs crudos (`/cuenta`) bajo un prefijo de locale: sacas al usuario de su idioma; todo enlace pasa por `localizedPath`.",
        "Detectar el locale solo por la URL: durante un rewrite de fallback la URL pierde el prefijo; prefiere `Astro.currentLocale` (`resolveLocale`).",
        "Mezclar tres patrones de i18n sin uno dominante — MOONKEY ya arrastra deuda por eso (5 páginas hardcodeadas en ES que se muestran a th/fr/it/ch).",
        "Asumir el significado de un código de locale: 'ch' aquí es suizo-alemán, no chino; documenta los ambiguos en el config o alguien traducirá mal."
      ]
    },
    "BD-05": {
      "objetivo": "Llevar un sitio de localhost a una URL real en Cloudflare Pages con dominio propio y variables de entorno gestionadas de forma segura, distinguiendo qué env va al cliente y qué jamás.",
      "porque": "Un proyecto que no está desplegado no existe para nadie. Y el deploy es donde más secretos se filtran: la línea entre una variable pública y una privada es invisible hasta que tu service_role key aparece en el bundle de producción. Saber desplegar bien es la mitad de shippear.",
      "leccion": [
        "MOONKEY se despliega en Cloudflare Pages (`moonkeylab.pages.dev`) desde el repo `garciafradepablo-pixel/xop`. El flujo base: conectas el repo de GitHub a Cloudflare Pages, defines el build command (`npm run build`) y el output directory (`dist/` en Astro). Cada push a main dispara un build en CI y publica. No subes archivos a mano: el deploy es una consecuencia de git push. Eso te da reproducibilidad — lo que está en producción es exactamente lo que está en el commit.",
        "Para una SSG pura como MOONKEY, Cloudflare solo sirve los HTML estáticos del build; no hay runtime de servidor. Esto es clave para el modelo de seguridad (lo verás en Ops): como no hay handler server-side, el único env que importa en producción es el que se inyectó EN BUILD. Si tu app necesita lógica server (no es el caso de MOONKEY ni XNLAB de contenido), ahí entran Cloudflare Functions/Workers — pero no las metas si no las necesitas.",
        "La frontera de los secretos es LA lección de este módulo. En Astro/Vite, las variables `PUBLIC_*` (Astro) o `VITE_*` (Vite puro como Espejo) se inyectan en el bundle del cliente — son VISIBLES para cualquiera que abra DevTools. Todo lo demás se queda en build. MOONKEY pone la Supabase anon key como pública a propósito: CLAUDE.md dice 'solo anon key' en `src/lib/supabase.ts`, porque la anon key está DISEÑADA para ser pública (su poder lo limita RLS, no el secreto). La regla absoluta: la `service_role` key NUNCA lleva prefijo público, NUNCA va a un sitio estático. Si la pones, está en el bundle, y es game over.",
        "En Cloudflare configuras las env vars en el dashboard (Settings → Environment variables), separando Production de Preview. Las que tu build necesita exponer al cliente llevan el prefijo correcto; las que solo usa el proceso de build (tokens de API de terceros para generar contenido) no lo llevan y no acaban en `dist/`. Verificación obligatoria post-deploy: descarga tu JS de producción y haz `grep -ri 'service_role\\|secret\\|sk_'` sobre `dist/`. Si algo aparece, tienes una fuga que rotar inmediatamente.",
        "Dominio propio: en Cloudflare Pages, Custom domains → añades tu dominio, Cloudflare crea los registros DNS (si el dominio está en Cloudflare es automático; si no, añades un CNAME al `*.pages.dev`). HTTPS es automático. El `.pages.dev` sigue funcionando como URL de respaldo. Para previews, cada PR/rama genera su propia URL efímera — úsalas para revisar cambios antes de mergear a main, sin tocar producción.",
        "Failure modes de deploy reales: (1) el build pasa en local pero falla en CI porque una dependencia estaba en `node_modules` local pero no en `package.json` — corre `npm ci` (no `install`) en limpio para reproducir CI. (2) Variables que existen en local (`.env`) pero no configuraste en el dashboard → build pasa pero la app falla en runtime con undefined. (3) El warning cosmético de Astro sobre `Astro.request.headers` en builds estáticos con i18n rewrite — `astro.config.mjs` lo documenta como inofensivo, el build pasa. No persigas warnings que la config ya marcó como esperados."
      ],
      "ejercicio": "Despliega un sitio Astro a Cloudflare Pages desde un repo de GitHub: configura build command `npm run build`, output `dist/`, y una variable `PUBLIC_API_BASE` y otra NO pública `BUILD_TOKEN`. Tras el deploy, descarga el JS de producción y demuestra con `grep` que `PUBLIC_API_BASE` aparece en el bundle y `BUILD_TOKEN` NO. Añade un dominio (o subdominio) y verifica HTTPS.",
      "entregable": "Una URL pública viva + un `DEPLOY.md` con: el build config, la tabla de qué variable es pública y por qué, y el log de `grep` probando que el token de build no se filtró al cliente.",
      "insight": "El deploy no es 'subir archivos', es congelar un commit en una URL con CI reproducible. Y el único error irreparable es de secretos: como un sitio estático no tiene servidor, todo lo que necesita en producción se inyecta en build — por eso la anon key (limitada por RLS) puede ser pública y la service_role jamás. El `grep` sobre `dist/` es tu última red antes de filtrar.",
      "errores": [
        "Poner una clave privada (service_role, secret de API) con prefijo `PUBLIC_`/`VITE_` o en un sitio estático — queda en el bundle visible; solo claves diseñadas para ser públicas (anon key limitada por RLS) van al cliente.",
        "Subir el build a mano en vez de conectar el repo: pierdes reproducibilidad y no sabes qué commit está en producción.",
        "Configurar env vars en `.env` local pero olvidarlas en el dashboard de Cloudflare → el build pasa y la app revienta en runtime.",
        "Usar `npm install` para reproducir CI; usa `npm ci` en limpio, que respeta el lockfile y reproduce el fallo real de dependencias.",
        "No verificar con `grep` sobre `dist/` tras el deploy: la fuga de un secreto es silenciosa hasta que alguien la encuentra."
      ]
    },
    "BD-06": {
      "objetivo": "Migrar la capa de datos de localStorage a un backend (Supabase) sin reescribir la aplicación, aprovechando la costura Store para que el cambio sea swap de implementación, no refactor.",
      "porque": "Toda app empieza con datos locales para ir rápido, pero localStorage no comparte entre dispositivos, no persiste de verdad y no tiene seguridad real. El salto a backend es inevitable — y si tu arquitectura no lo previó, ese salto es una reescritura dolorosa. Con la costura correcta, es una tarde.",
      "leccion": [
        "Recupera el seam de BD-03: en Espejo `src/brand/store.ts` define `interface Store` con firmas async y `LocalStore` la implementa sobre localStorage. El comentario es la tesis entera del módulo: 'Hoy: localStorage. Mañana: Supabase (mismas firmas).' Porque la interfaz YA es async (`Promise<Brand | null>`), la UI no nota la diferencia entre leer de memoria o de la red — el `await` ya está ahí. Una API síncrona habría hecho la migración imposible sin tocar cada llamada. Diseñar la costura async desde el día uno es la decisión que paga aquí.",
        "El swap concreto: escribes una clase `SupabaseStore implements Store`. `getBrand(slug)` pasa de leer localStorage a `await supabase.from('brands').select().eq('slug', slug).single()`. `addSubscriber` pasa de `write()` a `.insert()`. Las firmas son IDÉNTICAS. Al final, una línea cambia el mundo: `export const store: Store = new SupabaseStore()` en vez de `new LocalStore()`. Cero componentes tocados. Eso es lo que significa 'cambiar la capa de datos sin reescribir la app': el resto del código depende de `Store`, no de localStorage.",
        "Pero migrar el almacenamiento es la mitad fácil. La mitad seria es la SEGURIDAD, y aquí cambia el modelo entero. En localStorage no hay fronteras: todo vive en el navegador del usuario. En Supabase, como el cliente solo lleva la anon key (MOONKEY: `src/lib/supabase.ts`, 'solo anon key'), la autoridad NO es el cliente — es Postgres RLS. MOONKEY lo formula sin ambigüedad: 'autoridad = RLS, no el cliente'. Cuando Espejo migre los subscribers, el filtro `WHERE brandSlug = slug` que hoy hace en JS DEBE convertirse en una política RLS que garantice que un tenant solo lee SUS subscribers. El filtro de cliente era conveniencia; RLS es seguridad.",
        "Patrón de referencia de RLS en MOONKEY: las tablas `profiles/progress/feedback/proofs` tienen SELECT 'self-or-admin' vía `is_admin()` (función SECURITY DEFINER), verificado por impersonación — un no-admin no puede leer filas ajenas. `proofs` INSERT exige sesión y liga `user_id = auth.uid()` (política `proofs_insert_self`). Columnas sensibles (`role`, `founder_badge`) son inmutables para no-admins vía trigger guard, así que nadie se auto-escala. Esa es la plantilla mental para cualquier migración: cada operación (SELECT/INSERT/UPDATE) necesita una política que se cumpla AUNQUE el atacante controle el cliente.",
        "Higiene de migración de datos: necesitas un esquema (tabla `brands`, tabla `subscribers` con `brand_slug`), aplicado como MIGRACIÓN versionada (no SQL suelto en la consola — MOONKEY: 'cambios de RLS/seguridad: migración versionada + `get_advisors` después'). Luego un script de una sola vez que lea el localStorage existente y haga `insert` masivo al backend, idempotente (que puedas re-correr sin duplicar). Y un periodo de doble escritura opcional si no puedes parar la app: escribes a ambos hasta confirmar paridad, luego cortas localStorage.",
        "Ojo a la frontera ética que arrastra Espejo: la memoria del proyecto prohíbe sifón encubierto de datos personales/biométricos; solo datos consentidos de primera parte y agregados anónimos. Migrar a backend AUMENTA el poder de los datos (ahora persisten, se cruzan, se exportan), así que el modelo de consentimiento que en localStorage era casi teórico se vuelve real y auditable. La migración técnica y la responsabilidad sobre los datos suben juntas — no migres el almacenamiento sin migrar también las garantías de privacidad."
      ],
      "ejercicio": "Toma el `white-label-seam` de BD-03 (interface Store + LocalStore). Escribe una `SupabaseStore implements Store` con las MISMAS firmas contra una tabla real de Supabase. Migra cambiando UNA línea (`new LocalStore()` → `new SupabaseStore()`) sin tocar componentes. Aplica una política RLS que garantice que `listItems(slug)` solo devuelve filas del tenant correcto, y verifícalo impersonando otro usuario.",
      "entregable": "Carpeta `store-migration/` con `LocalStore` y `SupabaseStore` lado a lado, el diff de UNA línea que hace el swap, la migración SQL versionada con la política RLS, y un log probando que un tenant NO puede leer los datos de otro (test de impersonación).",
      "insight": "La costura Store convierte una migración de backend de reescritura a swap de una línea — pero solo si la interfaz fue async desde el principio. Y la parte que de verdad importa no es mover los bytes: es que al pasar a un cliente con anon key, la autoridad se traslada de la app a Postgres RLS. El filtro por slug que tenías en JS no era seguridad; ahora tiene que serlo, en la base.",
      "errores": [
        "Diseñar la interfaz Store síncrona y descubrir en la migración que cada llamada necesita `await` — hazla async (Promise) desde el día uno aunque localStorage no lo necesite.",
        "Creer que migrar el almacenamiento es el trabajo: la parte seria es trasladar el aislamiento del filtro JS de cliente a políticas RLS en Postgres ('autoridad = RLS, no el cliente').",
        "Aplicar el esquema y las políticas como SQL suelto en la consola en vez de migraciones versionadas + `get_advisors` después.",
        "Script de migración de datos no idempotente: si no puedes re-correrlo sin duplicar, una interrupción te deja datos corruptos.",
        "Migrar a backend sin reforzar el consentimiento: persistir y cruzar datos personales eleva el riesgo; en Espejo el límite es datos consentidos de primera parte, nada de sifón encubierto."
      ]
    }
  },
  "signal": {
    "SG-01": {
      "objetivo": "Construir un edge de ingesta read-only que trae datos públicos del mundo a disco sin exponer ni una sola credencial, y que es estructuralmente incapaz de operar o mover dinero.",
      "porque": "Un sistema de research que toca la red por todas partes es un sistema que puede filtrar llaves, ejecutar trades por accidente y contaminar resultados con datos no reproducibles. El edge keyless es la frontera donde el mundo entra sin poder hacer daño.",
      "leccion": [
        "La intuición central de XCAP es brutal y poco intuitiva para quien viene de SaaS: en un sistema de investigación de trading, la red no es una conveniencia, es la superficie de ataque más grande que tienes. Cada endpoint con llave es una credencial que puede filtrarse; cada llamada de escritura es un trade que puede dispararse por un bug. Por eso XCAP colapsa toda su exposición de red a un único punto: un edge de ingesta read-only, keyless y GET-only. El resto del sistema es determinista desde disco. Si entiendes esto, entiendes por qué `binance_public.py` solo pide klines (velas OHLCV históricas) por HTTP GET, sin API key, sin firmar nada, sin tocar jamás un endpoint de cuenta o de orden.",
        "Concretamente, el patrón es: una fuente abstracta (en XCAP, una jerarquía `OHLCSource`) con dos implementaciones. `LocalCSVSource` lee CSVs ya en disco — cero red, el caso por defecto, el que usan TODOS los loops automáticos. Y `HttpOHLCSource` (respaldado por `binance_public.py`) que solo se activa cuando un caller pasa explícitamente `allow_network=True` o invoca el comando `data-fetch`. La asimetría es deliberada: el camino seguro es el default y el camino con red exige una opción explícita en cada llamada. Nunca un flag global ambiental, nunca una variable de entorno que se quede encendida.",
        "El endpoint de Binance que usas es el público de mercado: `GET https://api.binance.com/api/v3/klines?symbol=BTCUSDT&interval=1d&limit=1000`. Devuelve un array de arrays: `[openTime, open, high, low, close, volume, closeTime, ...]`. Fíjate en lo que NO hay aquí: no hay header `X-MBX-APIKEY`, no hay query param de firma HMAC, no hay timestamp firmado. Ese endpoint físicamente no puede leer tu balance ni colocar una orden porque la API de cuenta de Binance vive en otra ruta y exige firma. La seguridad no es una promesa tuya de portarte bien: es una propiedad del endpoint que elegiste.",
        "El segundo pilar es la normalización en el borde. Datos del mundo real llegan sucios: timestamps en milisegundos, números como strings, velas duplicadas, huecos. Tu edge debe normalizar a UN formato canónico antes de que nada downstream los toque. En XCAP los CSVs de `data/real` son lowercase (`open,high,low,close,volume`), timestamps parseados a fechas, y `LocalCSVSource` los lee siempre igual. Esta disciplina es lo que hace reproducible al sistema: el clasificador de régimen (SG-02) y la Market Memory (SG-03) leen de disco un formato estable, no de una respuesta HTTP volátil que cambia entre corridas.",
        "La verificación es el cierre. XCAP tiene un comando `check` (corre con `PYTHONPATH=src python3 -m xcap.control check`) que afirma como invariante que el connector es keyless, read-only, no puede operar y no puede mover dinero. No es un test que pasa una vez: es una aserción que corre en cada `check`, de modo que si alguien añade una llave o un método de escritura, el `check` se pone rojo. Así es como conviertes una intención de diseño en una propiedad que el sistema defiende solo. La regla de oro: la seguridad que depende de que recuerdes algo no es seguridad; la seguridad que un test rojo te obliga a respetar, sí.",
        "Un matiz operativo importante: el dashboard de XCAP bindea solo a `127.0.0.1`, nunca a `0.0.0.0`. El edge de ingesta es la única superficie de salida; la de entrada (el dashboard) no escucha en la red. Y FileVault (AES-256) cifra el disco en reposo. La cadena completa — disco cifrado, ingest keyless read-only, dashboard local-only, Capital Gate cerrado — es lo que permite tener un sistema de trading-research en un portátil sin que sea una bomba de relojería."
      ],
      "ejercicio": "Implementa una fuente de datos con dos backends. Crea `ohlc_source.py` con una clase base `OHLCSource` (método `get(symbol, interval, limit) -> list[Bar]`). Implementa `LocalCSVSource` (lee de `./data/{symbol}.csv`, sin red) y `HttpOHLCSource` (GET a `api.binance.com/api/v3/klines`, sin ninguna API key). El constructor de `HttpOHLCSource` debe recibir `allow_network: bool = False` y lanzar `RuntimeError('network not allowed')` si se llama `get()` con `allow_network=False`. Normaliza ambos a la misma estructura `Bar(time, open, high, low, close, volume)` con floats y fecha parseada. Escribe `check_connector()` que verifique por introspección que la clase HTTP no tiene ningún método ni atributo cuyo nombre contenga 'order', 'trade', 'sign', 'key', 'secret' o 'withdraw', y que falle si lo encuentra.",
      "entregable": "`ohlc_source.py` con `LocalCSVSource` + `HttpOHLCSource(allow_network=False)` por defecto, normalización a `Bar` canónico, y una función `check_connector()` que afirma keyless/read-only por introspección y devuelve un reporte PASS/FAIL.",
      "insight": "El camino seguro debe ser el default y el camino peligroso debe exigir una opción explícita en cada llamada — nunca un flag global. Un `allow_network=True` que se pasa por argumento en cada uso es seguro; una variable de entorno `ALLOW_NETWORK=1` que queda encendida es una fuga esperando ocurrir.",
      "errores": [
        "Usar un endpoint que requiere API key 'porque da más datos' — en el momento que firmas una petición, tu edge ya no es estructuralmente incapaz de operar, y toda la garantía se cae.",
        "Poner el control de red en una variable de entorno global en vez de un argumento explícito por llamada; queda encendida entre corridas y los loops automáticos heredan red sin querer.",
        "No normalizar en el borde: dejar que timestamps en ms y números como string se propaguen downstream, rompiendo la reproducibilidad entre la fuente local y la HTTP.",
        "Bindear el dashboard a `0.0.0.0` para 'verlo desde el móvil' — expones a la red toda tu investigación; usa `127.0.0.1` y un túnel SSH si de verdad lo necesitas.",
        "Verificar la propiedad keyless una sola vez a mano en vez de codificarla como aserción en un `check` que corre siempre; la garantía se erosiona en el primer refactor que nadie revisa."
      ]
    },
    "SG-02": {
      "objetivo": "Construir un clasificador de régimen de precio puro y determinista que etiqueta el estado del mercado (tendencia, volatilidad, comportamiento, estrés) sin jamás dimensionar ni operar una posición.",
      "porque": "Decidir sin saber en qué régimen estás es operar a ciegas: una estrategia que gana en tendencia se desangra en rango. La etiqueta de régimen es la condición previa honesta de cualquier decisión, y separarla de la ejecución evita que el sesgo de 'querer operar' contamine el diagnóstico.",
      "leccion": [
        "El error conceptual que XCAP corrigió en su auditoría de junio 2026 es sutil y vale oro: confundir salud operacional con régimen de precio. `market_state.py` mide salud del sistema (¿tengo datos frescos?, ¿están los flags bien?). `regime.py` mide algo totalmente distinto: ¿en qué ESTADO está el mercado mismo? Son ejes ortogonales y mezclarlos produce un clasificador que no sabe qué está diciendo. La primera disciplina es: un clasificador de régimen habla solo del precio, nunca del estado de tu infraestructura.",
        "El diseño de XCAP clasifica a lo largo de cuatro ejes independientes. Trend (¿sube, baja, lateral? — vía pendiente de medias o retorno acumulado sobre una ventana). Volatility (¿calma o agitado? — vía desviación estándar de retornos, normalizada). Behavior (¿persistente/tendencial o mean-reverting? — vía autocorrelación de retornos). Y Stress (¿hay un drawdown desarrollándose?). Cada eje se calcula de forma pura: misma entrada, misma salida, sin estado oculto, sin aleatoriedad. Luego una cascada de prioridad documentada deriva un `Regime` titular único a partir de los cuatro ejes.",
        "El insight más afilado de XCAP está en cómo define Panic, y es donde casi todo el mundo se equivoca. La tentación es definir pánico como 'volatilidad alta' (ratio de stdev elevado). Es incorrecto. Un crash monótono — el precio cayendo en línea casi recta — tiene volatilidad INTERNA baja, porque cada vela se parece a la anterior. Si defines pánico por stdev, te pierdes exactamente el crash más peligroso. La definición correcta de XCAP: pánico es un drawdown grande desarrollándose RÁPIDO, medido por un retorno reciente muy negativo y agudo, no por la dispersión. Esto es lo que distingue a un clasificador pensado por alguien que miró crashes reales de uno que copió una fórmula de un libro.",
        "La pureza no es un capricho estético, es la base de la confianza. `regime.py` es una función pura: `classify(bars) -> Regime`. No lee de disco dentro de sí, no llama a la red, no muta nada global, no tiene rama aleatoria. Esto significa que puedes correrla mil veces sobre la misma ventana y obtener la misma etiqueta, puedes testearla con fixtures deterministas, y puedes auditar exactamente por qué dijo 'Panic' en una fecha concreta. Un clasificador con estado oculto es un clasificador en el que no puedes confiar para construir Market Memory encima.",
        "La regla más importante, y la que conecta con toda la constelación: el clasificador es label-only. JAMÁS dimensiona ni opera. Devuelve una etiqueta y nada más. La razón es de arquitectura de honestidad: si el mismo módulo que diagnostica el régimen también decide el tamaño de la posición, tienes un incentivo para que el diagnóstico justifique la operación que ya querías hacer. Separar el diagnóstico de la acción es lo que permite, en SG-03, medir honestamente si el régimen tiene valor predictivo — porque la etiqueta se generó sin saber qué ibas a hacer con ella.",
        "Para implementarlo bien, documenta la cascada de prioridad como código legible, no como una maraña de ifs. Por ejemplo: si Stress dispara Panic, gana Panic sobre cualquier otra cosa; si no, y Volatility es alta con Trend fuerte, es Trending-Volatile; si Behavior es mean-reverting y Trend es lateral, es Range; etc. Cada rama debe tener un comentario que explique el porqué del mercado, no solo el umbral. Ese comentario es lo que un revisor — o tú mismo en seis meses — necesita para confiar en la etiqueta."
      ],
      "ejercicio": "Escribe `regime.py` con una función pura `classify(bars: list[Bar]) -> Regime`. Calcula cuatro sub-señales: `trend` (signo y magnitud del retorno sobre la ventana), `vol` (stdev de retornos diarios), `behavior` (autocorrelación lag-1 de retornos), `stress` (drawdown máximo reciente Y rapidez = retorno de los últimos N días). Implementa la cascada de prioridad donde Panic = drawdown grande + retorno reciente fuertemente negativo (NO stdev alto). Devuelve un `Regime` con el label titular y los cuatro ejes expuestos. Prueba con tres fixtures sintéticos: una tendencia alcista limpia, un crash monótono (debe dar Panic pese a vol interna baja), y un rango ruidoso. Verifica que la función es pura corriendo `classify` dos veces sobre la misma entrada y afirmando igualdad.",
      "entregable": "`regime.py` con `classify(bars) -> Regime` puro y determinista, cuatro ejes (trend/vol/behavior/stress), cascada de prioridad documentada, y tres tests de fixtures donde el crash monótono se clasifica como Panic correctamente.",
      "insight": "Pánico no es volatilidad alta — un crash monótono tiene volatilidad interna baja porque cada vela imita a la anterior. Define pánico por la velocidad del drawdown (retorno reciente agudo), no por la dispersión, o te perderás justo el régimen más peligroso.",
      "errores": [
        "Mezclar régimen de precio con salud operacional en el mismo módulo; son ejes ortogonales y juntarlos produce etiquetas que no significan nada concreto.",
        "Definir Panic por ratio de stdev alto: pierdes los crashes monótonos, que son justo los que importan.",
        "Meter una rama aleatoria o una lectura de disco dentro del clasificador, rompiendo su pureza y haciendo imposible auditar por qué etiquetó una fecha.",
        "Dejar que el clasificador dimensione o sugiera operaciones; en el momento que diagnóstico y acción comparten módulo, el diagnóstico empieza a justificar la operación que ya querías.",
        "Esconder la cascada de prioridad en ifs sin comentar el porqué de mercado; nadie podrá confiar ni revisar el umbral en seis meses."
      ]
    },
    "SG-03": {
      "objetivo": "Construir Market Memory: un ledger que bloquea una predicción falsable ANTES del desenlace, la resuelve contra el retorno realizado, y puntúa su propia calibración (hit-rate, Brier, calibración por confianza y por régimen).",
      "porque": "Sin un registro de predicciones bloqueadas antes del resultado, un sistema de research no tiene forma honesta de saber si sabe algo. Es la diferencia entre 'creo que funciona' y 'aquí está mi historial out-of-sample puntuado'. Es la única fuente real de evidencia de edge.",
      "leccion": [
        "El hallazgo central de la auditoría de XCAP fue demoledor: cada módulo previo era o computación stateless o un acumulador pasivo; NADA registraba una predicción falsable antes del desenlace y luego puntuaba su propia calibración. Ese loop ausente es exactamente lo que hace que un sistema de research sea difícil de engañar, y la única fuente honesta de evidencia de edge. Market Memory (`market_memory.py`) es ese loop. Si no construyes este módulo, todo lo demás es teatro: cálculos bonitos que nunca se confrontan con la realidad.",
        "El corazón es `advance_market_memory(universe, state_dir, horizon_days=5, ...)` y su secuencia es sagrada en este orden: observar fechas nuevas → clasificar el régimen (usando el `regime.py` de SG-02) → registrar un forecast BLOQUEADO (un baseline derivado del régimen, con su probabilidad y su horizonte) → resolver los forecasts que ya maduraron contra el retorno forward realizado → puntuar. El bloqueo es la palabra clave: una vez registras 'en la fecha T predigo prob_up=0.62 a 5 días', ese registro es inmutable. No puedes volver a tocarlo cuando ves el resultado. Esa inmutabilidad es lo que convierte el ledger en evidencia y no en una racionalización a posteriori.",
        "Las métricas no son una sola; son un panel honesto. Hit-rate (¿qué fracción de predicciones direccionales acertó?). Brier score (¿qué tan bien calibradas estaban las probabilidades? — penaliza tanto la sobreconfianza como la subconfianza). Calibración por bucket de confianza (cuando dijiste 70%, ¿acertaste el 70% de las veces?). Y la joya: hit-rate POR RÉGIMEN. Esta última es la que de verdad informa: te dice si tu predicción tiene valor en tendencia pero es basura en rango, o si solo aciertas en calma. Un hit-rate global de 55% puede esconder 70% en tendencia y 40% en pánico — y esa descomposición es la que dice dónde, si acaso, hay edge.",
        "La propiedad anti-fabricación es por construcción (la profundizamos en SG-04, pero aquí es estructural): solo se resuelven forecasts cuyo horizonte ya maduró con datos que existían DESPUÉS de la fecha del forecast. Es out-of-sample por diseño. Nunca puedes 'predecir' una fecha cuyo resultado ya conoces, porque el forecast se bloqueó en T y solo se resuelve cuando llega T+horizon con datos reales. El state dir está gitignored — no se versiona el historial para que no haya tentación de editarlo a mano y maquillar resultados.",
        "Las tres propiedades de integridad que XCAP impone, y que debes replicar: acumula (el ledger crece entre corridas, nunca se resetea), idempotente (correr el tick dos veces sobre las mismas fechas no duplica forecasts ni los resuelve dos veces), y Gate-closed (jamás opera). Esta es la misma familia de invariantes que el capital invariant de SG-05: el estado del sistema es la suma de eventos reales, nunca un replay ni un baseline fresco. El CLI `market-memory-tick` orquesta esto y lee CSVs formato `data/real` (lowercase) vía `LocalCSVSource` — cero red, todo determinista desde disco.",
        "Un punto que separa al ingeniero del aficionado: el baseline que registras debe ser honesto y modesto. XCAP registra un 'regime_baseline' — una probabilidad derivada del régimen, no un modelo elaborado que ya optimizaste mirando el pasado. ¿Por qué empezar humilde? Porque el baseline es tu vara de medir. Si un forecaster sofisticado no le gana al baseline de régimen out-of-sample, no tienes edge, tienes overfitting. Market Memory existe para refutar tus ideas, no para confirmarlas. Y un buen registro de forecasts es evidencia, nunca prueba — por eso el Capital Gate sigue cerrado por mucho que el hit-rate se vea bien."
      ],
      "ejercicio": "Construye `market_memory.py` con un ledger en JSON-lines append-only. Implementa `advance(universe, state_dir, horizon_days=5)` que: (1) lea las fechas nuevas, (2) para cada fecha clasifique régimen con tu `regime.py`, (3) registre un forecast bloqueado `{date, symbol, regime, prob_up, horizon, resolved: false}`, (4) resuelva los forecasts maduros (donde existe la barra a date+horizon) calculando el retorno forward real y marcando `hit`, (5) escriba métricas: hit-rate global, Brier, y un desglose de hit-rate POR régimen. Haz el tick idempotente: si ya existe un forecast para (date, symbol), no lo dupliques; si ya está resuelto, no lo re-resuelvas. Demuestra la acumulación corriendo el tick sobre dos rangos de fechas consecutivos y mostrando que el ledger crece sin resetearse.",
      "entregable": "`market_memory.py` + ledger append-only en `state/` (gitignored) con forecasts bloqueados antes del desenlace, resolución out-of-sample, métricas hit-rate/Brier/calibración-por-régimen, y un CLI `market-memory-tick` idempotente y acumulativo.",
      "insight": "La métrica que de verdad informa es el hit-rate POR RÉGIMEN, no el global. Un 55% global puede esconder 70% en tendencia y 40% en pánico — y esa descomposición es la única que te dice dónde, si acaso, hay edge real que explotar.",
      "errores": [
        "Registrar la predicción y resolverla en la misma corrida mirando ya el resultado; eso no es forecast, es ajuste a posteriori, y destruye toda evidencia de edge.",
        "Permitir que el ledger se resetee entre corridas; pierdes el historial que es justamente el activo, y rompes la propiedad de acumulación.",
        "Hacer el tick no idempotente: correrlo dos veces duplica forecasts o re-resuelve, inflando o corrompiendo las métricas.",
        "Reportar solo el hit-rate global y esconder el desglose por régimen, que es donde vive la información accionable.",
        "Versionar el state dir en git, abriendo la puerta a editar el historial a mano y maquillar la calibración; mantenlo gitignored."
      ]
    },
    "SG-04": {
      "objetivo": "Diseñar el sistema para que sea estructuralmente incapaz de inventarse datos o edge: null controls, significancia con corrección de comparaciones múltiples, evaluación out-of-sample y un edge_demonstrated que es siempre False por defecto.",
      "porque": "La forma más cara de mentirse en research es producir números que parecen edge pero son ruido, deriva o data-snooping. La anti-fabricación no es una revisión final: es una propiedad arquitectónica que hace imposible que un forecaster cante victoria sin haberle ganado al azar Y a la deriva, con significancia.",
      "leccion": [
        "La pregunta que define este módulo: ¿cómo construyes un sistema que no PUEDA mentirte sobre si tiene edge? La respuesta de XCAP no es 'ser cuidadoso'. Es introducir adversarios estructurales — null controls — contra los que toda afirmación de edge debe competir. El Hypothesis Lab (`hypothesis_lab.py`) tiene un registro de forecasters reales (regime_baseline, trend_follow, momentum_follow, mean_revert) Y dos controles nulos deliberados: `always_up` (el null de DERIVA — el mercado sube de media, ¿tu forecaster solo está capturando eso?) y `coin_flip` (el null de AZAR — sembrado y determinista, ¿le ganas a tirar una moneda?). Un forecaster que no le gana a AMBOS nulos no tiene edge, tiene una ilusión.",
        "El segundo pilar es la significancia con corrección por comparaciones múltiples. Si pruebas veinte forecasters, alguno parecerá bueno por puro azar — es el problema del data-snooping. XCAP lo neutraliza con un umbral z corregido por Bonferroni que ESCALA con K (el número de forecasters evaluados): cuantas más hipótesis pruebas, más alta la vara que cada una debe superar. `evaluate_forecasters(universe, names, horizon_days, min_samples, alpha)` compara cada forecaster real contra ambos nulos (Δazar y Δderiva) y solo le otorga `candidate_edge` si le gana al azar Y a la deriva Y supera el umbral corregido Y tiene al menos `min_samples` observaciones. Las cuatro condiciones, conjuntas. Quitar cualquiera abre una puerta a la fabricación.",
        "El detalle que demuestra que el sistema es honesto y no está amañado: `edge_demonstrated` es SIEMPRE False. El lab puede otorgar `candidate_edge` — 'esto merece más investigación' — pero nunca declara edge demostrado, porque ningún backtest, por bueno que sea, prueba edge futuro. Esta es una decisión de diseño que protege contra tu propio optimismo: el sistema literalmente no tiene un camino de código que diga 'sí, tienes edge, opera'. El Capital Gate sigue cerrado por construcción, no por tu disciplina.",
        "La verificación de que el scoreboard no está trucado es elegante y la debes replicar: XCAP comprobó que sobre tendencias sintéticas, `trend_follow` gana en grande (detecta la señal real), `mean_revert` se va significativamente NEGATIVO (detecta un anti-edge — apostar contra la tendencia pierde), y `coin_flip` aterriza en ~50% (el azar no se ve premiado). Si tu coin_flip saliera con 60% de hit-rate, tu pipeline está roto o tienes leakage. Los nulls no son solo adversarios: son sondas de calibración de tu propio sistema de medición. Un coin_flip que no da ~50% es un sistema que se está mintiendo, y debes pararlo todo hasta entender por qué.",
        "El fundamento compartido evita el truco de mover la portería: `scoring.py` es la única fuente de verdad para las primitivas (prob_up, is_hit, brier, conf_bucket, hit_rate_z). Tanto Market Memory (SG-03) como el Hypothesis Lab puntúan con EL MISMO código. Si tuvieras dos implementaciones de 'hit', podrías — consciente o no — usar la más generosa para tu forecaster favorito. Una sola primitiva compartida cierra esa puerta. Esto es anti-fabricación por construcción: no puedes elegir la regla que te conviene porque solo hay una regla.",
        "Toda evaluación es out-of-sample por la misma mecánica de SG-03: cada barra se clasifica una sola vez, el forecast se genera sin ver el futuro, y se resuelve contra el retorno forward real. El doc `docs/HYPOTHESIS_LAB.md` y los 4 invariantes del `check` codifican estas garantías. La lección de fondo, que es la tesis de toda la constelación Signal: el binding constraint para operar capital real es edge DEMOSTRADO, y estas herramientas miden edge honestamente (null controls + significancia) en vez de afirmarlo. Un sistema que solo puede emitir `candidate_edge` y nunca `edge_demonstrated` es un sistema que respeta lo que no sabe."
      ],
      "ejercicio": "Extiende tu Hypothesis Lab con `forecasters.py` (registro con al menos `trend_follow`, `mean_revert` y dos nulls: `always_up` para deriva y `coin_flip` sembrado para azar) y `evaluate_forecasters(names, horizon_days, min_samples, alpha)`. Para cada forecaster real calcula su hit-rate out-of-sample y el delta contra AMBOS nulls. Implementa un umbral z con corrección Bonferroni que escale con K = número de forecasters. Otorga `candidate_edge=True` solo si gana a azar Y a deriva Y supera el umbral Y tiene >= min_samples. Fija `edge_demonstrated=False` siempre. Valida tu pipeline sobre una serie sintética con tendencia: `trend_follow` debe salir significativamente positivo, `mean_revert` significativamente negativo, y `coin_flip` debe aterrizar en ~50% (si no, tu medición tiene leakage).",
      "entregable": "`forecasters.py` + `hypothesis_lab.py` con dos null controls (azar y deriva), umbral z Bonferroni que escala con K, gate de cuatro condiciones para `candidate_edge`, `edge_demonstrated` siempre False, y una validación donde coin_flip sale ~50% probando que el scoreboard no está trucado.",
      "insight": "Los null controls no son solo adversarios contra los que compites — son sondas de calibración de tu propio sistema de medición. Si tu coin_flip no sale ~50%, tienes leakage o un bug, y debes parar TODO hasta entender por qué antes de creerte cualquier otro número.",
      "errores": [
        "Comparar el forecaster solo contra el azar y olvidar la deriva; muchos 'edges' son solo el sesgo alcista de fondo del mercado disfrazado.",
        "No corregir por comparaciones múltiples: prueba 20 forecasters y uno parecerá significativo por puro azar (data-snooping).",
        "Permitir un camino de código que declare `edge_demonstrated=True`; ningún backtest prueba edge futuro, y ese flag invita a abrir el Capital Gate prematuramente.",
        "Tener dos implementaciones de 'hit' o 'brier' en vez de una sola primitiva compartida; te deja elegir la regla más generosa para tu forecaster favorito.",
        "Ignorar que el coin_flip salió 60%: eso no es suerte, es leakage en tu pipeline, y cualquier otro resultado del lab es basura hasta arreglarlo."
      ]
    },
    "SG-05": {
      "objetivo": "Implementar el capital invariant: un ledger donde el capital acumula entre sesiones, solo se mueve en cierres REALES (P&L realizado), y nunca se resetea — de modo que el número en pantalla siempre refleja resultados cerrados, no replays ni ganancias de papel.",
      "porque": "Un balance que se infla con P&L no realizado o que se resetea entre corridas es un balance que miente. La integridad del capital es la condición sin la cual ningún otro número del sistema merece confianza: si la contabilidad puede mentir, todo lo demás también.",
      "leccion": [
        "Este módulo nace de un fallo real y doloroso en XCAP, y por eso enseña tan bien. Un autorun programado estaba silenciosamente RESETEANDO el ledger al balance inicial de 500 y reproduciendo el mismo fixture idéntico cada 5 minutos. El sistema mostraba actividad, números cambiando, parecía vivo — y era completamente falso. Tres reglas se violaban a la vez, y son las tres que definen el capital invariant. Memoriza estas reglas porque son el núcleo: acumula, realized-only, y re-entry continúa sesiones previas.",
        "Regla 1 — Acumula. El capital NO se resetea entre corridas ni sesiones. Si terminaste la sesión de ayer con 547.30, hoy empiezas con 547.30. Un baseline fresco cada arranque no es contabilidad, es una demo disfrazada. El ledger se carga del disco al iniciar y se persiste al terminar; el estado vive entre ejecuciones.",
        "Regla 2 — Realized-only. El balance solo se mueve cuando una posición se CIERRA/vende. El P&L no realizado (mark-to-market, el valor flotante de una posición abierta) jamás infla el balance. La invariante exacta, que debes poder afirmar en cualquier momento: `current_balance == starting_balance + realized_pnl`. Una posición abierta con +200 de ganancia flotante NO cambia el balance; solo lo hace cuando la cierras y esos +200 se vuelven realizados. Esto evita la trampa psicológica más vieja del trading: contar las ganancias antes de embolsarlas.",
        "Regla 3 — Re-entry continúa. Al reiniciar, el capital es el resultado acumulado de las operaciones de sesiones previas, no una línea base nueva. El sistema debe poder apagarse y encenderse mil veces y el capital ser siempre la suma honesta de todos los cierres reales que ocurrieron. Esto es lo mismo que la propiedad de acumulación de Market Memory (SG-03): el estado es la suma de eventos reales, una sola dirección, sin replay.",
        "La lección de arquitectura es la distinción entre dos comandos que XCAP mantiene deliberadamente separados. El autorun programado corre `sim-autopilot-tick`: carga el ledger existente, rota regímenes sintéticos, y ACUMULA sobre lo anterior. Existe también `sim-run-agent`, que resetea y reproduce un fixture — pero ese se conserva SOLO para demos deterministas, y JAMÁS debe ser lo que corre el autorun. El bug fue precisamente apuntar el scheduler al comando equivocado. La enseñanza: un comando que resetea y un comando que acumula nunca deben ser confundibles; nómbralos de forma que el peligroso grite lo que hace.",
        "Cómo se blinda esto para que no vuelva a romperse: la invariante está LOCKED por `tests/test_sim_autopilot_tick.py`. Si tocas el ledger o el autorun, esos tests deben seguir verdes. No es documentación, es un candado ejecutable. La regla operativa que el usuario impuso enfáticamente: si tocas la contabilidad, corres ese test, y si se pone rojo, has roto la integridad del capital y paras. Esta es la misma filosofía que el Capital Gate y el `check` de SG-01: las invariantes que de verdad importan se codifican como tests que se niegan a dejarte romperlas. La contabilidad de XCAP no es confiable porque el autor sea cuidadoso; es confiable porque un test rojo le impide ser descuidado."
      ],
      "ejercicio": "Implementa `ledger.py` con un balance persistido en `state/ledger.json`. Funciones: `load()` (lee el balance acumulado, o el starting_balance solo en el primerísimo arranque), `open_position(symbol, entry, size)`, `mark(symbol, price)` (calcula P&L NO realizado pero NO toca el balance), y `close_position(symbol, exit)` (realiza el P&L y SOLO entonces actualiza el balance, luego persiste). Mantén la invariante `current_balance == starting_balance + sum(realized_pnl)` y afírmala tras cada operación. Escribe `test_capital_invariant.py` que: (1) abra una posición ganadora, llame `mark` con precio superior, y verifique que el balance NO cambió; (2) la cierre y verifique que ahora SÍ subió exactamente el P&L realizado; (3) simule un reinicio (load tras persistir) y verifique que el balance continúa, no se resetea.",
      "entregable": "`ledger.py` con balance acumulativo persistido + `test_capital_invariant.py` que prueba las tres reglas: mark no mueve el balance, close sí lo mueve por el P&L realizado exacto, y un reinicio continúa el acumulado en vez de resetear.",
      "insight": "La invariante `current_balance == starting_balance + realized_pnl` es una sola línea que, afirmada tras cada operación, hace estructuralmente imposible que el P&L de papel o un replay inflen tu balance. Cuando la contabilidad es una ecuación verificable y no una creencia, deja de poder mentirte.",
      "errores": [
        "Resetear el ledger al starting_balance en cada arranque del autorun — el bug original de XCAP; muestra actividad falsa y borra todo historial real.",
        "Dejar que el P&L no realizado (mark-to-market) toque el balance; cuentas ganancias que aún no embolsaste y el número en pantalla miente.",
        "Confundir el comando que acumula (`sim-autopilot-tick`) con el que resetea-y-reproduce (`sim-run-agent`); nombra el peligroso para que sea imposible apuntarle el scheduler por error.",
        "Tocar el ledger o el autorun sin correr `test_capital_invariant.py` después; la invariante se erosiona en el primer refactor que nadie verificó.",
        "Tratar la invariante como un comentario en el código en vez de una aserción ejecutada tras cada operación; un comentario no impide que la rompas, un assert sí."
      ]
    },
    "SG-06": {
      "objetivo": "Construir loops de autopilot que acumulan aprendizaje real entre ticks — deterministas desde disco, idempotentes, Gate-cerrados — en lugar de generar ruido o actividad falsa.",
      "porque": "Un loop autónomo mal diseñado es peligroso: corre solo, sin nadie mirando, y puede resetear estado, duplicar registros o disparar acciones reales. La diferencia entre un loop que aprende y uno que solo se mueve es toda la diferencia entre research y teatro automatizado.",
      "leccion": [
        "Un autopilot tick es código que corre sin un humano mirando, y esa es exactamente la razón por la que debe ser el código MÁS disciplinado del sistema. El error que XCAP sufrió en carne propia lo enseña todo: un tick programado que cada 5 minutos reseteaba el ledger y replicaba un fixture idéntico. Visto desde fuera, el sistema parecía vivo — números moviéndose, actividad constante. Por dentro era una mentira en bucle. La pregunta que define un buen tick: ¿este loop, corriendo mil veces, ACUMULA algo verdadero, o solo genera la apariencia de actividad?",
        "Propiedad 1 — Determinista desde disco. El tick lee su entrada de disco (CSVs `data/real` vía `LocalCSVSource`), no de la red. Los loops automáticos de XCAP NUNCA habilitan red — `allow_network` queda en False, siempre. Esto es continuidad directa de SG-01: la red es opt-in explícito por llamada humana (el comando `data-fetch`), jamás algo que un loop autónomo active solo. Un tick que toca la red sin supervisión es un tick que puede filtrar, fallar de forma no reproducible o colgarse esperando un socket. Determinista desde disco significa que el mismo estado en disco produce el mismo resultado, audaz y reproducible.",
        "Propiedad 2 — Idempotente. Correr el tick dos veces sobre las mismas fechas no debe duplicar forecasts, ni re-resolver lo ya resuelto, ni contar dos veces un cierre. Esto importa muchísimo en loops porque los schedulers fallan, reintentan, se solapan. Si tu tick no es idempotente, un reintento corrompe el estado en silencio. La forma de lograrlo: cada evento (un forecast, un cierre) tiene una clave natural (fecha+símbolo) y el tick comprueba existencia antes de escribir. `advance_market_memory` y `sim-autopilot-tick` son idempotentes por esta razón.",
        "Propiedad 3 — Acumula, no resetea. El tick carga el estado previo y construye SOBRE él. `sim-autopilot-tick` carga el ledger, rota regímenes sintéticos, y acumula — exactamente la regla del capital invariant de SG-05 y de Market Memory de SG-03. Esta es la misma invariante apareciendo por tercera vez en la constelación, y no es casualidad: la honestidad de un sistema autónomo ES su negativa a resetearse. Un loop que empieza de cero cada vez no aprende nada; un loop que acumula es lo único que puede construir un historial de calibración que valga.",
        "Propiedad 4 — Gate-cerrado por construcción. El tick JAMÁS opera capital real. El Capital Gate (`capital/broker/live_trading/automation` todos en False) está verificado por `python3 -m xcap.control check`. Un autopilot es justo donde un sistema de trading mata a su dueño: corre solo, y si tuviera capacidad de operar en real, un bug a las 3am vacía la cuenta. Por eso en XCAP el autopilot es sim-only, sin excepción, y `check` lo afirma en cada corrida. La automatización y la capacidad de operar real son dos cosas que NUNCA deben coexistir sin una decisión humana deliberadísima.",
        "Cómo se opera y se blinda todo esto: el tick corre vía el scheduler (en XCAP, una tarea programada que invoca `sim-autopilot-tick`, el comando que acumula — NUNCA `sim-run-agent`, el que resetea para demos). El comportamiento queda LOCKED por `tests/test_sim_autopilot_tick.py`: si tocas el autorun, esos tests siguen verdes o has roto la integridad del loop. Y cada corrida termina con un `check` que reafirma los invariantes (keyless, Gate-cerrado, acumulación). El principio final, que sella la constelación entera: un loop honesto es uno cuyas garantías no dependen de que alguien lo vigile, porque están codificadas como tests e invariantes que el propio loop verifica en cada vuelta. Construye loops que se nieguen a mentir incluso cuando nadie mira — esa es la única automatización en la que un sistema de research puede confiar."
      ],
      "ejercicio": "Construye `autopilot_tick.py` con una función `tick(state_dir)` que en cada llamada: (1) cargue el estado previo de disco (ledger + ledger de forecasts), (2) lea barras nuevas vía `LocalCSVSource` con `allow_network=False`, (3) avance Market Memory (bloquea forecasts nuevos, resuelve los maduros) acumulando, (4) corra `check()` afirmando Gate-cerrado y keyless, (5) persista. Hazlo idempotente: corre `tick` dos veces seguidas sobre el mismo estado y verifica que el segundo no añade ni cambia nada. Escribe `test_autopilot_tick.py` que pruebe: idempotencia (doble tick = estado idéntico), acumulación (dos ticks sobre rangos distintos crecen el estado), y que `check` falla si alguien pone `allow_network=True` o abre el Gate. Programa el tick con una tarea recurrente que invoque SOLO el comando que acumula.",
      "entregable": "`autopilot_tick.py` (determinista desde disco, idempotente, acumulativo, Gate-cerrado) + `test_autopilot_tick.py` que lockea idempotencia y acumulación, y un scheduler apuntando al comando que acumula — nunca al que resetea.",
      "insight": "La invariante de acumulación (no resetear) aparece tres veces en Signal — capital, Market Memory y autopilot — y no es casualidad: la honestidad de un sistema autónomo ES su negativa a resetearse. Un loop que empieza de cero cada vuelta no aprende; solo simula estar vivo.",
      "errores": [
        "Apuntar el scheduler al comando que resetea-y-reproduce (`sim-run-agent`) en vez del que acumula (`sim-autopilot-tick`); el bug exacto que falsificaba la actividad de XCAP.",
        "Dejar que el loop habilite red (`allow_network=True`); un tick autónomo tocando la red puede filtrar, colgarse o fallar de forma no reproducible sin nadie mirando.",
        "Hacer el tick no idempotente: cuando el scheduler reintente o se solape, duplicará forecasts y corromperá el estado en silencio.",
        "Dar al autopilot cualquier capacidad de operar real sin una decisión humana deliberada; automatización + capacidad de trade real es como un loop mata una cuenta a las 3am.",
        "Confiar en vigilancia humana en vez de codificar las garantías como tests e invariantes que el propio loop verifica con `check` en cada vuelta."
      ]
    }
  },
  "brand-surface": {
    "BS-01": {
      "objetivo": "Auditar una pantalla real y entregar un before/after donde cada elemento eliminado esté justificado por una decisión, no por gusto.",
      "porque": "El minimalismo radical de XNLAB no es estética: es un método de decisión. La mayoría de operadores cree que \"limpiar\" una interfaz es bajar opacidades y añadir aire. En realidad es lo contrario: cada elemento que sobrevive debe ganarse el sitio respondiendo a una pregunta concreta (¿qué hace el usuario aquí? ¿qué decisión toma?). Cirugía, no decoración. Sin un método explícito, \"minimalista\" degenera en \"vacío y arbitrario\", que es peor que recargado: parece descuidado. El antídoto es un audit con criterio escrito, donde quitar es una acción defendible y reversible, no un impulso.",
      "leccion": [
        "El minimalismo radical es un método de eliminación, no un estilo visual. Por defecto se quita; añadir es la excepción que carga con la prueba.",
        "Cada elemento de una pantalla debe responder a una pregunta del usuario en ese momento. Si no responde a ninguna, es ruido aunque sea bonito.",
        "Un before/after sin justificación escrita es opinión. Con justificación es un audit: defendible ante un cliente, repetible por otra persona, reversible si los datos lo contradicen.",
        "El vacío arbitrario (bajar opacidad, meter aire sin razón) se lee como descuido, no como sofisticación. Minimalismo mal hecho parece página rota.",
        "La regla XNLAB es conservadora pero no dogmática: el default es la edición quirúrgica, revisable con datos de conversión, no una ley moral."
      ],
      "ejercicio": "Toma una pantalla densa que ya exista (una landing, un dashboard, un formulario; la del propio MOONKEY sirve). Captura el estado actual como 'before'. Lista cada elemento visible en una tabla. Para cada uno responde en una línea: ¿qué decisión del usuario sirve aquí y ahora? Marca como candidato a eliminar todo lo que no responda. Aplica los cortes y captura el 'after'. Escribe junto a cada eliminación la razón (redundante / no responde a ninguna decisión / movible a otra vista / decorativo sin función). Verifica que ningún corte rompió una tarea que el usuario sí necesita completar.",
      "entregable": "Un audit before/after de una pantalla real: las dos capturas, la tabla de elementos con su pregunta-de-usuario, y la lista de eliminaciones con su justificación escrita en una línea cada una. Reversible: cualquiera puede leer por qué se quitó cada cosa y deshacerlo si un dato lo contradice.",
      "insight": "Quitar es la decisión más difícil de defender porque no deja rastro; por eso hay que escribir la razón antes de borrar, no después. El audit convierte el minimalismo de opinión en método.",
      "errores": [
        "Confundir minimalismo con bajar opacidades y meter aire: eso es vacío arbitrario, no cirugía. El resultado parece descuidado, no premium.",
        "Quitar elementos que sí sostienen una tarea del usuario y romper el flujo por perseguir limpieza visual.",
        "No documentar la razón de cada corte: sin justificación escrita el audit es irreversible y no se puede defender ante el cliente.",
        "Tratar la regla como ley moral inquebrantable en vez de default revisable con datos de conversión."
      ]
    },
    "BS-02": {
      "objetivo": "Construir un par de titulares que mezclen Inter sans con spans de itálica serif (Cormorant) sin que el ojo detecte el salto, y documentar la corrección de tamaño que lo hace funcionar.",
      "porque": "Mezclar una sans (Inter) con una itálica serif (Cormorant) dentro del mismo titular es uno de los gestos tipográficos con más retorno y más trampas. La trampa concreta y verificada en XNLAB: la serif itálica tiene una x-height menor, así que al mismo font-size se ve ópticamente más pequeña y el titular parece roto, como si la cursiva se hundiera. El operador que no conoce el ajuste lo deja así y el resultado chirría sin que sepa por qué. La corrección no es teórica: es un bump explícito de font-size en el span itálico (~1.18–1.2em) para igualar la altura percibida. Saber esto es la diferencia entre tipografía con intención y dos fuentes peleándose.",
      "leccion": [
        "El font-size nominal no es el tamaño percibido. Lo que el ojo mide es la x-height, y la itálica serif la tiene más baja que la sans, así que al mismo px se ve más pequeña.",
        "El ajuste verificado para Inter + Cormorant itálico es un bump de fontSize a 1.18-1.2em en el span serif. No es opcional: sin él el titular se lee roto.",
        "La itálica serif se usa para una palabra o frase con peso semántico dentro de un titular sans, no para decorar al azar. El contraste debe marcar algo, no solo verse bonito.",
        "La intención tipográfica se demuestra: el span corregido y el span sin corregir, lado a lado, hacen el problema visible y la solución innegable."
      ],
      "ejercicio": "Escribe dos titulares de XNLAB donde una palabra clave vaya en itálica serif dentro de texto sans (ej: un sustantivo que cargue el sentido). Renderiza cada uno dos veces: una con el span itálico al mismo font-size que la sans, otra con fontSize:'1.18em' en el span. Compara las dos versiones lado a lado y confirma con el ojo (no con el número) cuál iguala la altura percibida. Anota el valor exacto que cuadra para tu par tipográfico. Si el salto sigue visible, sube hacia 1.2em y vuelve a comprobar.",
      "entregable": "Un par de titulares italic-mix funcionando, cada uno acompañado de su comparación 'sin corregir / corregido' que hace visible el problema de x-height, más la línea de CSS exacta (fontSize:'1.18em' o el valor que cuadre) documentada como decisión reutilizable.",
      "insight": "La tipografía con intención se nota justo cuando no se nota: si el lector percibe el salto entre sans y serif, fallaste. El ajuste de x-height es invisible bien hecho y obvio mal hecho.",
      "errores": [
        "Asumir que mismo font-size significa mismo tamaño visual: ignora la x-height y deja el titular hundido en la cursiva.",
        "Usar la itálica serif como decoración sin peso semántico: el contraste tipográfico debe señalar algo, no rellenar.",
        "Verificar el ajuste por el número (1.18em) y no por el ojo: el valor exacto depende del par de fuentes y del tamaño; hay que mirarlo.",
        "Aplicar el bump globalmente en vez de al span concreto, rompiendo el resto de la jerarquía."
      ]
    },
    "BS-03": {
      "objetivo": "Escribir un copy deck de página real (hero + tres secciones) en voz de estudio anónima que pase el test \"¿verdad o marketing?\": cada frase o es verificable o se borra.",
      "porque": "La voz de XNLAB es anónima por diseño: estudio, nunca persona. No hay fundador con nombre, ni bio, ni foto, ni \"yo creo que\". Esto suena fácil y es lo más difícil: sin un yo al que apelar, la única autoridad que queda es la precisión. El fallo típico del operador es rellenar el vacío con humo —\"transformamos tu negocio\", \"resultados excepcionales\", \"pasión por el detalle\"— frases que no dicen nada verificable. La voz de estudio se sostiene en lo contrario: afirmaciones que o son verdad comprobable o no se escriben. El estado legal de XNLAB lo exige literalmente: no inventar NIF, dirección ni datos de registro; marcar como no registrado lo que no lo está. El copy deck es el artefacto donde esa disciplina se vuelve testeable.",
      "leccion": [
        "La voz de estudio es anónima: nunca primera persona singular, nunca nombre de fundador, nunca bio. La autoridad viene de la precisión, no de quién habla.",
        "El test '¿verdad o marketing?': cada frase se etiqueta. Si es verificable, queda. Si es promesa vacía o adjetivo sin respaldo, se borra o se reescribe en hecho.",
        "Las promesas vacías ('transformamos', 'excepcional', 'líder') son ruido que baja la credibilidad de todo lo que las rodea, incluido lo que sí es verdad.",
        "La honestidad legal es parte de la voz: no inventar datos de entidad. Lo no registrado se marca como no registrado; el ciclo se describe como está ('Open.'), no como conviene.",
        "Anónimo no es frío. La voz es directa y concreta —el calor viene del rigor y de hablar de cosas reales, no de confesiones personales."
      ],
      "ejercicio": "Elige una página (home o /contact). Escribe el hero y tres secciones en voz de estudio. Después pasa cada frase por el test: en una columna pega la frase, en otra escribe 'verdad' (y cómo se verifica) o 'marketing' (y por qué es humo). Toda frase marcada 'marketing' se reescribe como hecho verificable o se elimina. Revisa que no haya colado ningún 'yo', ningún nombre propio, ningún dato de entidad inventado. Confirma que el estado del ciclo se describe como está realmente, no como vendería mejor.",
      "entregable": "Un copy deck de una página real (hero + 3 secciones) con su tabla de test '¿verdad o marketing?' al lado: cada frase final clasificada como verdad-verificable, sin una sola promesa vacía superviviente y sin datos de entidad inventados. Listo para pegar en la página.",
      "insight": "Quitar el 'yo' obliga a que la prueba la cargue cada frase, no la persona; por eso la voz anónima es el detector de humo más severo que existe. Si una afirmación necesita un nombre detrás para sostenerse, no era verdad: era marketing.",
      "errores": [
        "Rellenar la falta de fundador con superlativos ('excepcional', 'líder', 'transformador') en vez de con hechos verificables.",
        "Colar primera persona o un nombre propio y romper el anonimato del estudio.",
        "Inventar datos de entidad (NIF, dirección, registro) para parecer establecido: viola el estado legal real y se detecta.",
        "Confundir anónimo con frío y escribir un copy sin vida: el calor viene del rigor y de lo concreto, no de un yo.",
        "Describir el ciclo como conviene comercialmente ('una plaza queda') en vez de como está ('Open.')."
      ]
    },
    "BS-04": {
      "objetivo": "Producir un par EN/ES de un bloque de copy donde el español diga lo mismo que el inglés —no lo literal— y entregar las notas de cada decisión de sentido.",
      "porque": "Traducir el copy de XNLAB no es sustituir palabras: es preservar significado a través de dos idiomas con gramáticas distintas. El error documentado y recurrente es el espejo léxico: traducir 'Not X' como 'Sin X' cuando el inglés marcaba una comparación (no una ausencia) y el español la convierte en otra cosa. Lo mismo con el paso de pasiva inglesa a refleja española, o con modificadores y verbos que se caen sin que nadie lo note. Cada uno de esos resbalones cambia el sentido un grado, y tres grados después la página en español dice algo que el inglés nunca dijo. La única defensa es leer cada línea en español como un nativo —no como traductor— y verificar que la afirmación es la misma, no que las palabras coinciden.",
      "leccion": [
        "Traducir es preservar el sentido, no espejar el léxico. La pregunta correcta no es '¿esta palabra existe en español?' sino '¿esta frase afirma lo mismo?'.",
        "'Not X' → 'Sin X' es la trampa estrella: ausencia vs comparación. El inglés a veces contrasta, y 'Sin X' lo convierte en una carencia que el original no decía.",
        "La pasiva inglesa que pasa a refleja española puede cambiar quién hace la acción y la responsabilidad implícita. Hay que comprobar el agente, no solo la forma.",
        "Modificadores y verbos se caen en la traducción sin avisar. Un adjetivo perdido o un verbo aplanado baja la temperatura o cambia la fuerza de la afirmación.",
        "El control de calidad es leer cada línea ES como nativo, no contrastar contra el EN palabra a palabra. Si suena a traducción, es traducción; si suena a original, está bien."
      ],
      "ejercicio": "Toma un bloque EN real (hero o sección con afirmaciones cargadas). Traduce al español priorizando sentido. Luego audita línea a línea: ¿algún 'Not X' se volvió 'Sin X' cambiando comparación por ausencia? ¿alguna pasiva pasó a refleja y movió el agente? ¿cayó algún modificador o verbo? Para cada hallazgo, anota la decisión: qué dijo el inglés, qué decía tu primer borrado, qué pusiste y por qué preserva el sentido. Lee la versión final en español en voz alta como nativo y confirma que afirma exactamente lo mismo que el inglés.",
      "entregable": "Un par EN/ES de un bloque real con sus notas de traducción: para cada línea con riesgo (Not X / pasiva-refleja / modificador caído), la decisión documentada de cómo se preservó el sentido. El ES pasa la lectura de nativo sin oler a traducción.",
      "insight": "Una traducción correcta palabra a palabra puede ser una mentira frase a frase; el espejo léxico es el camino más rápido a decir algo que el original nunca afirmó. Preservar el sentido a veces exige alejarse de las palabras.",
      "errores": [
        "Espejo léxico: 'Not X' → 'Sin X' convirtiendo una comparación en una ausencia que el inglés no enunciaba.",
        "Dejar que la pasiva inglesa se vuelva refleja española y desplace el agente o la responsabilidad implícita.",
        "Perder modificadores o verbos en el trasvase y aplanar la fuerza de la afirmación sin darse cuenta.",
        "Verificar contrastando palabra a palabra contra el EN en vez de leer el ES como nativo: el espejo léxico pasa esa prueba y aun así miente."
      ]
    },
    "BS-05": {
      "objetivo": "Documentar un sistema de color y profundidad (glass, bandas, campos de color) como conjunto de reglas y tokens reutilizables, con muestras que prueben que da vida sin meter ruido.",
      "porque": "Color y profundidad son donde una superficie minimalista gana vida o se rompe. Glass, bandas de color y campos de fondo pueden hacer que una página plana respire —o convertirla en un carnaval que contradice el minimalismo radical de BS-01. La diferencia entre los dos resultados no es el gusto: es un sistema. Sin reglas escritas (cuándo glass, qué intensidad, qué profundidad de campo, qué se anima y qué no), cada pantalla improvisa y el conjunto pierde coherencia. Hay además una trampa de verificación documentada en XNLAB: las animaciones de fondo se pausan cuando la pestaña no está activa, así que un read de opacidad miente; la profundidad animada se verifica con captura visual, no leyendo valores. El entregable es el sistema escrito: tokens, reglas y muestras que cualquiera puede aplicar sin reinventarlo.",
      "leccion": [
        "Color y profundidad dan vida solo dentro de un sistema. Glass, bandas y campos sin reglas degeneran en ruido que contradice el minimalismo.",
        "El sistema se escribe como tokens y reglas: cuándo usar glass, qué intensidad, qué profundidad de campo, qué se mueve y qué se queda quieto. Reutilizable, no improvisado por pantalla.",
        "Nunca interpolar clases de color dinámicas (bg-${theme}): rompe el JIT de Tailwind. Los temas se declaran como entradas explícitas en el mapa de THEME.",
        "Las animaciones de fondo se pausan en pestaña inactiva: un read de opacidad da un valor falso. La profundidad animada se verifica con captura de pantalla, no leyendo el DOM.",
        "El criterio es 'vida sin ruido': si un campo de color o un glass no aporta jerarquía o respiración, sobra. La profundidad sirve a la lectura, no se exhibe."
      ],
      "ejercicio": "Inventaria los recursos de profundidad que ya usa la superficie (glass, bandas, blobs, campos de fondo). Para cada uno escribe la regla: cuándo se usa, a qué intensidad, qué jerarquía sirve, si se anima o no. Conviértelos en tokens nombrados. Renderiza una muestra con el sistema aplicado y otra sin él, lado a lado, para probar que la versión con sistema da vida sin subir el ruido. Verifica cualquier capa animada con captura de pantalla en pestaña activa, no leyendo opacidad (que miente con la pestaña en background). Confirma que ningún color se interpola en clase dinámica.",
      "entregable": "Un sistema documentado de color y profundidad: tokens nombrados + reglas de uso (cuándo/qué intensidad/qué se anima) + un par de muestras con-sistema / sin-sistema que prueba 'vida sin ruido', verificadas por captura visual. Reutilizable por cualquier pantalla sin reinventar el criterio.",
      "insight": "La profundidad bien hecha se siente como aire y mal hecha como ruido, y la única forma de mantener el lado correcto a escala es un sistema escrito, no buen gusto por pantalla. Y nunca te fíes de un read de opacidad: la pestaña dormida te mentirá.",
      "errores": [
        "Improvisar glass, bandas y campos pantalla por pantalla sin reglas: el conjunto pierde coherencia y el minimalismo se rompe.",
        "Interpolar clases de color dinámicas (bg-${theme}) y romper el JIT de Tailwind, dejando estilos que no compilan.",
        "Verificar profundidad animada leyendo opacidad en el DOM: la animación se pausa en pestaña inactiva y el valor leído miente. Hay que capturar pantalla.",
        "Meter campos de color o glass que no aportan jerarquía ni respiración: decoración que sube el ruido y traiciona 'vida sin ruido'."
      ]
    }
  },
  "ops-security": {
    "OS-01": {
      "objetivo": "Decidir, para cualquier clave o token de tus proyectos, si puede vivir en el cliente o jamás debe salir del servidor, y construir el árbol de decisión que te impide volver a equivocarte.",
      "porque": "Una sola service_role key filtrada en un bundle de Astro es acceso total a tu base de datos saltándose RLS. No es un bug que se parchea: es una brecha que obliga a rotar credenciales, auditar accesos y, si hay datos de usuarios, notificar. El coste de equivocarte aquí es el más alto de toda la galaxia.",
      "leccion": [
        "Empieza por la regla mental que nunca falla: todo lo que compila en un sitio estático es público. MOONKEY LAB es Astro 4 en modo SSG — el build genera HTML+JS plano que se sirve desde Cloudflare Pages. No hay servidor que guarde secretos en runtime. Cualquier string que importes en un componente .astro que llegue al cliente acaba, literalmente, en un archivo que cualquiera descarga con `view-source` o abriendo DevTools → Sources. Por eso `src/lib/supabase.ts` SOLO contiene la anon key. La anon key está DISEÑADA para ser pública: es un identificador de proyecto + un token con permisos de rol `anon`, cuya autoridad real la limita Postgres RLS. No es un secreto; es una dirección con un portero.",
        "Ahora el contraste que tienes que interiorizar. Hay dos claves Supabase con el mismo aspecto (un JWT largo) pero universos opuestos de poder. La `anon` key opera bajo el rol `anon`/`authenticated` y está SOMETIDA a RLS: ve solo lo que las policies permiten. La `service_role` key opera con `BYPASSRLS`: ignora todas las policies, lee y escribe cualquier fila de cualquier tabla — incluidas las de XHUB IRON (`iron_*`, `world_*`) que comparten la misma instancia `wuchsslgbqlhyxljsmxi`. Si la `service_role` aparece en el cliente, un atacante no solo compromete MOONKEY: pivota a todos los proyectos que viven en esa DB. Regla dura: la `service_role` NUNCA entra en un repo de frontend, ni en un `.astro`, ni en un `import.meta.env.PUBLIC_*`. Su sitio es un backend con sesión (una Edge Function, un proceso server) o tu llavero local — nunca el bundle.",
        "El vector de fuga más común no es pegar la clave en el código: es el prefijo de variable de entorno. En Astro, `import.meta.env.PUBLIC_X` se INLINEA en el bundle del cliente en build-time; `import.meta.env.X` (sin `PUBLIC_`) solo existe en contexto server/build y NO se expone. Vite/Astro hacen esto por diseño. El error clásico: nombrar `PUBLIC_SUPABASE_SERVICE_KEY` 'para que el componente la vea'. Lo logras — y se la regalas al mundo. Tu árbol de decisión para nombrar una env var: ¿esta clave puede ser pública? Sí → `PUBLIC_`. No → sin prefijo, y además pregúntate qué hace en un proyecto SSG (probablemente no debería estar ahí).",
        "Construye el inventario. Abre tu proyecto y clasifica cada credencial en una tabla de tres columnas: nombre · clase (pública/privada) · dónde vive hoy. En MOONKEY las entradas reales son: `PUBLIC_SUPABASE_URL` (pública, identificador), `PUBLIC_SUPABASE_ANON_KEY` (pública, limitada por RLS), y la `service_role` (privada — NO debe existir en este repo en absoluto; si la necesitas para un script de migración, vive en tu shell local o en un secret de CI, jamás en `src/`). Añade cualquier token de terceros que toques: claves de email transaccional, webhooks, tokens de analytics server-side. Cada fila privada que esté en el lado equivocado es un incidente esperando a pasar.",
        "El control que cierra el ciclo es la detección automática, porque la disciplina humana falla. Antes de cada commit quieres que algo grite si una clave privada se cuela. El patrón mínimo: un grep en pre-commit que busque el patrón de una `service_role` (los JWT de Supabase service llevan `\"role\":\"service_role\"` en el payload base64) y palabras como `service_role`, `BEGIN PRIVATE KEY`, `sk-`. Para los repos reales usa `gitleaks` o `trufflehog` como hook. Y si una clave YA se filtró en un commit pasado: no basta con borrarla del código — sigue en el historial de git. Hay que ROTARLA en el panel de Supabase (Settings → API → roll key) y reescribir historial si era un secreto duro. Rotar es obligatorio; borrar el archivo no revoca nada.",
        "Cierra con el caso Espejo, que añade un eje ético, no solo técnico. Espejo es multi-tenant: cada influencer-tenant tiene sus datos. Ahí la pregunta 'pública vs privada' se duplica: además de claves de infraestructura, manejas datos personales/consentidos de usuarios finales. La clave de servicio que pudiera leer ACROSS tenants es el secreto más sensible del producto — su fuga no es solo técnica, viola la frontera de datos que la memoria de proyecto marca como innegociable. Lección transversal: una credencial privada no se define por su formato, se define por lo que desbloquea. Mídela siempre por su blast radius."
      ],
      "ejercicio": "En tu proyecto (usa MOONKEY o uno propio): 1) Crea `SECRETS.md` con la tabla de inventario (nombre · clase · ubicación · blast radius) para cada credencial que el proyecto toca. 2) Ejecuta `grep -rn \"service_role\\|BEGIN.*PRIVATE KEY\\|sk-\" src/ .env* 2>/dev/null` y documenta qué encontró (idealmente nada en `src/`). 3) Escribe un hook `.git/hooks/pre-commit` que haga ese grep sobre los archivos staged (`git diff --cached --name-only`) y aborte con exit 1 si hay match. 4) Verifica el hook intentando commitear un archivo de prueba con la string `\"role\":\"service_role\"` y confirma que lo bloquea.",
      "entregable": "`SECRETS.md` con la tabla de inventario completa (cada credencial clasificada por clase y blast radius) + un hook `.git/hooks/pre-commit` ejecutable que bloquea commits con secretos privados, probado contra un caso que debe fallar.",
      "insight": "Una credencial no es secreta por cómo se ve, sino por lo que desbloquea: la anon key y la service_role son ambas JWTs idénticos a la vista, y una es pública por diseño mientras la otra compromete toda la galaxia. Clasifica siempre por blast radius, nunca por formato.",
      "errores": [
        "Poner el prefijo `PUBLIC_` a una clave privada 'para que el componente la lea' — Astro la inlinea en el bundle y la publica al mundo.",
        "Creer que borrar la clave del archivo la revoca: sigue en el historial de git y sigue activa hasta que la ROTES en el panel del proveedor.",
        "Tratar la anon key como un secreto y ofuscarla: pierdes el tiempo protegiendo algo público mientras la autoridad real (RLS) queda sin revisar.",
        "Asumir que un sitio SSG 'no tiene secretos expuestos' sin auditar el bundle generado: lo que importa es qué hay en `dist/`, no qué hay en `src/`.",
        "Meter la service_role en un script de migración versionado en el repo en vez de leerla del shell local o de un secret de CI en tiempo de ejecución."
      ]
    },
    "OS-02": {
      "objetivo": "Pensar como el atacante de un sitio estático con anon key: enumerar la superficie de ataque real de una SSG + Supabase y entender por qué la única defensa que cuenta es RLS, no el cliente.",
      "porque": "Mucha gente protege la puerta equivocada: oculta el panel admin en el frontend y deja la DB abierta. Un sitio estático no tiene servidor que valide nada — si tu modelo de amenaza no parte de 'el cliente es hostil y controlado por el atacante', construirás defensas decorativas que se saltan con curl.",
      "leccion": [
        "Adopta el marco mental correcto: en una SSG, el cliente no es tu aplicación, es territorio enemigo. El atacante tiene tu bundle completo (es público), tu `PUBLIC_SUPABASE_URL`, tu anon key, y conocimiento de que detrás hay Postgres. No necesita 'hackear' tu JavaScript: lo lee. Puede instanciar su propio cliente Supabase con TU anon key desde una consola de Node y hablar directo con tu DB, sin pasar jamás por tu HTML. Por tanto, toda lógica de seguridad que viva en `.astro` o en JS de cliente — un `if (user.role === 'admin')`, un componente que se oculta — es UX, no seguridad. CLAUDE.md de MOONKEY lo dice literal: `admin.astro` es UX; la seguridad NO depende de ese gate.",
        "Enumera la superficie de ataque concreta. Para MOONKEY hay cinco tablas alcanzables con la anon key: `profiles, progress, feedback, leads, proofs`. El atacante, con un cliente autenticado vía magic-link (cualquiera puede registrarse), intentará lo evidente: `supabase.from('profiles').select('*')` para volcar TODOS los perfiles; `supabase.from('leads').select('*')` para robar la lista de leads; un `update` sobre su propia fila de `profiles` poniendo `role = 'admin'` o `founder_badge = true` para auto-escalar; un `insert` en `proofs` con `user_id` ajeno para falsear progreso de otro. Cada una de estas es una hipótesis de ataque que TÚ debes refutar con una policy. Si no la has refutado explícitamente, asume que funciona.",
        "Entiende por qué RLS es la autoridad y cómo se compone la defensa real de MOONKEY. SELECT en `profiles/progress/feedback/proofs` es self-or-admin: la policy permite la fila solo si `user_id = auth.uid()` O `is_admin()`. `is_admin()` es SECURITY DEFINER — corre con los permisos del dueño, consulta si el llamante es admin sin que el llamante pueda manipular esa decisión. Resultado: un no-admin que pide `select('*')` no recibe un error, recibe SUS filas y nada más — el dump masivo devuelve una sola fila, la suya. La escalada de privilegios la corta un trigger (`guard_privileged_profile_columns`): `role` y `founder_badge` son inmutables para no-admins, así que el `update role='admin'` se rechaza a nivel de DB. El insert ajeno lo corta `proofs_insert_self`: exige sesión y liga `user_id = auth.uid()`.",
        "La consecuencia operativa del modelo: verifica por impersonación, no por lectura de código. Una policy que 'parece' correcta puede tener un agujero (un OR mal puesto, un USING sin WITH CHECK que deja pasar updates). La única prueba que vale es ponerte en los zapatos del atacante: autentícate como un usuario normal y EJECUTA los ataques enumerados arriba contra la DB real, confirmando que cada uno devuelve vacío o error. CLAUDE.md menciona que el aislamiento de lectura está 'verificado por impersonación' — ese es el estándar. 'Escribí la policy' no es evidencia; 'intenté el ataque como usuario X y no leyó la fila de usuario Y' sí lo es.",
        "No olvides los vectores que no son RLS. El modelo de amenaza de una SSG incluye también: (a) la diferencia USING vs WITH CHECK — USING filtra qué filas ves/afectas, WITH CHECK valida el estado resultante de un INSERT/UPDATE; olvidar WITH CHECK en un UPDATE deja que muevas una fila a un user_id ajeno. (b) Las RPC: una función SECURITY DEFINER mal escrita es un boquete que ignora RLS por diseño — MOONKEY las restringe a `authenticated` y revoca operaciones peligrosas. (c) TRUNCATE: revocado de anon/authenticated, porque RLS no protege contra TRUNCATE (borra la tabla entera sin evaluar policies por fila). (d) Fuga por errores y por columnas: un SELECT que devuelve una columna sensible que olvidaste excluir.",
        "Aterriza con el contraste de proyectos para fijar el principio. XNLAB es un sitio de marca casi sin backend: su superficie es mínima — formularios de contacto, cabeceras, quizá un endpoint de captación. Ahí el modelo de amenaza es 'spam y XSS en lo poco que acepta input', no escalada de DB. XHUB IRON vive en la MISMA DB que MOONKEY pero es un panel operativo del fundador: sus tablas `iron_*` no deben ser legibles por un usuario de MOONKEY — el mismo motor RLS tiene que mantener DOS productos que comparten Postgres sin que uno lea al otro (eso es OS-05). El modelo de amenaza no es genérico: se deriva de QUÉ datos hay, QUIÉN puede autenticarse, y QUÉ comparten la infraestructura. Empieza siempre por inventariar esos tres ejes."
      ],
      "ejercicio": "Sobre MOONKEY (o tu proyecto Supabase): 1) Escribe `THREAT-MODEL.md` listando, por cada tabla alcanzable con anon key, los ataques que un usuario autenticado hostil intentaría (dump masivo, escalada de privilegios, escritura sobre fila ajena). 2) Para cada ataque, anota la defensa que DEBERÍA detenerlo (qué policy/trigger) y márcalo como 'verificado' o 'no verificado'. 3) Escribe el snippet de impersonación que ejecutarías para probar el dump de `profiles` como usuario normal (cliente con anon key + sesión de un user de prueba haciendo `.from('profiles').select('*')`) y predice el resultado esperado (solo 1 fila).",
      "entregable": "`THREAT-MODEL.md`: una tabla ataque→defensa→estado-de-verificación que cubre cada tabla expuesta por la anon key, más el snippet de impersonación concreto para al menos un ataque de dump masivo con su resultado esperado.",
      "insight": "En una SSG el cliente es territorio del atacante: tiene tu bundle, tu anon key y habla directo con tu Postgres por curl sin tocar tu HTML. Toda comprobación de seguridad que vive en .astro es UX; la única frontera real es RLS, y solo cuenta cuando la has refutado por impersonación, no por leerla.",
      "errores": [
        "Confundir ocultar el panel admin en el frontend con protegerlo: `admin.astro` esconde la UI, pero la DB sigue accesible por curl con la anon key.",
        "Verificar policies leyéndolas en vez de atacándolas como usuario impersonado — un OR mal puesto solo se ve ejecutando el ataque.",
        "Escribir un UPDATE con USING pero sin WITH CHECK, permitiendo reasignar la fila a un `user_id` ajeno aunque 'parezca' que la policy protege.",
        "Olvidar que TRUNCATE y las RPC SECURITY DEFINER esquivan RLS: protegerlas exige REVOKE explícito, no basta con tener policies de fila.",
        "Aplicar un modelo de amenaza genérico copiado de un tutorial en vez de derivarlo de tus tres ejes reales: qué datos, quién autentica, qué infraestructura comparte."
      ]
    },
    "OS-03": {
      "objetivo": "Auditar de forma adversarial: tratar cada hallazgo de seguridad como una hipótesis que debes refutar antes de creerla, en vez de aceptar 'parece vulnerable' o 'parece seguro' por inspección.",
      "porque": "El sesgo de confirmación arruina auditorías: un escáner dice 'RLS desactivado' y entras en pánico, o tú dices 'la policy se ve bien' y firmas el visto bueno. Ambos errores cuestan caro — falsos positivos queman tu tiempo y credibilidad; falsos negativos dejan brechas abiertas. La disciplina de refutar primero es lo que separa una auditoría real de una opinión.",
      "leccion": [
        "El principio rector es popperiano: un hallazgo de seguridad no es verdadero porque lo afirmes, es verdadero porque INTENTASTE refutarlo y no pudiste. Invierte el flujo natural. Cuando crees haber encontrado una vulnerabilidad ('cualquiera puede leer `leads`'), tu siguiente paso no es reportarla — es intentar demostrar que estás equivocado: ¿hay una policy que lo impide? ¿la probé como el rol correcto? ¿el `select` devolvió datos reales o vacío? Solo cuando tu intento de refutación falla, el hallazgo asciende a confirmado. Y al revés: cuando crees que algo es seguro ('la policy self-or-admin protege `profiles`'), tu trabajo es atacarlo hasta que se rompa o sobreviva.",
        "Define los dos errores que persigues con nombres claros. Falso positivo: reportas una vulnerabilidad que no existe — p.ej. gritas 'la anon key está expuesta en el bundle' cuando la anon key es pública por diseño y la DB está protegida por RLS (ver OS-01). Quemas confianza; la próxima vez nadie te cree. Falso negativo: das por seguro algo que no lo es — p.ej. 'el panel admin está oculto, estamos bien' sin probar el acceso directo a la DB. Dejas la brecha abierta. La auditoría adversarial existe para minimizar AMBOS, y la herramienta para ambos es la misma: la prueba ejecutable.",
        "El método concreto: por cada hallazgo, produce una prueba reproducible, no una afirmación. 'Reproducible' significa: un comando o snippet que cualquiera puede correr y ver el mismo resultado. Para confirmar que `profiles` NO se puede dumpear: un script que se autentica como user_A, hace `.from('profiles').select('*')`, e imprime el conteo de filas — esperado 1 (solo la suya). Para confirmar que la escalada de privilegios está cerrada: como user_A, `.from('profiles').update({ role: 'admin' }).eq('id', myId)` e imprimir el error que devuelve el trigger guard. Si el ataque devuelve lo que esperabas (vacío/error), el control está verificado. Si devuelve datos, tienes un hallazgo REAL — y ya viene con su PoC adjunto.",
        "Trabaja con la herramienta del propio Supabase pero sin rendirte a ella. `get_advisors` (security lints) te señala tablas sin RLS, policies permisivas, funciones SECURITY DEFINER sin search_path fijo. Trátalo como generador de HIPÓTESIS, no de veredictos. Un advisor que dice 'función X es SECURITY DEFINER mutable search_path' es una hipótesis de riesgo: ve a la función, mira si un atacante puede plantar un objeto en un schema que ella resuelva primero. A veces es explotable, a veces no por el contexto. El advisor te ahorra el barrido inicial; la refutación la haces tú. CLAUDE.md ya lo institucionaliza: cambios de RLS = migración versionada + `get_advisors` después. Esa segunda parte es la auditoría adversarial hecha rutina.",
        "Cuida los falsos negativos del propio método: el ataque que 'pasa' puede pasar por la razón equivocada. Si tu `select('*')` sobre `profiles` devuelve vacío, ¿es porque RLS lo bloqueó, o porque tu sesión de prueba caducó / no estabas autenticado / la tabla estaba vacía? Un control nunca se da por bueno sin un caso de CONTRASTE: prueba que el mismo query SÍ devuelve datos cuando debe (como admin, o como el dueño de la fila). Una prueba de seguridad sin su control positivo es un falso negativo disfrazado de éxito. Siempre verifica que tu arma funciona antes de concluir que el blanco es inmune.",
        "Aterriza el rigor en los proyectos donde más duele equivocarse. En XCAP la auditoría adversarial es doctrina central: la memoria del proyecto lo describe como 'anti-fabricación por construcción' — un sistema que registra forecasts y los calibra contra resultados reales, refutando sus propias predicciones en vez de creérselas. Ese mismo músculo aplicas a seguridad: no firmes 'el ledger es read-only' hasta que un intento de escritura con la credencial de ingest falle ante tus ojos. En Espejo, donde la frontera de datos personales es ética además de técnica, una auditoría que dice 'no hay fuga entre tenants' sin un PoC de un tenant intentando leer a otro NO es una auditoría: es un deseo. Refutar primero, siempre, especialmente cuando el resultado que esperas es el cómodo."
      ],
      "ejercicio": "Toma un control de seguridad real de MOONKEY (p.ej. 'un no-admin no puede leer perfiles ajenos'). 1) Formúlalo como hipótesis refutable. 2) Escribe DOS pruebas: la del ataque (como user_A intento leer la fila de user_B → espero vacío) y la de contraste (como user_A leo MI fila → espero 1 fila; o como admin leo todas → espero N). 3) Ejecuta `get_advisors` (o documenta qué señalaría) y para cada lint clasifícalo como 'hipótesis confirmada como riesgo real' o 'refutada (no explotable porque…)'. 4) Escribe un mini-reporte donde cada afirmación va acompañada de su comando reproducible.",
      "entregable": "`AUDIT.md`: un reporte donde cada hallazgo (vulnerable o seguro) lleva su PoC reproducible — ataque + caso de contraste positivo — y donde cada lint de `get_advisors` está marcado como confirmado o refutado con su razón.",
      "insight": "Una prueba de seguridad que 'pasa' sin caso de contraste positivo es un falso negativo disfrazado: el select vacío puede deberse a RLS o a una sesión caducada. Verifica siempre que tu arma dispara antes de declarar inmune al blanco — refutar primero aplica también a tu propio método.",
      "errores": [
        "Reportar un hallazgo por inspección de código sin un PoC ejecutable: 'la policy parece floja' no es un hallazgo, es una corazonada.",
        "Tratar los lints de `get_advisors` como veredictos en vez de hipótesis a refutar — generan falsos positivos que, repetidos, queman tu credibilidad.",
        "Dar por seguro un control porque el ataque devolvió vacío, sin probar que el mismo query SÍ devuelve datos cuando debe (control positivo).",
        "Gritar 'anon key expuesta' como vulnerabilidad cuando es pública por diseño: el falso positivo clásico que delata no haber entendido el modelo de seguridad.",
        "Auditar solo en la dirección cómoda (confirmar lo que esperas) en vez de atacar igual de fuerte los controles que crees seguros."
      ]
    },
    "OS-04": {
      "objetivo": "Responder a un incidente en producción con un procedimiento frío: detectar, contener, hacer rollback de forma segura, y conducir un post-mortem sin culpa que cierre la causa raíz.",
      "porque": "Cuando algo se rompe en producción, el instinto (improvisar, tocar la DB en caliente, ocultar el error) suele empeorar el daño. La diferencia entre un susto de diez minutos y un fin de semana perdido es tener un runbook escrito ANTES del incidente. Sin él, un rollback mal hecho destruye datos que el bug solo había corrompido.",
      "leccion": [
        "Interioriza la secuencia DCRP: Detectar → Contener → Recuperar (rollback) → Post-mortem. El error de la mayoría es saltar directo a 'arreglar', tocando producción a ciegas. El orden importa porque cada fase protege a la siguiente: si no detectas bien, contienes lo que no es; si no contienes, el daño crece mientras recuperas; si recuperas sin entender, repites el incidente. Y la regla cero, escrita en cualquier runbook serio: en un incidente NO improvisas sobre producción. Ejecutas pasos predefinidos. La creatividad es para el post-mortem, no para las 3am con el sitio caído.",
        "Detectar es saber QUÉ está roto y desde CUÁNDO. En MOONKEY (Cloudflare Pages + Supabase) tus señales son: el deploy de Cloudflare (¿qué commit está live? ¿el último build pasó?), los logs de Supabase (`get_logs` por servicio: api, postgres, auth — ahí ves errores de policy, queries fallando, picos de 4xx/5xx), y la queja del usuario (a menudo la primera señal). Lo primero que estableces es la línea de tiempo: ¿qué cambió justo antes? Casi siempre el incidente correlaciona con un deploy o una migración reciente. 'Empezó tras el merge de las 16:20' acota el espacio de búsqueda de horas a minutos.",
        "Contener es detener el sangrado antes de curar la herida. Pregunta clave: ¿esto está corrompiendo datos AHORA mismo? Si un bug está escribiendo filas malas en `proofs` o `progress`, cada minuto que tardas multiplica el cleanup. Opciones de contención según severidad: revertir el deploy de Cloudflare al anterior (un click — Cloudflare Pages guarda deploys previos y permite rollback instantáneo, esta es tu palanca más rápida y segura), deshabilitar la feature rota, o en el caso extremo de escritura destructiva, cortar el acceso a nivel de policy. Contener NO es arreglar: es congelar el daño en su estado actual para tener tiempo de pensar.",
        "Recuperar exige distinguir DOS rollbacks que la gente confunde y que tienen riesgos opuestos. (1) Rollback de CÓDIGO: volver a un deploy/commit anterior. Es barato, reversible y casi siempre seguro — en Cloudflare Pages es inmediato. Esta es tu primera opción. (2) Rollback de SCHEMA/DATOS: revertir una migración o restaurar datos. Es PELIGROSO e irreversible: un `down` migration que hace `DROP COLUMN` borra datos que quizá solo estaban corruptos, no perdidos. Reglas: nunca corras un rollback de datos sin un backup/PITR confirmado primero; prefiere una migración FORWARD que corrija (un nuevo `UPDATE` que repara las filas malas) sobre un `down` destructivo; toda migración de Supabase es versionada (`apply_migration`) precisamente para que el estado sea reconstruible. Si la duda es 'rollback de código o de datos', empieza SIEMPRE por el de código: a menudo basta y no toca la DB.",
        "El post-mortem es donde el incidente paga su deuda convirtiéndose en aprendizaje. Es SIN CULPA (blameless): el objetivo no es quién rompió, sino qué del SISTEMA permitió que un humano normal rompiera. Estructura: timeline (qué pasó y cuándo, minuto a minuto), impacto (a cuántos usuarios, qué datos, cuánto tiempo), causa raíz (los '5 por qués' hasta llegar al fallo de sistema, no de persona), y acciones correctivas con dueño y fecha. Cada incidente debe producir al menos un control que lo habría PREVENIDO o DETECTADO antes — un test, un advisor en CI, una alerta. Si el post-mortem no cambia nada del sistema, no fue un post-mortem, fue una confesión.",
        "Ancla el runbook en la realidad de cada proyecto, porque la severidad y las palancas cambian. En XCAP existe una invariante de capital sagrada — el capital se acumula entre sesiones y solo se mueve en cierres realizados, nunca se resetea. Un incidente que toque esa invariante NO se arregla con un rollback de datos a ciegas: borrarías historia contable real. Ahí el runbook obliga a corrección forward y verificación de la invariante antes de tocar nada. En Espejo, multi-tenant, un incidente tiene una pregunta extra en la fase de detección: ¿el fallo cruzó la frontera entre tenants? Si un bug expuso datos de un tenant a otro, además del rollback hay obligación de contención de la fuga. El runbook genérico te da la espina dorsal; los invariantes de cada proyecto te dan las reglas que NUNCA violas durante el pánico."
      ],
      "ejercicio": "Escribe el `RUNBOOK.md` de incidentes para MOONKEY: 1) La sección Detectar (qué comandos/paneles miras: deploy de Cloudflare, `get_logs` de Supabase por servicio, cómo estableces la timeline). 2) La sección Contener con la palanca de rollback de Cloudflare Pages paso a paso. 3) Un árbol de decisión 'rollback de código vs rollback de datos' con la regla 'empieza por código, datos solo con PITR confirmado'. 4) Una plantilla de post-mortem blameless (timeline / impacto / 5-por-qués / acciones correctivas con dueño). 5) Simula un incidente: 'tras el deploy de las 16:20, los usuarios no pueden guardar proofs' y escribe la respuesta paso a paso siguiendo tu propio runbook.",
      "entregable": "`RUNBOOK.md` con las cuatro fases DCRP operacionalizadas para Cloudflare Pages + Supabase, el árbol de decisión código-vs-datos, una plantilla de post-mortem blameless, y un incidente simulado resuelto end-to-end con tu propio procedimiento.",
      "insight": "Hay dos rollbacks con riesgos opuestos: el de código es barato y reversible (empieza siempre por ahí), el de datos es destructivo e irreversible. Ante la duda, una migración forward que repara filas corruptas es casi siempre mejor que un `down` que las borra — porque a menudo los datos estaban dañados, no perdidos.",
      "errores": [
        "Saltar directo a 'arreglar' tocando producción a ciegas en vez de seguir Detectar→Contener→Recuperar: la improvisación a las 3am es cómo un susto se vuelve un desastre.",
        "Hacer un rollback de datos (down migration con DROP) sin backup/PITR confirmado, destruyendo datos que el bug solo había corrompido.",
        "No establecer la timeline antes de actuar: sin saber qué deploy o migración disparó el incidente, buscas la causa en todo el sistema en vez de en los últimos cambios.",
        "Confundir contener con arreglar: dejar el bug escribiendo filas malas mientras 'investigas' multiplica el cleanup posterior.",
        "Cerrar el incidente con un post-mortem que busca culpable en vez de un fallo de sistema, y que no produce ni un solo control nuevo que lo habría prevenido."
      ]
    },
    "OS-05": {
      "objetivo": "Hacer que varios proyectos compartan una única base de datos Postgres sin que uno pueda leer, escribir o romper los datos de otro — aislamiento multi-proyecto y multi-tenant impuesto por la DB, no por convención.",
      "porque": "Compartir una instancia Supabase entre proyectos ahorra coste pero crea el riesgo más silencioso de la galaxia: que el cliente de MOONKEY, con su anon key, lea las tablas de XHUB IRON. El aislamiento por 'acuérdate de no tocar esas tablas' es papel; un día alguien lo toca. El aislamiento real es estructural y se verifica atacándolo.",
      "leccion": [
        "Parte del hecho concreto: la instancia `wuchsslgbqlhyxljsmxi` aloja DOS productos. MOONKEY usa `profiles, progress, feedback, leads, proofs`. XHUB IRON usa `iron_*, world_*, focus_*, daily_focus_history`. Comparten el mismo Postgres, el mismo `auth.users`, y — clave — la MISMA anon key se usa para hablar con la DB. Esto significa que el cliente público de MOONKEY técnicamente PUEDE intentar `from('iron_command').select('*')`. La única razón de que no funcione tiene que ser una barrera dura en la DB, no la nota de CLAUDE.md que dice 'no las toques'. Esa nota protege contra que TÚ las edites por error escribiendo código; no protege contra un atacante que ya tiene tu anon key.",
        "Entiende las capas de aislamiento de menor a mayor fuerza. (1) Convención (nombres `iron_*` vs sin prefijo): organiza, no aísla — un atacante ignora convenciones. (2) RLS por tabla: cada tabla de XHUB debe tener policies que solo concedan acceso a su propio rol/usuario; una tabla `iron_*` sin RLS o con una policy permisiva es una fuga directa. (3) Schemas separados: poner XHUB en su propio schema Postgres y NO exponerlo en la API de PostgREST (la `db.schema` que Supabase publica) es una barrera más fuerte — lo que no está en el schema expuesto, el cliente ni lo ve. (4) Roles/grants: REVOKE de `anon`/`authenticated` sobre las tablas/objetos ajenos. La defensa robusta APILA estas capas; no confíes en una sola.",
        "Para el caso MOONKEY↔XHUB, la pregunta de auditoría es directa: ¿puede un usuario autenticado de MOONKEY leer una fila de `iron_*`? Refútalo (OS-03): autentícate como user normal de MOONKEY y ejecuta `supabase.from('iron_command').select('*')`. Lo correcto es que devuelva error de permiso o vacío por RLS. Si devuelve datos, tienes una fuga de aislamiento crítica. La memoria de proyecto dice que la salud se computa y que el modelo es 'open RLS, computed health' para el HUB — eso significa que debes verificar EXPLÍCITAMENTE que 'open' para el HUB no significa 'abierto a clientes de MOONKEY'. Aislamiento entre proyectos = cada tabla solo responde a su dueño legítimo, probado por impersonación cruzada.",
        "El segundo eje es el multi-tenant DENTRO de un proyecto, que es Espejo. Aquí no son dos productos, son N tenants (cada influencer) en las mismas tablas. El aislamiento se vuelve por-fila: cada fila lleva un `tenant_id`, y la policy RLS exige que `tenant_id` coincida con el tenant del llamante. El ataque a refutar: el tenant A intenta leer/escribir filas con el `tenant_id` de B. La trampa clásica es el UPDATE sin WITH CHECK: USING limita qué filas A ve, pero sin WITH CHECK, A podría reasignar una fila a B o crear una con `tenant_id` ajeno. Otra trampa: una RPC SECURITY DEFINER que recibe `tenant_id` como parámetro y no valida que coincide con `auth.uid()` — boquete que cruza todos los tenants. El aislamiento multi-tenant solo es real si CADA policy de cada operación (SELECT/INSERT/UPDATE/DELETE) ancla el tenant al identidad del llamante, no a un parámetro que el cliente controla.",
        "Hay vectores de cruce que no son SELECT y que la gente olvida. `auth.users` es compartido: un usuario es el mismo en MOONKEY y en XHUB — asegúrate de que tener cuenta en uno no concede roles en el otro (en MOONKEY, `role` y `founder_badge` son inmutables para no-admins precisamente para que registrarse no escale privilegios cruzados). Las RPC y funciones SECURITY DEFINER son objetos globales: una función de XHUB ejecutable por `authenticated` es invocable desde el cliente de MOONKEY — restríngela por rol o valida el contexto dentro. Triggers y secuencias compartidas, vistas que hacen JOIN entre tablas de ambos productos, y `get_advisors` que señala RLS faltante en cualquier tabla del proyecto: todo eso es superficie de cruce. El aislamiento no es una policy, es una propiedad del sistema entero.",
        "Cierra con la disciplina operativa que mantiene el aislamiento vivo en el tiempo. Cada cambio de schema entra como migración versionada (`apply_migration`), nunca como edición manual en el panel — así el estado de seguridad es reconstruible y auditable. Tras cada migración que toque tablas o policies, corre `get_advisors` para cazar RLS desactivado o policies permisivas introducidas sin querer. Y mantén un test de aislamiento como parte del runbook: un script que, como usuario de MOONKEY, intenta leer cada prefijo ajeno (`iron_*`, `world_*`) y falla el build si alguno devuelve filas. El aislamiento entre proyectos no se 'configura una vez'; se re-verifica en cada cambio, porque una migración inocente puede abrir una puerta que llevaba meses cerrada."
      ],
      "ejercicio": "Sobre la DB compartida `wuchsslgbqlhyxljsmxi` (o una réplica de prueba): 1) Lista en `ISOLATION.md` las tablas de cada producto (MOONKEY vs XHUB) y la capa de aislamiento que protege a cada una (RLS / schema no expuesto / REVOKE). 2) Escribe el test de impersonación cruzada: como usuario normal de MOONKEY, intenta `select('*')` sobre `iron_command` (o cualquier `iron_*`/`world_*`) y documenta el resultado esperado (error/vacío). 3) Para el caso multi-tenant de Espejo, escribe las cuatro policies (SELECT/INSERT/UPDATE/DELETE) de una tabla con `tenant_id`, asegurando que UPDATE lleva WITH CHECK y que ninguna confía en un `tenant_id` pasado por el cliente. 4) Documenta cómo `get_advisors` entra en tu rutina post-migración.",
      "entregable": "`ISOLATION.md`: el mapa de tablas→capa-de-aislamiento para los dos productos de la DB compartida, un test ejecutable de impersonación cruzada MOONKEY→XHUB con resultado esperado, y el set de cuatro policies RLS por-tenant de Espejo con WITH CHECK correcto en las operaciones de escritura.",
      "insight": "Compartir Postgres entre proyectos es seguro solo cuando el aislamiento es estructural (RLS + schema no expuesto + REVOKE), no documental: la nota 'no toques esas tablas' protege contra tus errores de código, jamás contra un atacante que ya tiene tu anon key y enumera prefijos ajenos.",
      "errores": [
        "Confiar el aislamiento a convenciones de nombres o a una nota en CLAUDE.md: un atacante con la anon key ignora ambas y prueba `from('iron_*').select('*')` directamente.",
        "Dejar una tabla ajena (`iron_*`, `world_*`) sin RLS o con policy permisiva en una DB cuya anon key comparten varios proyectos — fuga directa entre productos.",
        "En multi-tenant, escribir UPDATE/INSERT con USING pero sin WITH CHECK, permitiendo que un tenant reasigne o cree filas con el `tenant_id` de otro.",
        "Construir una RPC SECURITY DEFINER que confía en un `tenant_id` (o `user_id`) pasado como parámetro por el cliente en vez de derivarlo de `auth.uid()`.",
        "Tratar el aislamiento como configuración de una vez y no re-correr `get_advisors` tras cada migración: un cambio inocente puede reabrir una puerta cerrada hace meses."
      ]
    },
    "OS-06": {
      "objetivo": "Montar un panel de observabilidad y un presupuesto con alertas para un sistema vivo (Astro en Cloudflare Pages + Supabase): definir qué logs y métricas vigilar, qué dispara una alerta, y qué techos de uso y de gasto en tokens te avisan ANTES de que llegue la factura sorpresa.",
      "porque": "Lanzar no es terminar: es el momento en que empiezas a operar. Un sistema en producción genera tres flujos que casi nadie mira hasta que duelen: logs (qué está pasando), errores (qué se rompe sin que nadie te lo diga) y coste (cuánto te cobran por seguir vivo). MOONKEY LAB, XHUB, Espejo y XCAP comparten infraestructura real con planes free/pro y cuotas reales de Supabase y Cloudflare, y todo el uso de IA se factura por token. El operador que no monitoriza descubre los problemas por dos vías, ambas caras: un usuario que se queja, o un cargo en la tarjeta. La observabilidad convierte 'no sé qué pasa en producción' en 'tengo un tablero que me lo dice'. El coste es seguridad: un endpoint abierto que alguien martillea no solo es un agujero, es una factura. Esta es la última disciplina de la constelación porque cierra el ciclo: construiste, aseguraste, ahora operas con los ojos abiertos.",
      "leccion": [
        "Los tres flujos de un sistema vivo. Logs = el diario de qué ocurre (peticiones, queries, auth). Errores = lo que se rompe en silencio del lado del cliente, donde tus logs de servidor no llegan. Coste = el contador que corre aunque nadie use el sistema. Son tres tuberías distintas con tres herramientas distintas; confundirlas es por qué la gente cree estar monitorizando y no lo está.",
        "Observabilidad en una SSG no es lo mismo que en un servidor. MOONKEY es estático: NO hay handler server-side donde poner un logger. Tu telemetría vive en tres sitios ajenos a tu HTML: (1) Cloudflare Pages Analytics (requests, ancho de banda, errores de edge), (2) los logs de Supabase (Dashboard → Logs: API, Postgres, Auth — cada query que pasa por PostgREST y cada login deja rastro), y (3) el navegador del usuario (errores de JS que solo tú no ves). Acéptalo: en una SSG, gran parte de lo que falla ocurre en una máquina que no controlas.",
        "Qué vigilar de verdad, no todo. La tentación es loguearlo todo y no leer nada. Define un puñado de señales que importan: tasa de error 4xx/5xx en el edge (un pico de 401/403 = RLS rechazando o un atacante probando), errores de Auth en Supabase (intentos fallidos de magic-link), latencia de las queries lentas (Supabase marca las que tardan), y volumen de filas leídas (una query sin filtro que de pronto devuelve 10.000 filas es un bug de coste). Una métrica que no dispara ninguna decisión es ruido; bórrala.",
        "Error tracking del lado del cliente: el punto ciego. En un sitio estático, un TypeError en tu JS rompe la experiencia del usuario y a ti no te llega NADA — ni log de servidor ni alerta. Necesitas capturar errores en el navegador y mandarlos fuera. Lo mínimo honesto: un window.addEventListener('error', ...) y ('unhandledrejection', ...) que haga un fetch a un endpoint propio (una Edge Function de Supabase que inserta en una tabla de logs con RLS de solo-insert). Lo robusto: Sentry o similar con su SDK gratuito hasta cierto volumen. La regla: si no capturas errores de cliente, estás operando a ciegas en la mitad de tu sistema.",
        "El coste real de operar, desglosado. Supabase free tier tiene techos concretos que, al cruzarlos, o te cortan o te cobran: filas de DB, ancho de banda de egreso, almacenamiento, Monthly Active Users de Auth, e invocaciones de Edge Functions. Cloudflare Pages es generoso pero tiene límites de builds/mes y de requests en funciones. Y el gasto que más escala y menos se ve: los tokens de IA. Cada llamada a la API de Claude se factura por tokens de entrada y de salida, con tarifas distintas; un prompt con un CLAUDE.md gigante o un bucle de agente sin tope puede multiplicar el coste sin que cambie 'lo que hace' la app. El operador conoce sus tres facturas: DB, edge y tokens.",
        "Por qué llegan las facturas sorpresa, y cómo no recibirlas. Tres causas clásicas: (1) un endpoint o Edge Function sin rate-limit que alguien descubre y martillea — uso = dinero; (2) una query sin LÍMITE ni índice que escanea toda la tabla en cada carga; (3) un loop de agente IA sin tope de tokens ni de iteraciones (el XCAP capital invariant existe en parte por esto: bucles que acumulan aprendizaje, no coste). La defensa no es vigilar la factura a fin de mes, es poner los frenos ANTES: presupuestos con alerta al 50/80/100%, límites duros donde la plataforma los ofrezca, y techos explícitos de max_tokens y de iteraciones en cada llamada de IA.",
        "Alertar sobre lo que importa, sin fatiga de alertas. Una alerta que salta cada hora se ignora a la semana. El criterio: alerta solo lo accionable y lo urgente. Bueno: 'el gasto del mes superó el 80% del presupuesto', 'la tasa de 5xx pasó del 1%', 'errores de cliente > X en 10 min'. Malo: 'hubo una petición'. Configura los budget alerts que ya te dan Supabase, Cloudflare y la consola de Anthropic — son gratis y son tu primera línea. Cada alerta debe responder a 'qué hago cuando salta'; si no hay acción, no es una alerta, es ansiedad.",
        "Cerrar el bucle con la respuesta a incidentes (OS-04). Observar sin un plan de reacción es solo mirar arder. El panel y las alertas de este módulo son los sensores; el rollback y el incident response de OS-04 son los actuadores. Un sistema bien operado conecta los dos: la alerta te despierta, los logs te dicen qué pasó, y el plan de respuesta te dice qué hacer. Observabilidad + coste + respuesta = operar de verdad, no rezar."
      ],
      "ejercicio": "Operas MOONKEY LAB (Astro SSG en Cloudflare Pages + Supabase moonkey-lab + posibles llamadas de IA). Monta su observabilidad real y su presupuesto en cuatro pasos.\n\nPASO 1 — Mapa de señales (entregable escrito). Abre el Dashboard de Supabase → Logs y revisa los tres streams (API, Postgres, Auth). Documenta en una tabla qué señal vigilas en cada uno (ej. Auth: tasa de magic-links fallidos; API: ratio de 401/403; Postgres: queries > 500ms), y de cada una: umbral, qué significa cruzarlo, y qué acción dispara. Mínimo 6 señales. Una sin acción se descarta.\n\nPASO 2 — Captura de errores de cliente. Implementa un script global (en Layout.astro, junto al IntersectionObserver ya global — no lo dupliques) con window.addEventListener('error') y ('unhandledrejection') que haga fetch a una Edge Function de Supabase. Crea la Edge Function y una tabla client_errors con RLS de SOLO insert para anon (nunca SELECT para anon: los logs no se leen desde el cliente). Aplícala como migración versionada y corre get_advisors después (regla de coste del proyecto). Provoca un error a propósito y verifica que la fila aparece en la tabla.\n\nPASO 3 — Las tres facturas y sus topes. Documenta los límites actuales y dónde se ven: (a) Supabase — uso de DB, egreso, Auth MAU, invocaciones de funciones (Dashboard → Reports/Usage); (b) Cloudflare — builds y requests (Pages → Analytics); (c) tokens de IA — si hay llamadas a la API de Claude, calcula el coste de una llamada típica (tokens de entrada del CLAUDE.md + prompt, tokens de salida esperados, por la tarifa vigente del modelo). Para cada factura, anota el techo gratuito y a qué % estás hoy.\n\nPASO 4 — Frenos y alertas. Activa budget alerts en la consola de Anthropic y en Supabase (o documenta exactamente dónde se configuran si tu plan no los expone). Para cualquier llamada de IA en el sistema, fija un max_tokens explícito y un tope de iteraciones si es un bucle. Entrega un runbook de una página: 'cuando salte la alerta X → mira el log Y → ejecuta la acción Z (que enlaza con el rollback de OS-04)'.\n\nNo entregues capturas de 'parece que funciona'. Entrega: la tabla de señales, la Edge Function + migración corriendo con una fila de error real capturada, el desglose de las tres facturas con porcentajes, y el runbook que conecta alerta → log → acción.",
      "entregable": "Un panel de observabilidad operativo para MOONKEY LAB con: (1) una tabla de 6+ señales (umbral · significado · acción) sobre los logs reales de Supabase y Cloudflare; (2) captura de errores de cliente funcionando — Edge Function + tabla client_errors con RLS solo-insert, aplicada como migración versionada y verificada con una fila de error real; (3) el desglose de las tres facturas (Supabase, Cloudflare, tokens de IA) con el techo gratuito y el % de uso actual de cada una; y (4) un runbook de una página con budget alerts activos que conecta cada alerta con su log y su acción de respuesta (enlazando con el rollback de OS-04).",
      "insight": "En una SSG no hay servidor donde loguear, así que la mitad de lo que falla ocurre en el navegador del usuario y nunca te llega: si no capturas errores de cliente, no estás monitorizando, estás adivinando. Y el coste es una superficie de ataque, no solo una línea contable — un endpoint sin freno que alguien martillea es a la vez un agujero de seguridad y una factura sorpresa, así que los topes (max_tokens, rate-limit, budget alerts al 80%) son seguridad, no contabilidad. La factura no se vigila a fin de mes; se pone el freno antes.",
      "errores": [
        "Creer que loguear = observar. Acumular logs que nadie lee no es observabilidad; observabilidad es tener un puñado de señales que disparan decisiones. Una métrica que no acciona nada es ruido: bórrala.",
        "Ignorar los errores de cliente porque 'el servidor no reporta nada'. En una SSG el servidor no PUEDE reportar el TypeError que rompe la pantalla del usuario. Sin captura en el navegador, operas a ciegas en media app.",
        "Abrir una tabla de logs a SELECT para anon. Los logs se insertan desde el cliente pero JAMÁS se leen desde él (filtrarías datos de otros usuarios). RLS de solo-insert para anon, lectura solo admin — y pasa get_advisors.",
        "Lanzar un bucle de agente IA o una Edge Function sin max_tokens ni tope de iteraciones. Es la causa nº1 de factura sorpresa: el coste escala invisible mientras 'lo que hace' la app no cambia.",
        "Vigilar la factura a fin de mes en vez de poner budget alerts al 50/80/100% antes. Cuando ves el cargo, ya lo pagaste; la alerta existe para frenar, no para lamentar.",
        "Fatiga de alertas: configurar avisos para todo hasta que el equipo los silencia. Alerta solo lo accionable y urgente; cada alerta debe responder a 'qué hago cuando salta' o no debería existir.",
        "Tocar tablas de otros proyectos en la DB compartida (iron_*, world_*, focus_*) al montar logging. La tabla de errores es de MOONKEY; créala con prefijo propio y no pises XHUB IRON."
      ]
    }
  }
};

/** Returns the lesson for a constellation module, or null if none authored yet. */
export const getLesson = (constellationId: string, code: string): Lesson | null =>
  curriculum[constellationId]?.[code] ?? null;

/** How many modules in a constellation have a full lesson. */
export const lessonCount = (constellationId: string): number =>
  Object.keys(curriculum[constellationId] ?? {}).length;
